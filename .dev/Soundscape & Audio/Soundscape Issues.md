# Issues to Solve re: Soundscape

1. SoundscapeFeaturedMusicTest() includes a noticeable double-play of the first split second of the looping track following the play of the intro.
2. I need a way to balance volume. Create a new button in the "== DEBUG ==" panel, called "Debug Soundscape".  When clicked, it opens a panel that is centered on screen and large enough to contain one button for every single track, divided into "Background Music", "Featured Music", "Location", "Weather", "Rain", "Wind", and "Thunder".  When clicked, the button will play the track (crossfading out another track in the same category if one is playing) and add it to a new section at the bottom, "Playing Tracks".  Each track in "Playing Tracks" will have its own row, and a slider to control volume. The slider should be initialized to whatever the volume the track is playing at, and the numeric volume should be displayed next to the slider.  Moving the slider should change the volume of the track immediately, and update the numeric volume display.
# Multiplayer Authority Audit — 2026-06-25

**Agent:** Cursor session audit per [Multiplayer Performance Audit.md](Multiplayer%20Performance%20Audit.md)
**Scope:** Policies P1–P10 ([Preparing For Multiplayer §1](Preparing%20For%20Multiplayer.md))
**Linear:** TOR-144 (multiplayer E2E), TOR-221 (bootstrap/host guards), TOR-197 (event listener policy)

---

## Executive summary

| Verdict | Count |
| --- | --- |
| **Pass** (mechanical compliance) | 38+ areas (Waves 1–5 closed) |
| **Fail** (fix before trusting multiplayer) | 0 mechanical (pending TOR-144 multiclient) |
| **Risk** (likely OK / needs TOR-144 confirmation) | P6 roll paths, P10 live gameState |
| **Doc drift** (P9) | Resolved W5 |

**TOR-144 gate:** All mechanical P0/P1 blockers from audit 2026-06-25 **Waves 1–5 are implemented** (solo verified). **Remaining:** Wave 6 — friend multiclient session (**TOR-249**).

---

## Mechanical search summary

| Query | Result | Notes |
| --- | --- | --- |
| `^function Global` in `core/global_script.ttslua` | **51** entries | Includes read-only + gates |
| `requireHostForWorldMutation` / `isHostClient` in `global_script.ttslua` | **44** hits | Tier C Global paths mostly covered |
| Same guards in `core/sync.ttslua` | **7** hits | All orchestrator mutators gated |
| Same guards in `lib/util.ttslua` | **3** hits | Gate implementations |
| `require("core.*")` in `objects/` | **0** mutating (comment only in control board) | P8 pass |
| `require("core.*")` in `ui/` | **0** | P8 pass |
| `^function HUD_` in `global_script.ttslua` | **99** handlers | **32** use `requireStorytellerHostForMutation` |
| `GlobalReportBagDiceCount` call sites | **0** | Dead entry; doc lists as read-only (incorrect) |
| World I/O in `objects/*.ttslua` | `dice_bag.ttslua` | destroy/spawn paths |
| World I/O in `ui/*.ttslua` | `ui_tarot_button.ttslua`, `ui_csheet_core.ttslua` | tarot poses; csheet page Y snap |

---

## Findings table

| ID | Sev | Policy | Location | Issue | Recommended fix |
| --- | --- | --- | --- | --- | --- |
| **MPA-001** | **P0** | P2, P7 | `core/global_script.ttslua` chunk `trEarlySilence*` | ~~Chunk load ran on all clients~~ **Fixed W1** — host gate at `trEarlySilenceSoundscapeEmitters` / `trScheduleEarlySoundscapeSilenceDeferred` entry. | TOR-144 phase A4 |
| **MPA-002** | **P0** | P2 | `core/global_script.ttslua` chunk `SS.bootstrapSilenceStrayEmitterLoops` | ~~Chunk load ran on all clients~~ **Fixed W1** — wrapped in `U.isHostClient()`. | TOR-144 phase A4 |
| **MPA-003** | **P1** | P2, P5, P8 | `ui/ui_tarot_button.ttslua` | ~~Tier C without Global.call~~ **Fixed W2** — `GlobalApplyTarotState` + `lib/tarot_toggle.ttslua`; object click routes via `Global.call` only. | TOR-144: tarot toggle once |
| **MPA-004** | **P1** | P2 | `objects/dice_bag.ttslua` `onLoad` | ~~`destroyObject` on fan-out~~ **Fixed W2** — host gate before stale spawn destroys. | Join load: no destroy spam |
| **MPA-005** | **P1** | P3, P2 | `HUD_pcPanel` | ~~No ST+host gate~~ **Fixed W3** — `requireStorytellerHostForMutation` before `PCST.onHudClick`. | TOR-144 phase D |
| **MPA-006** | **P2** | P2 | `HUD_debugLight*` | ~~No host gate~~ **Fixed W3** — ST+host on Activate/Enabled/ResetRow/Slider. | Join client cannot move spotlights |
| **MPA-007** | **P2** | P6, P1 | `GlobalReportBagDiceCount` | ~~Dead mutator~~ **Fixed W3** — removed (zero call sites). | grep clean |
| **MPA-008** | **P2** | P2 | `GlobalWaitDrawerThenReleaseBagDice` | ~~No coroutine host guard~~ **Fixed W4** — `requireHostForWorldMutation` at entry. | Phase C roll |
| **MPA-009** | **P2** | P6 | `HUD_rollInitiate`, `HUD_rollRollButton`, `HUD_rollConfirm`, etc. | Tier B roll FSM / Tier C via `Global.call` — **intentionally** not full-handler host-gated (P6). | **Pass** — document in Event Listener Policy; TOR-144 phase C validates join-client roll. |
| **MPA-010** | **P2** | P2 | `ui/ui_csheet_core.ttslua` onLoad layout | ~~Local setPosition fan-out~~ **Fixed W4** — host gate before `applyCsheetLayoutFromFirstSpread` world snap. | Manual csheet page test |
| **MPA-011** | **P3** | P9 | Event Listener Policy | ~~Stale inventory~~ **Fixed W5** — HUD appendix + TOR-144 column; removed `GlobalReportBagDiceCount`. | Living doc |
| **MPA-012** | **P4** | P10 | `gameState` on join clients | Known gap: no live Lua table broadcast. | Document in TOR-144 phase D; **do not** remove host guards. |

---

## Domain pass summary

### Helpers & gates — **PASS**

- `U.isStorytellerSteamPlayer`, `U.isHostClient`, `U.requireHostForWorldMutation` in `lib/util.ttslua` L2035–2081.
- `requireStorytellerHostForMutation` in `global_script.ttslua` L1905–1909 combines steam + host for ST XmlUI.
- Bundle gates `GlobalIsStorytellerSteamPlayer`, `GlobalRequireHostForWorldMutation` L271–288.

### Bootstrap & load — **PARTIAL FAIL** (MPA-001, MPA-002)

- Join branch L1619–1628: `M.onLoadJoinClient`, UI-only `Sync.full`, early return — **PASS**.
- Host branch: soundscape, `M.onLoad`, table sync, full reconcile — **PASS**.
- `onSave` L1780: host-only — **PASS**.
- Chunk-load silence **before** `onLoad` — **FAIL**.

### Built-in Global handlers — **PASS**

| Handler | Guards | Notes |
| --- | --- | --- |
| `onObjectPickUp` / `onObjectDrop` | steam + host + tag | Gameboard `require` after gates — P4 OK |
| `onObjectRandomize` | d10 tag only; **no** full host gate | **P6 intentional** |
| `onObjectLeaveContainer` | d10 + host before `setGMNotes` / drawer | **PASS** |
| `onPlayerConnect` / `onPlayerChangeColor` | host | **PASS** |

### `Global.call` mutators — **PASS** (except MPA-007 dead code)

All Tier C gameboard, signal, dice spawn/destroy/release, roll cleanup entries checked via grep — **41** mutating paths use `requireHostForWorldMutation` except:

- Read-only getters (correct).
- `GlobalPostRollModifyPool` — delegates to guarded add/remove (**risk** MPA-008 style, acceptable).
- `GlobalSpawnActivePoolDiceForActive` — guard in `spawnActivePoolDiceForActive` L3806 (**PASS**).
- `GlobalGameboardInstallPaletteSnaps` — `isHostClient()` L397 (**PASS**).

### `Sync.*` — **PASS**

`core/sync.ttslua`: non-host `Sync.full` → UI delta only L375–377; `Sync.npcs`, `Sync.lighting`, `Sync.soundscape`, `Sync.lightRef`, `Sync.npcCutouts` no-op; `Sync.player` splits lights (host) vs UI (all).

### HUD handlers — **PARTIAL**

- **32** ST-world handlers use `requireStorytellerHostForMutation` (scenes, soundscape, reset, sync, anchors, spotlights, post-roll ST cells, debug roll tests).
- **Roll / PC paths** intentionally unguarded at HUD top — Tier B + `Global.call` for Tier C (**MPA-009**).
- **Gap:** ~~`HUD_pcPanel`~~ **Fixed W3**; ~~`HUD_debugLight*`~~ **Fixed W3**.
- **Tier A pass:** `HUD_togglePanel`, `HUD_selectStorytellerPanel`, scene modals (`HUD_scenesOpenDistrictModal`, etc.), `HUD_scenesLibSlot` (library key only).

### Object & UI scripts — **PARTIAL FAIL**

| Script | Verdict |
| --- | --- |
| `npc_control_board.ttslua` | **PASS** — steam via Global; mutators in Global |
| `npc_control_board_palette.ttslua` | **PASS** — install snaps host-guarded in Global |
| `dice_bag.ttslua` | **PASS** — spawn paths host-guard; **onLoad destroy host-gated** (W2) |
| `ui_signal_candle.ttslua` | **PASS** → `GlobalToggleSignalFireState` |
| `ui_tarot_button.ttslua` | **PASS** — `GlobalApplyTarotState` (W2) |
| `ui_csheet_core.ttslua` | **PASS** rolls via `GlobalInitiateRoll`; page snap **risk** MPA-010 |

### P7 Dual-apply — **PASS** (spot-check)

Soundscape HUD uses `commitEagerSteadyState`; dual-apply survey rows marked mitigated — no new un-fingerprinted eager+`Sync.full` paths found in `HUD_soundscape*` / `StorytellerScenesPanel` skim.

---

## Registry drift

| Item | Drift |
| --- | --- |
| `GlobalReportBagDiceCount` | Listed Tier A read-only in Event Listener Policy; **mutates** roll pool; **zero** call sites |
| `HUD_*` inventory | ~84 handlers undocumented in policy table |
| TOR-144 status | Policy marks host inventory “Phase Done”; Multiplayer-E2E states **not validated** until two clients |

---

## Intentional exceptions (do not “fix” blindly)

- **`onObjectRandomize`** — Tier B on all clients; host-only lights via `Sync.player` inside roll controller (**P6**).
- **`GlobalRollSeatCamera` / `GlobalSetSheetCamera`** — per-player camera; local presentation, not shared world authority.
- **`Sync.ui` / `UpdateUIDisplays`** — all clients (**Tier A**).
- **P10 live `gameState`** — separate follow-up; not a host-guard removal.

---

## Supplemental discoveries (not in seed list)

1. **`SS.bootstrapSilenceStrayEmitterLoops` at chunk load** — paired with MPA-001; easy to miss because host `onLoad` *also* calls it (L1651) — join client may run **twice** if chunk path kept.
2. **`HUD_soundscapeInspect`** — no host gate; read-only inspect — OK.
3. **`HUD_saveState`** — no host gate; triggers encode only — OK (actual save via host `onSave`).
4. **`core/npc_gameboard.ttslua`** `Sync.npcs` call sites — safe when reached only from host-guarded Global/HUD paths (verified entry guards on Apply/Clear).
5. **`lib/e2e_gameboard.ttslua`** — test harness calls `Sync.npcs({ force = true })`; dev-only, out of multiplayer scope.

---

# Multiplayer Authority — Implementation Plan (from audit 2026-06-25)

## Wave 1 — Chunk bootstrap (MPA-001, MPA-002) — **P0** — **Done 2026-06-25**

| Task | Files | Change | Policies | Over-gate risk | Verify |
| --- | --- | --- | --- | --- | --- |
| W1.1 Host-only chunk silence | `core/global_script.ttslua` | `trEarlySilenceSoundscapeEmitters` + `trScheduleEarlySoundscapeSilenceDeferred` gate on `U.isHostClient()`; chunk `SS.bootstrapSilenceStrayEmitterLoops` host-only. | P2, P7 | Join client may hear brief stray audio if host late — acceptable vs duplicate silence | TOR-144 phase A4: no audio/light sweep on join |
| W1.2 Join-client log probe | same | One-time console: `Join client: skipped soundscape emitter silence (host-only).` | P9 | None | Join console message |

**Solo regression:** Save & Play host load; confirm emitters silent; no console errors.

---

## Wave 2 — Object script world I/O (MPA-003, MPA-004) — **P1** — **Done 2026-06-25**

| Task | Files | Change | Policies | Over-gate risk | Verify |
| --- | --- | --- | --- | --- | --- |
| W2.1 Tarot Global route | `ui/ui_tarot_button.ttslua`, `core/global_script.ttslua` | New `GlobalApplyTarotState(params)` with host guard; move `applyTarotState` logic or call into Global/module; object `click_tarotToggle` → `Global.call` only. | P2, P5, P8 | Pink player toggle must still work — host executes once, engine replicates | TOR-144: tarot toggle once, no jitter |
| W2.2 Dice bag onLoad | `objects/dice_bag.ttslua` | At start of `onLoad`, `Global.call("GlobalRequireHostForWorldMutation", { context = "dice_bag.onLoad" })` before destroys; or skip destroy on non-host. | P2 | None if join client never owned stale spawns | Join load: no destroy spam |
| W2.3 Lua local order | both files | Forward-declare new Global helper if needed. | — | nil-call if order wrong | Save & Play click tarot |

---

## Wave 3 — ST HUD gates (MPA-005, MPA-006, MPA-007) — **P1–P2** — **Done 2026-06-25**

| Task | Files | Change | Policies | Over-gate risk | Verify |
| --- | --- | --- | --- | --- | --- |
| W3.1 PCs panel | `core/global_script.ttslua` `HUD_pcPanel` | Add `requireStorytellerHostForMutation(player, "HUD_pcPanel")` before `PCST.onHudClick`. | P3, P2 | None — ST panel | ST Apply hunger on host |
| W3.2 Debug lights | `core/light_debug_focus.ttslua`, `HUD_debugLight*` | Host gate at module entry or HUD wrapper. | P2 | Debug only | Join client cannot move spotlights |
| W3.3 Dead Global API | `core/global_script.ttslua` `GlobalReportBagDiceCount` | Remove or host-guard + document; update Event Listener Policy. | P9, P2 | N/A if dead | grep clean |

---

## Wave 4 — Defensive hardening (MPA-008, MPA-010) — **P2** — **Done 2026-06-25**

| Task | Files | Change | Policies | Over-gate risk | Verify |
| --- | --- | --- | --- | --- | --- |
| W4.1 Drawer coroutine | `GlobalWaitDrawerThenReleaseBagDice` | First line: `requireHostForWorldMutation("GlobalWaitDrawerThenReleaseBagDice")`. | P2 | None | Phase C roll |
| W4.2 Csheet page snap | `ui/ui_csheet_core.ttslua` | After TOR-144: if fan-out confirmed, host-guard page Y snap. | P2 | Page flip on join client | Manual csheet page test |
| W4.3 `GlobalPostRollModifyPool` | optional | Top-level host guard for clarity. | P2 | ST post-roll only | ST dashboard die add/remove |

---

## Wave 5 — Documentation & inventory (MPA-011) — **P3** — **Done 2026-06-25**

| Task | Files | Change |
| --- | --- | --- |
| W5.1 Event Listener Policy | `Event Listener Policy.md` | Full `HUD_*` tier table or generated appendix; fix ReportBagDiceCount; add “TOR-144 verified” column |
| W5.2 Bootstrap Authority | `Bootstrap Authority.md` | Note chunk-load host-only expectation after W1 |
| W5.3 Preparing For Multiplayer | §1.3 | Add tarot + chunk-load to high-risk table after fixes |

---

## Wave 6 — Human verification (TOR-144)

Run [Preparing For Multiplayer §2](Preparing%20For%20Multiplayer.md) phases A–E with friend + [Multiplayer-E2E](../E2E%20Playbooks/Multiplayer-E2E.md) matrix.

| Wave | Playbook phases |
| --- | --- |
| W1 | A (join bootstrap, audio) |
| W2 | B + tarot if in scene |
| W3 | D (HUD / ST panel) |
| W4 | C (rolls) |
| W6 | All |

---

## Out of scope

| Item | Reason |
| --- | --- |
| Live `gameState` broadcast bridge | P10 — product decision; not host-guard removal |
| Performance Audit rank 0–7 latency | Separate doc; do not weaken P4 guards |
| `lib/e2e_gameboard.ttslua` Sync calls | Dev harness only |

---

## Linear sync (recommended)

- Comment on **TOR-144 (multiplayer E2E)** with link to this audit.
- Comment on **TOR-221 (non-Host onLoad guard)** — W1 closes chunk-load gap.
- Create issue(s) if splitting work: e.g. “Multiplayer chunk bootstrap host-only (MPA-001/002)”, “Tarot + dice bag object script host authority (MPA-003/004)”.
- Do **not** mark host-authority **Done** until Wave 6 passes.

---

## Appendix: Global mutator guard checklist (2026-06-25)

| Function | Host guard | Tier |
| --- | --- | --- |
| `GlobalToggleSignalFireState` | Yes | C |
| `GlobalGameboardApply/Clear/ClearClick/ToggleControlBoardSnaps/StageLerpOrchestrator/TokenDroppedOnDiceBag` | Yes | C |
| `GlobalGameboardInstallPaletteSnaps` | `isHostClient` | C |
| `GlobalGameboardSyncSnapsToggleLabel` | No (label) | A |
| All dice `GlobalSpawn*` / `GlobalDestroy*` / `GlobalRelease*` / bag hooks | Yes | C |
| `GlobalInitiateRoll`, `GlobalDiceBagClick/RightClick/StorytellerDiceBagClick` | Yes | B+C |
| `GlobalGet*`, `GlobalResolve*`, `GlobalCollect*`, `GlobalRegister*` | No | A |
| `GlobalRollSeatCamera`, `GlobalSetSheetCamera` | No (local camera) | A |
| `GlobalPostRollModifyPool` | Yes | B+C |
| `GlobalApplyTarotState` | Yes | C |

-- Committed stub (safe for Save & Play before manifest generation).
-- Overwritten by: npm run custom-ui-assets:manifest-sites (or manifest-sites:batch)
-- Required by DEBUG.loadCustomUiUploadManifest()
local M = {
  assets = {}
}

return M

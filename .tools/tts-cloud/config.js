"use strict";

/**
 * Edit CLOUD_ROOT when your TTS Cloud Manager top-level folder name changes.
 * Subfolders are passed on the CLI (e.g. Sites → "Vampire the Masquerade 5E/Sites").
 */
module.exports = {
  /** Tabletop Simulator Steam App ID */
  TTS_APP_ID: 286160,

  /** Top-level Cloud Manager folder (no trailing slash). */
  CLOUD_ROOT: "Vampire the Masquerade 5E",
};

<!-- Main HUD - VTM5E Module -->
<!-- Agent guidance: .dev/TTS_BUNDLING_SETUP.md (bundling + XML templates); .dev/HUD_FUNCTIONS.md; .dev/HUDs & Overlays/Player HUD Overview.md; .dev/tts-api/UI API/ (XML elements). -->
<!-- Root bundle file; no layout applied here. Include order matters for TTS defaults. -->
<!-- Rolandostar: <Include src> paths are relative to the workspace folder root — prefix with ui/ (not the ui/ file’s directory). -->

<!-- 1. Tag-based defaults (include first) -->
<Include src="defaults_tags.xml" />

<!-- 2. General shared classes -->
<Include src="defaults_classes.xml" />

<!-- 4. Player HUD (per-color panels) -->
<Panel id="HUD_PANEL_PLAYER" class="hud_root_panel">

  <!-- Defaults -->
  <Include src="player/panel_overlays_defaults.xml" />
  <Include src="player/panel_right_sidebar_defaults.xml" />
  <Include src="player/panel_map_core_defaults.xml" />

  <!-- 4a. Player HUD Overlays -->
  <Include src="player/panel_overlays.xml" />
  <!-- Game State Overlay -->
  <Include src="shared/game_state_overlay.xml" />

  <!-- 4a. Player HUD Right Sidebar -->

  <!-- Map panels (generated per player color) -->
  <Include src="player/panel_map_core.xml" />

  <!-- Right sidebar buttons (generated per player color) -->
  <Include src="player/panel_right_sidebar_buttons.xml" />


  <!-- Reference layer (generated per player color) -->
  <Include src="player/panel_right_sidebar_referenceLayer.xml" />

  <!-- Camera Controls Overlay (generated per player color) -->
  <Include src="player/panel_overlay_camera.xml" />

  <!-- Location Overlay (generated per player color) -->
  <Include src="player/panel_overlay_location.xml" />

  <!-- Player HUD Blindfold Overlay -->
  <Include src="player/panel_overlay_blindfold.xml" />

  <!-- Roll control panels (generated per player color) -->
  <Include src="player/panel_roll_controls.xml" />

</Panel>


<!-- 3. Storyteller HUD (Black): admin panel + sidebar + camera defaults -->
<Panel id="HUD_PANEL_BLACK" class="hud_root_panel" visibility="Black|Host">
  <!-- 3a. Storyteller HUD defaults -->
  <Include src="storyteller/hud_storyteller_defaults.xml" />
  <Include src="storyteller/panel_pcs_defaults.xml" />

  <!-- 3b. Storyteller HUD -->
  <Include src="storyteller/hud_storyteller.xml" />
</Panel>


<!-- 5. Dice System Panels (roll panels + ST dashboard + result broadcast) -->
<!-- Included outside the player/ST panels so the broadcast panel is visible to all. -->
<Include src="shared/roll_panels.xml" />
<Include src="shared/roll_options_modal.xml" />
<!-- Storyteller Scenes: district/site pickers from `node .dev/scripts/generate_scenes_location_modals_xml.js` -->
<Include src="storyteller/panel_scenes_location_modals.xml" />

# Global Lua (TTS)

The real Tabletop Simulator **Global** script lives in **`global_script.ttslua`**.

The file `.tts/objects/Global.lua` is a **stub** only (`require("global.global_script")`). TTS may overwrite that stub when you reload; restore the stub from git if needed, or re-add the single `require` line.

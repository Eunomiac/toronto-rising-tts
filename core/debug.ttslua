---@diagnostic disable: lowercase-global
--[[
    Debug module (core/debug.ttslua)

    Development helpers: inspection, dice roll shortcuts, soundscape setters, file logging,
    coordinate utilities, and light-feasibility probes. Exposed globally for TTS console
    and External Editor (`lua rollTest("Brown", 3)`, `lua debugHelp()`, etc.).

    Manual E2E verification: `.dev/E2E Playbooks/` (see `.dev/TESTING.md`).

    Agent guidance: .dev/DEBUG_FILE_LOGGING.md; .dev/TESTING.md; .dev/User Guides/TTS-3D-Positioning-Coordinate-Utilities.md
]]

DEBUG = {}
U = require("lib.util")
S = require("core.state")
M = require("core.main")
Scenes = require("core.scenes")
Soundscape = require("core.soundscape")
C = require("lib.constants")
G = require("lib.guids")
L = require("core.lighting")
O = require("core.objects")
local Sync = require("core.sync")
local SRI = require("lib.seat-role-identity")
LightTest = require("core.light_test")
-- Bundler tracing: `lib.workspace_ndjson_log` is otherwise only required from inside DEBUG.* at runtime
-- and can be omitted from Save & Play bundles → a late require there fails in-game. Keep one top-level require.
require("lib.workspace_ndjson_log")

--- Keep debug-side hunger writes aligned with runtime sync behavior.
--- @param color string
--- @param value number
local function setHungerAndSync(color, value)
    S.setPlayerVal(color, "hunger", value)
    Sync.player(color)
end

--[[
    File Logging Configuration
    External Editor bridge (repo tts-bridge on port 39998): `sendExternalMessage` with type "write"
    and `name` is written under **workspaceFolder/.dev/.debug/<name>** when MCP or `npm run tts-bridge:listen`
    is active. Same for logToFile and writeWorkspaceFile. See .dev/DEBUG_FILE_LOGGING.md.
]]
local LOG_DIR = "debug_logs"
local LOG_EXTENSION = "txt"

--- Prefix printed in DEBUG messages (relative to workspace folder); files appear here when the bridge handles writes.
local WORKSPACE_DEBUG_WRITE_ROOT = ".dev/.debug"

-- Cache for log file contents (to support append mode)
-- Key: filename (without extension), Value: accumulated content string
local logCache = {}

--- Printed at most once when `sendExternalMessage` is missing (NDJSON / workspace writes).
local warnedNdjsonSendExternalMissing = false
local lastCapturedCameraEntry = nil

--[[
    Helper Functions
]]

--- Gets current timestamp as a formatted string
-- @return string Timestamp in format "YYYY-MM-DD HH:MM:SS"
local function getTimestamp()
    local time = os.time()
    -- Lua 5.1 doesn't have os.date with format strings, so we use a workaround
    -- For TTS, we'll use a simpler format
    return tostring(time)
end

--- Writes content to a file in the workspace using TTS Tools extension
-- Supports append mode by maintaining an in-memory cache of file contents
-- @param filename string Name of the file (without extension)
-- @param content string Content to write
-- @param format string Optional format: "auto" (for JSON) or "none" (default: "none")
-- @param append boolean If true, appends to existing cached content (default: true for logs, false for dumps)
local function writeToFile(filename, content, format, append)
    format = format or "none"
    append = append ~= false  -- Default to true (append mode) unless explicitly set to false

    -- Use sendExternalMessage to write to file.
    -- Don't set `object`; with only `name`, the bridge writes to .dev/.debug/<name>.
    local fullName = LOG_DIR .. "/" .. filename .. "." .. LOG_EXTENSION

    -- For append mode, maintain cache and accumulate content
    if append then
        if not logCache[filename] then
            logCache[filename] = ""
        end
        logCache[filename] = logCache[filename] .. content
        content = logCache[filename]  -- Write accumulated content
    else
        -- Overwrite mode: clear cache and write new content
        logCache[filename] = content
    end

    -- Check if sendExternalMessage exists (TTS Tools extension)
    ---@diagnostic disable-next-line: undefined-global
    if sendExternalMessage then
        ---@diagnostic disable-next-line: undefined-global
        sendExternalMessage({
            type = "write",
            content = content,
            name = fullName,
            format = format
        })
        print("DEBUG: Log written to " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. fullName)
    else
        -- Fallback: just print if extension not available
        print("DEBUG (file logging not available): " .. filename)
        print(content)
    end
end

--- Compare config-only hand frames vs legacy generator (see `R.compareHandFrames`).
--- Uses global `R` (Execute Code cannot require modules).
--- @param tableKey string|nil defaults to active table from gameState (`Table A`, not `A`)
--- @return table summary `{ ok, failCount, maxPosErr, maxRotErr, ... }`
function DEBUG.compareHandFrames(tableKey)
  if type(R) ~= "table" or type(R.compareHandFrames) ~= "function" then
    error("DEBUG.compareHandFrames: global R.compareHandFrames missing — re-bundle Global and Save & Play")
  end
  local key = tableKey
  if U.Type(key) ~= "string" or key == "" then
    key = S.getStateVal("seatLayout", "currentTableKey")
  end
  if U.Type(key) ~= "string" or key == "" then
    error("DEBUG.compareHandFrames: pass a table key (e.g. 'Table A') or set gameState.seatLayout.currentTableKey")
  end
  return R.compareHandFrames(key)
end

--- PC hand-frame deltas: simplified vs legacy generator (see `R.compareLayoutPaths`).
--- @param tableKey string|nil defaults to active table from gameState
--- @return table summary
function DEBUG.compareLayoutPaths(tableKey)
  if type(R) ~= "table" or type(R.compareLayoutPaths) ~= "function" then
    error("DEBUG.compareLayoutPaths: global R.compareLayoutPaths missing — re-bundle Global and Save & Play")
  end
  local key = tableKey
  if U.Type(key) ~= "string" or key == "" then
    key = S.getStateVal("seatLayout", "currentTableKey")
  end
  if U.Type(key) ~= "string" or key == "" then
    error("DEBUG.compareLayoutPaths: pass a table key (e.g. 'Table A') or set gameState.seatLayout.currentTableKey")
  end
  return R.compareLayoutPaths(key)
end

--- Manual reference-segment rig refresh (see `R.refreshSeatRigsFromReferenceSegment`). Uses global `R`.
--- @param opts table|nil `{ tableKey?, seatKeys?, dryRun?, skipHandZones? }`
--- @return table summary
function DEBUG.refreshSeatRigsFromReference(opts)
  if type(R) ~= "table" or type(R.refreshSeatRigsFromReferenceSegment) ~= "function" then
    error("DEBUG.refreshSeatRigsFromReference: global R.refreshSeatRigsFromReferenceSegment missing — re-bundle Global and Save & Play")
  end
  return R.refreshSeatRigsFromReferenceSegment(opts)
end

--- Layout sync (production simplified path only).
--- @param tableKey string|nil defaults to `gameState.seatLayout.currentTableKey`
--- @param opts table|nil `{ force? }` merged into resolve options
--- @return table|nil computed
function DEBUG.syncTableSimplified(tableKey, opts)
  if type(R) ~= "table" or type(R.resolveSeatObjectsFromTable) ~= "function" then
    error("DEBUG.syncTableSimplified: global R.resolveSeatObjectsFromTable missing — re-bundle Global and Save & Play")
  end
  local key = tableKey
  if U.Type(key) ~= "string" or key == "" then
    key = S.getStateVal("seatLayout", "currentTableKey")
  end
  if U.Type(key) ~= "string" or key == "" then
    error("DEBUG.syncTableSimplified: pass a table key (e.g. 'Table A') or set gameState.seatLayout.currentTableKey")
  end
  local resolveOpts = U.Type(opts) == "table" and U.clone(opts, true) or {}
  resolveOpts.force = resolveOpts.force ~= false
  return R.resolveSeatObjectsFromTable(key, nil, resolveOpts)
end

--- Writes content via sendExternalMessage type "write". File appears as **workspace/.dev/.debug/<relativePath>** when the bridge listens on 39998.
--- Does not use log append cache; each call overwrites that file path.
--- @param relativePath string Path suffix under .dev/.debug, e.g. "debug_logs/npcs_panel.txt"
--- @param content string
--- @param format string|nil "none" or "auto" (default "none")
--- @param silent boolean|nil If true, do not print confirmation (for high-frequency writes)
--- @return boolean
function DEBUG.writeWorkspaceFile(relativePath, content, format, silent)
    format = format or "none"
    if U.Type(relativePath) ~= "string" or relativePath == "" then
        print("DEBUG.writeWorkspaceFile: invalid relativePath")
        return false
    end
    if U.Type(content) ~= "string" then
        content = tostring(content)
    end
    ---@diagnostic disable-next-line: undefined-global
    if sendExternalMessage then
        ---@diagnostic disable-next-line: undefined-global
        sendExternalMessage({
            type = "write",
            content = content,
            name = relativePath,
            format = format
        })
        if not silent then
            print("DEBUG: Wrote " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. relativePath)
            print(
                "DEBUG: tts-bridge stores custom writes under .dev/.debug/ (first workspace folder if multi-root). "
                    .. "Enable custom messages (ttsEditor.enableMessages) if writes are ignored."
            )
        end
        return true
    end
    print("DEBUG.writeWorkspaceFile: sendExternalMessage not available (TTS Tools extension?)")
    return false
end

--- @param n number|nil
--- @return string
local function formatCameraNumber(n)
    if type(n) ~= "number" then
        return "0"
    end
    return string.format("%.4f", n)
end

--- Capture the player's current view and export a paste-ready Lua snippet.
--- The output can be pasted into C.UniversalCameraAngles or C.ReferenceCameraAngles.
--- @param player Player
--- @return boolean ok
--- @return string resultMessage
function DEBUG.captureCameraViewPreset(player)
    if player == nil or not U.isPlayer(player) then
        return false, "DEBUG.captureCameraViewPreset: invalid player"
    end

    local function callPlayerMethod(name, ...)
        local fn = player[name]
        if type(fn) ~= "function" then
            return nil
        end
        return fn(player, ...)
    end

    local cameraMode = callPlayerMethod("getCameraMode") or "FirstPerson"
    local pointerPos = callPlayerMethod("getPointerPosition")
    local pointerRot = callPlayerMethod("getPointerRotation")
    local handTf = callPlayerMethod("getHandTransform", 1) or callPlayerMethod("getHandTransform")

    local pos = nil
    if pointerPos ~= nil then
        pos = Vector(pointerPos)
    elseif type(handTf) == "table" and handTf.position ~= nil then
        pos = Vector(handTf.position)
    else
        pos = Vector({0, 0, 0})
    end

    local pitch = 0
    local yaw = 0
    local distance = 0
    if pointerRot ~= nil then
        local rot = Vector(pointerRot)
        pitch = rot.x
        yaw = rot.y
    elseif type(handTf) == "table" and handTf.rotation ~= nil then
        local rot = Vector(handTf.rotation)
        pitch = rot.x
        yaw = rot.y
    end

    if type(handTf) == "table" and handTf.position ~= nil and pointerPos ~= nil then
        local origin = Vector(handTf.position)
        local target = Vector(pointerPos)
        local dx = target.x - origin.x
        local dy = target.y - origin.y
        local dz = target.z - origin.z
        local horizontal = math.sqrt(dx * dx + dz * dz)

        -- If API does not expose pointer rotation, derive orientation from origin -> pointer target.
        if pointerRot == nil then
            yaw = math.deg(math.atan2(dx, dz))
            if horizontal > 0.001 then
                pitch = -math.deg(math.atan2(dy, horizontal))
            end
        end
        distance = math.sqrt(dx * dx + dy * dy + dz * dz)
    end

    lastCapturedCameraEntry = {
        cameraMode = tostring(cameraMode),
        position = Vector(pos),
        pitch = pitch,
        yaw = yaw,
        distance = distance,
    }

    local ts = os.time()
    local colorKey = string.upper(player.color or "UNKNOWN")
    local relativePath = "debug_logs/camera_capture_" .. colorKey .. "_" .. tostring(ts) .. ".lua"
    local body = table.concat({
        "-- Camera capture snapshot (generated by HUD_debugCaptureCameraPreset)",
        "-- Paste one of the entries below into C.UniversalCameraAngles or C.ReferenceCameraAngles.",
        "-- Suggested mode key: <replace_me>",
        "",
        "local CAPTURED_CAMERA_ENTRY = {",
        "  cameraMode = " .. string.format("%q", tostring(cameraMode)) .. ",",
        "  position = Vector({" .. formatCameraNumber(pos.x) .. ", " .. formatCameraNumber(pos.y) .. ", " .. formatCameraNumber(pos.z) .. "}),",
        "  pitch = " .. formatCameraNumber(pitch) .. ",",
        "  yaw = " .. formatCameraNumber(yaw) .. ",",
        "  distance = " .. formatCameraNumber(distance) .. ",",
        "}",
        "",
        "-- Example paste targets:",
        "C.UniversalCameraAngles[\"<replace_me>\"] = CAPTURED_CAMERA_ENTRY",
        "C.ReferenceCameraAngles[\"<replace_me>\"] = CAPTURED_CAMERA_ENTRY",
        "",
    }, "\n")

    local okWrite = DEBUG.writeWorkspaceFile(relativePath, body, "none", false)
    if not okWrite then
        return false, "camera capture failed to write file"
    end
    return true, relativePath
end

--- Apply the most recently captured camera entry to a player.
--- @param player Player
--- @return boolean ok
--- @return string resultMessage
function DEBUG.applyLastCapturedCameraPreset(player)
    if player == nil or not U.isPlayer(player) then
        return false, "DEBUG.applyLastCapturedCameraPreset: invalid player"
    end
    if type(lastCapturedCameraEntry) ~= "table" then
        return false, "No captured camera snapshot is available yet."
    end

    local entry = {
        cameraMode = lastCapturedCameraEntry.cameraMode or "FirstPerson",
        position = Vector(lastCapturedCameraEntry.position or {0, 0, 0}),
        pitch = tonumber(lastCapturedCameraEntry.pitch) or 0,
        yaw = tonumber(lastCapturedCameraEntry.yaw) or 0,
        distance = tonumber(lastCapturedCameraEntry.distance) or 0,
    }

    player.setCameraMode(entry.cameraMode)
    player.lookAt({
        position = entry.position,
        pitch = entry.pitch,
        yaw = entry.yaw,
        distance = entry.distance,
    })
    U.delay(function()
        player.setCameraMode(entry.cameraMode)
    end, 0.5)

    return true, "Applied most recent captured camera preset."
end

--- Clear debug NDJSON buffer and truncate workspace log (same path as NPC lighting instrumentation).
--- TTS console: lua DEBUG.beginWorkspaceDebugSession()
function DEBUG.beginWorkspaceDebugSession()
    local W = require("lib.workspace_ndjson_log")
    if W == nil or U.Type(W.beginSession) ~= "function" then
        print("DEBUG.beginWorkspaceDebugSession: lib.workspace_ndjson_log invalid")
        return
    end
    W.beginSession("debug_workspace_ndjson.log")
    W.append("debug_workspace_ndjson.log", {
        hypothesisId = "bootstrap",
        location = "DEBUG.beginWorkspaceDebugSession",
        message = "NDJSON session opened",
        data = { relativePath = "debug_workspace_ndjson.log" },
    })
    print("DEBUG: began workspace debug session -> " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/debug_workspace_ndjson.log")
end

--- Truncate + clear in-memory buffer for a named NDJSON log under `.dev/.debug/<relativePath>`.
--- Same transport as `beginWorkspaceDebugSession` (`lib.workspace_ndjson_log` + `sendExternalMessage`).
--- @param relativePath string e.g. `debug_session.log`
function DEBUG.workspaceNdjsonBegin(relativePath)
    if U.Type(relativePath) ~= "string" or relativePath == "" then
        print("DEBUG.workspaceNdjsonBegin: invalid relativePath")
        return
    end
    ---@diagnostic disable-next-line: undefined-global
    if sendExternalMessage == nil then
        if not warnedNdjsonSendExternalMissing then
            warnedNdjsonSendExternalMissing = true
            print(
                "DEBUG.workspaceNdjsonBegin: sendExternalMessage is nil — workspace NDJSON will NOT be written."
            )
            print(
                "  Fix: use TTS Editor / extension connected to this workspace, enable custom messages (ttsEditor.enableMessages), then Save & Play."
            )
            print(
                "  Expected file when working: <workspaceFolder>/" .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. relativePath
            )
        end
        return
    end
    local W = require("lib.workspace_ndjson_log")
    if W == nil or U.Type(W.beginSession) ~= "function" then
        print("DEBUG.workspaceNdjsonBegin: lib.workspace_ndjson_log invalid")
        return
    end
    W.beginSession(relativePath)
    -- First disk write is always a non-empty append (beginSession does not touch the file).
    W.append(relativePath, {
        hypothesisId = "bootstrap",
        location = "DEBUG.workspaceNdjsonBegin",
        message = "NDJSON session opened",
        data = { relativePath = relativePath },
    })
    print("DEBUG: NDJSON session + bootstrap line -> " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. relativePath)
end

--- Append one NDJSON record (human header + JSON line). File appears under `.dev/.debug/<relativePath>`.
--- @param relativePath string
--- @param payload table fields e.g. sessionId, hypothesisId, location, message, data
function DEBUG.workspaceNdjsonAppend(relativePath, payload)
    if U.Type(relativePath) ~= "string" or relativePath == "" then
        return
    end
    if U.Type(payload) ~= "table" then
        payload = { message = tostring(payload) }
    end
    ---@diagnostic disable-next-line: undefined-global
    if sendExternalMessage == nil then
        if not warnedNdjsonSendExternalMissing then
            warnedNdjsonSendExternalMissing = true
            print(
                "DEBUG.workspaceNdjsonAppend: sendExternalMessage is nil — NDJSON append skipped. See DEBUG.workspaceNdjsonBegin message for fixes."
            )
        end
        return
    end
    local W = require("lib.workspace_ndjson_log")
    if W == nil or U.Type(W.append) ~= "function" then
        print("DEBUG.workspaceNdjsonAppend: lib.workspace_ndjson_log invalid")
        return
    end
    W.append(relativePath, payload)
end

--- Prints a formatted test header
-- @param testName string Name of the test
local function printTestHeader(testName)
    print("\n" .. string.rep("=", 60))
    print("TEST: " .. testName)
    print(string.rep("=", 60))
end

--- Prints a formatted test result and logs to file
-- @param testName string Name of the test
-- @param passed boolean Whether the test passed
-- @param details string Optional details about the test
local function printTestResult(testName, passed, details)
    local status = passed and "✓ PASS" or "✗ FAIL"
    local color = passed and {0, 1, 0} or {1, 0, 0}
    print(status .. ": " .. testName)
    if details then
        print("  Details: " .. details)
    end

    -- Also log to file
    DEBUG.logTestToFile(testName, passed, details, "test_results")
end



--[[
    Light Creation Feasibility Tests

    These functions test the feasibility of creating custom point-source lights
    programmatically instead of using third-party configurable lights.

    See .dev/LIGHT_CREATION_FEASIBILITY.md for detailed investigation.
]]

--- Test if "Light" is a spawnable built-in type
-- @usage testSpawnLightType()
function DEBUG.testSpawnLightType()
    return LightTest.testSpawnLightType()
end

--- Test cloning an existing light object
-- @param templateGUID string GUID of light object to clone
-- @usage testCloneLight("abc123")
function DEBUG.testCloneLight(templateGUID)
    return LightTest.testCloneLight(templateGUID)
end

--- Inspect Light component properties
-- @param lightGUID string GUID of light object to inspect
-- @usage testInspectLight("abc123")
function DEBUG.testInspectLight(lightGUID)
    return LightTest.testInspectLight(lightGUID)
end

--- Test spawning a light from a template
-- @param templateGUID string GUID of light template
-- @param position Vector Position to spawn light (optional)
-- @param rotation Vector Rotation of light (optional)
-- @param config table Light configuration {enabled, range, intensity, color, angle} (optional)
-- @usage testSpawnLight("abc123", Vector(0,5,0), Vector(0,0,0), {range=50, intensity=15})
function DEBUG.testSpawnLight(templateGUID, position, rotation, config)
    return LightTest.testSpawnLight(templateGUID, position, rotation, config)
end

--- Compare current light structure with potential new structure
-- @param currentLightGUID string GUID of current configurable light
-- @usage testCompareStructures("abc123")
function DEBUG.testCompareStructures(currentLightGUID)
    return LightTest.testCompareStructures(currentLightGUID)
end

--- Find all objects that might be lights
-- @usage findPotentialLights()
function DEBUG.findPotentialLights()
    return LightTest.findPotentialLights()
end



--[[
    State Inspection Functions
]]

--- Display current game state (readable format)
function DEBUG.showState()
    printTestHeader("Current Game State")
    local state = S.getGameState()
    print(JSON.encode_pretty(state))
end

--- Print orchestrator reconcile skip-cache snapshot (fingerprints, layout, init-light retries).
function DEBUG.dumpSyncCacheState()
    printTestHeader("Sync reconcile caches")
    local Sync = require("core.sync")
    local snap = type(Sync.debugReconcileCacheSnapshot) == "function" and Sync.debugReconcileCacheSnapshot() or {}
    if type(snap) ~= "table" then
        print("(no snapshot)")
        return
    end
    for k, v in pairs(snap) do
        print(tostring(k) .. ": " .. tostring(v))
    end
end

--- Display live session scene + library linkage (current authoring model).
function DEBUG.showScene()
    printTestHeader("Session Scene + Library")
    print("currentScene (lighting preset key): " .. tostring(S.getStateVal("currentScene")))
    print("sessionScene.lightingPresetKey: " .. tostring(S.getStateVal("sessionScene", "lightingPresetKey")))
    print("sceneLibrary.activeKey: " .. tostring(S.getStateVal("sceneLibrary", "activeKey")))
    print("sceneLibrary.lastAppliedKey: " .. tostring(S.getStateVal("sceneLibrary", "lastAppliedKey")))
    print("sessionScene.siteKey: " .. tostring(S.getStateVal("sessionScene", "siteKey")))
    print("sessionScene.districtKey: " .. tostring(S.getStateVal("sessionScene", "districtKey")))
    local npcWorld = S.getStateVal("sessionScene", "npcWorld")
    if type(npcWorld) == "table" and type(npcWorld.byArea) == "table" then
        local Convert = require("lib.npc_placements_convert")
        if Convert.byAreaHasEntries(npcWorld.byArea) then
            print("sessionScene.npcWorld.byArea: legacy entries present — run S.validateState or npm run npc-placements:migrate-byarea")
        end
    end
    if type(npcWorld) == "table" and type(npcWorld.placements) == "table" then
        local n = 0
        for _ in pairs(npcWorld.placements) do
            n = n + 1
        end
        print("sessionScene.npcWorld.placements: " .. tostring(n))
    end
end

--- Scan `npc_light` for duplicate pooled spotlights per roster key (same as load audit).
--- @param opts table|nil forwarded to `NPCS.auditDuplicatePooledSpotlights` (e.g. `{ alert = true, destroyDuplicates = true }`)
--- @return table audit result
function DEBUG.auditDuplicatePooledSpotlights(opts)
    local NPCS = require("core.npcs")
    opts = type(opts) == "table" and opts or {}
    if opts.phase == nil then
        opts.phase = "debug-audit"
    end
    return NPCS.auditDuplicatePooledSpotlights(opts)
end

--- Dump `sessionScene.npcWorld.placements` and live instance stage poses.
function DEBUG.dumpNpcPlacements()
    printTestHeader("NPC gameboard placements")
    local placements = S.getStateVal("sessionScene", "npcWorld", "placements")
    if type(placements) ~= "table" then
        print("placements: (none)")
        return
    end
    local keys = {}
    for k, _ in pairs(placements) do
        keys[#keys + 1] = k
    end
    table.sort(keys)
    for _, ck in ipairs(keys) do
        local row = placements[ck]
        if type(row) == "table" then
            print(string.format(
                "  %s u=%.3f v=%.3f yaw=%.0f mode=%s",
                ck,
                tonumber(row.u) or 0,
                tonumber(row.v) or 0,
                tonumber(row.yaw) or 0,
                tostring(row.npcLightMode or "")
            ))
        end
    end
    if NPCS ~= nil and type(NPCS.collectSessionStagePlacements) == "function" then
        print("collectSessionStagePlacements rows: " .. tostring(#NPCS.collectSessionStagePlacements()))
    end
end

--[[ Gameboard & Stage E2E (TOR-141 / TOR-169) — see .dev/E2E Playbooks/Gameboard-E2E.md ]]
local E2E_GB = require("lib.e2e_gameboard")

--- Assert gameboard/state probes; prints `[gbConfirm] PASS` / `FAIL`.
--- @param labelOrExpected string|table test label, or `{ placements = … }` only
--- @param maybeExpected table|nil
--- @return boolean
function DEBUG.gbConfirm(labelOrExpected, maybeExpected)
    if type(labelOrExpected) == "table" and maybeExpected == nil then
        return E2E_GB.confirm("gbConfirm", labelOrExpected)
    end
    return E2E_GB.confirm(tostring(labelOrExpected), maybeExpected or {})
end

--- Prerequisite check (boards, tokens, fixture NPCs, scene library row).
--- @return boolean
function DEBUG.gbE2ePrereqCheck()
    return E2E_GB.runPrereqCheck()
end

--- Deterministic baseline before suites.
--- @return boolean
function DEBUG.gbE2eReset()
    return E2E_GB.reset()
end

--- Smoke macro (automated blocks + scene Apply human gate).
--- @return boolean
function DEBUG.gbE2eRunSmoke()
    return E2E_GB.runSmoke()
end

--- Full macro (deep suites + optional reload human gate).
--- @return boolean
function DEBUG.gbE2eRunFull()
    return E2E_GB.runFull()
end

--- Deferred expected-FAIL probes (TOR-172/173/175/174/D177b).
--- @return boolean
function DEBUG.gbE2eRunDeferred()
    return E2E_GB.runDeferred()
end

--- Resume after human gate (`_G.gbE2ePending`).
--- @return boolean
function DEBUG.gbE2eContinue()
    return E2E_GB.runContinue()
end

--- Verify each `pc_control_token` is pinned to its PC seat-row column with flip matching `seatSlots[color].isPresent` (TOR-152 / TOR-236).
--- @return boolean
function DEBUG.gbE2eVerifyPcTokens()
    return E2E_GB.verifyPcTokensFromSeatState()
end

--- Print last gameboard interaction profile (Apply/Clear/token drop).
--- @param label string|nil optional prefix for console line
--- @return table|nil
local function printGameboardInteractionProfile(label)
    local Gameboard = require("core.npc_gameboard")
    local profile = Gameboard.getLastInteractionProfile()
    if type(profile) ~= "table" then
        print("[DEBUG] gameboard profile: (none — run Apply, Clear, or drop a token first)")
        return nil
    end
    local prefix = type(label) == "string" and label ~= "" and (label .. " ") or ""
    print(
        "[DEBUG] "
            .. prefix
            .. "gameboard profile reason="
            .. tostring(profile.reason)
            .. " phaseA_ms="
            .. string.format("%.2f", tonumber(profile.phaseA_ms) or 0)
            .. " catalog_builds="
            .. tostring(profile.catalog_builds or 0)
            .. " tag_scans="
            .. tostring(profile.tag_scans or 0)
    )
    return profile
end

--- Reset gameboard interaction profile snapshot.
function DEBUG.resetGameboardProfile()
    local Gameboard = require("core.npc_gameboard")
    Gameboard.resetInteractionProfile()
    print("[DEBUG] gameboard profile reset")
end

--- Run Apply and print interaction profile (seat Apply hitch baseline).
--- @return boolean ok
function DEBUG.profileGameboardApply()
    local Gameboard = require("core.npc_gameboard")
    Gameboard.resetInteractionProfile()
    local ok = Gameboard.applyFromControlBoard()
    printGameboardInteractionProfile("Apply")
    return ok == true
end

--- Run Clear and print interaction profile.
--- @return boolean ok
function DEBUG.profileGameboardClear()
    local Gameboard = require("core.npc_gameboard")
    Gameboard.resetInteractionProfile()
    local ok = Gameboard.clearControlBoard()
    printGameboardInteractionProfile("Clear")
    return ok == true
end

--- Print last token-drop profile (drop a control token first, then call).
--- @return table|nil
function DEBUG.profileGameboardTokenDrop()
    return printGameboardInteractionProfile("TokenDrop")
end

--- Snap table from DEBUG.installNpcControlBoardSnaps options (flat fields or `{ config = … }`).
--- @param options table|nil
--- @return table|nil
local function resolveDebugControlBoardSnapConfig(options)
    options = type(options) == "table" and options or {}
    if type(options.config) == "table" then
        return options.config
    end
    if type(options.origin) == "table" or type(options.snapGroups) == "table" or options.rings ~= nil then
        return options
    end
    return nil
end

--- Force CONTROL_BOARD snap grid (normally runs from reconcile / Sync.npcs).
--- @param options table|nil Full `CONTROL_BOARD_SNAP` table, or `{ config?: table }` — overrides `DATA.CONTROL_BOARD_SNAP` for IDE iteration
--- @return boolean
function DEBUG.installNpcControlBoardSnaps(options)
    local Gameboard = require("core.npc_gameboard")
    return Gameboard.installPolarSnaps(nil, {
        force = true,
        config = resolveDebugControlBoardSnapConfig(options),
    }) == true
end

--- Install a new CONTROL_BOARD snap grid, remap tokens by ring + spread offset, sync stage figurines.
--- A. Logs each token's ring (old config catalog match).
--- B. `installPolarSnaps` with `options` / `options.config` as the new snap table.
--- C. Moves tokens + `sessionScene.npcWorld.placements` to best-matching snaps on the same ring; missing rings → palette; `Sync.npcs` unless `skipSync`.
--- @param options table|nil New `CONTROL_BOARD_SNAP` (or `{ config = … }`); optional `oldConfig` for matching override; by default old config comes from currently installed board snaps; `skipSync` to skip figurine reconcile
--- @return table|nil stats from Gameboard.remapControlBoardSnapsToConfig
function DEBUG.installAndRemapNpcControlBoardSnaps(options)
    options = type(options) == "table" and options or {}
    local Gameboard = require("core.npc_gameboard")
    local newConfig = resolveDebugControlBoardSnapConfig(options)
    if type(newConfig) ~= "table" then
        print("[DEBUG] installAndRemapNpcControlBoardSnaps: pass a CONTROL_BOARD_SNAP table or { config = … }")
        return nil
    end
    local oldConfig = type(options.oldConfig) == "table" and options.oldConfig or nil
    local hasInstalled = Gameboard.getInstalledControlBoardSnapConfig() ~= nil
    local sourceLabel = hasInstalled and "installed snaps" or "DATA fallback"
    if oldConfig ~= nil then
        sourceLabel = "options.oldConfig"
    end
    print("[DEBUG] installAndRemapNpcControlBoardSnaps: begin (" .. sourceLabel .. " → new config)")
    return Gameboard.remapControlBoardSnapsToConfig(oldConfig, newConfig, {
        skipSync = options.skipSync == true,
    })
end

--- Print control-board table-component marker decisions (table leaves, etc.) and optionally force a debug reconcile pass.
--- @param options table|nil `{ includeTagless?: boolean, forceReconcile?: boolean }`
--- @return table|nil
function DEBUG.previewControlBoardSnapCount(config)
    local Gameboard = require("core.npc_gameboard")
    local total, perRing = Gameboard.countControlBoardSnapPoints(config)
    print("[DEBUG] CONTROL_BOARD snap count: " .. tostring(total))
    for ringIndex, ringTotal in ipairs(perRing) do
        print("  ring " .. tostring(ringIndex) .. ": " .. tostring(ringTotal))
    end
end

--- Print Lua table or CSV of snap family roles → world X/Z (STAGE) for tuning or migration.
--- @param config table|nil Full CONTROL_BOARD_SNAP table or nil for DATA default
--- @param options table|nil `{ useInterpolatedRings?: boolean, format?: "lua"|"csv" }`
function DEBUG.exportControlBoardSnapFamilies(config, options)
    local Gameboard = require("core.npc_gameboard")
    options = type(options) == "table" and options or {}
    local byRole = Gameboard.exportSnapFamiliesByRole(config, options)
    local format = options.format == "csv" and "csv" or "lua"
    local text = format == "csv"
        and Gameboard.formatSnapFamiliesCsv(byRole)
        or Gameboard.formatSnapFamiliesLua(byRole)
    print("[DEBUG] CONTROL_BOARD snap families (world X/Z on STAGE, " .. format .. "):")
    print(text)
    return byRole
end

--- Write CONTROL_BOARD polar snap groups to CSV content via `writeToFile` (same path as `logStateToFile`).
--- Group/index ordering matches anchor-family coterie spread (`orderFamilySnapsForAnchorSpread`).
--- Requires TTS Tools extension / External Editor (`sendExternalMessage`); see `.dev/DEBUG_FILE_LOGGING.md`.
--- @param filename string|nil Basename without extension (default: `control_board_snap_groups` → `debug_logs/control_board_snap_groups.txt`)
--- @param config table|nil Full `CONTROL_BOARD_SNAP` table or nil for DATA default
--- @return table[] rows written
function DEBUG.exportControlBoardSnapGroupsToFile(filename, config)
    filename = (type(filename) == "string" and filename ~= "") and filename or "control_board_snap_groups"
    local Gameboard = require("core.npc_gameboard")
    local rows = Gameboard.buildControlBoardSnapGroupExportRows(config)
    local csv = Gameboard.formatControlBoardSnapGroupsCsv(rows)
    writeToFile(filename, csv, "none", false)
    print(
        "DEBUG: CONTROL_BOARD snap groups logged to "
            .. WORKSPACE_DEBUG_WRITE_ROOT
            .. "/"
            .. LOG_DIR
            .. "/"
            .. filename
            .. "."
            .. LOG_EXTENSION
            .. " ("
            .. tostring(#rows)
            .. " row(s), CSV body)"
    )
    return rows
end

--- Toggle all Anchor-tagged objects:
--- hidden(alpha=0) -> reveal(alpha=1) and y-190
--- visible(alpha=1) -> hide(alpha=0) and y+190
--- @return table
function DEBUG.toggleAllAnchorObjects()
    local anchors = getObjectsWithTag("Anchor") or {}
    local movedUp = 0
    local movedDown = 0
    local skipped = 0
    for _, obj in ipairs(anchors) do
        if obj ~= nil then
            local pos = obj.getPosition()
            local tint = obj.getColorTint() or { 1, 1, 1, 1 }
            local r = tint.r or tint[1] or 1
            local g = tint.g or tint[2] or 1
            local b = tint.b or tint[3] or 1
            local a = tint.a or tint[4] or 1
            local isHidden = a <= 0.001
            if isHidden then
                obj.setPosition({ x = pos.x, y = pos.y - 190, z = pos.z })
                obj.setColorTint({ r, g, b, 1 })
                movedDown = movedDown + 1
            else
                obj.setPosition({ x = pos.x, y = pos.y + 190, z = pos.z })
                obj.setColorTint({ r, g, b, 0 })
                movedUp = movedUp + 1
            end
        else
            skipped = skipped + 1
        end
    end
    local summary = {
        total = #anchors,
        movedUpHidden = movedUp,
        movedDownShown = movedDown,
        skipped = skipped,
    }
    print("[DEBUG.toggleAllAnchorObjects] " .. JSON.encode(summary))
    return summary
end

--- Toggle Spotlight-tagged object arrows using AssetBundle looping effects.
--- UI 1 <-> UI 2 maps to API index 0 <-> 1 (0-based).
--- @return table
function DEBUG.toggleAllSpotlightArrows()
    local spotlights = getObjectsWithTag("Spotlight") or {}
    local toggled = 0
    local skipped = 0
    for _, obj in ipairs(spotlights) do
        local ab = obj and obj.AssetBundle or nil
        if ab ~= nil and ab.getLoopingEffectIndex ~= nil and ab.playLoopingEffect ~= nil then
            local currentIdx0 = ab.getLoopingEffectIndex()
            if type(currentIdx0) == "number" then
                local currentUi = currentIdx0 + 1
                local targetUi = (currentUi == 2) and 1 or 2
                local targetIdx0 = targetUi - 1
                ab.playLoopingEffect(targetIdx0)
                toggled = toggled + 1
            else
                skipped = skipped + 1
            end
        else
            skipped = skipped + 1
        end
    end
    local summary = {
        tagged = #spotlights,
        toggled = toggled,
        skipped = skipped,
    }
    print("[DEBUG.toggleAllSpotlightArrows] " .. JSON.encode(summary))
    return summary
end

--- Set every Spotlight-tagged object to Looping Effect UI slot 2 (invisible arrow).
--- Same object set as `DEBUG.toggleAllSpotlightArrows` (`getObjectsWithTag("Spotlight")`).
--- UI slot 2 → API index 1 (0-based), matching the Toggle Spotlights debug control.
--- Console: `lua DEBUG.hideAllSpotlightArrows()`
--- @return table
function DEBUG.hideAllSpotlightArrows()
    local SPOTLIGHT_INVISIBLE_LOOPING_EFFECT_UI = 2
    local targetIdx0 = SPOTLIGHT_INVISIBLE_LOOPING_EFFECT_UI - 1
    local spotlights = getObjectsWithTag("Spotlight") or {}
    local updated = 0
    local skipped = 0
    for _, obj in ipairs(spotlights) do
        local ab = obj and obj.AssetBundle or nil
        if ab ~= nil and ab.getLoopingEffectIndex ~= nil and ab.playLoopingEffect ~= nil then
            local currentIdx0 = ab.getLoopingEffectIndex()
            if type(currentIdx0) == "number" and currentIdx0 == targetIdx0 then
                skipped = skipped + 1
            elseif type(currentIdx0) == "number" then
                ab.playLoopingEffect(targetIdx0)
                updated = updated + 1
            else
                skipped = skipped + 1
            end
        else
            skipped = skipped + 1
        end
    end
    local summary = {
        tagged = #spotlights,
        updated = updated,
        skipped = skipped,
        targetLoopingEffectUi = SPOTLIGHT_INVISIBLE_LOOPING_EFFECT_UI,
    }
    print("[DEBUG.hideAllSpotlightArrows] " .. JSON.encode(summary))
    return summary
end

--- Inspect soundscape AssetBundle emitters, looping/trigger effect names, and AudioSource counts.
-- @return table
function DEBUG.inspectSoundscapeAudio()
    printTestHeader("Soundscape Audio Emitters")
    local rows = Soundscape.inspectEmitters()
    print(JSON.encode_pretty(rows))
    if U.mcpEmitResult ~= nil then
        U.mcpEmitResult({ test = "inspectSoundscapeAudio", emitters = rows })
    end
    return rows
end

--- Quick test: set a Storyteller-controlled background playlist.
-- @param moodKey string One of: main, intrigue, combat
function DEBUG.soundscapeMusic(moodKey)
    local ok, message = Soundscape.setMusicMood(moodKey or "main")
    printTestResult("Soundscape music mood", ok, message)
    UpdateUIDisplays()
end

--- Quick test: set weather audio condition and optional indoor ducking.
-- @param weatherKey string e.g. "rainLight", "rainHeavy", "wind", "thunderstorm", "none"
-- @param isIndoors boolean|nil
function DEBUG.soundscapeWeather(weatherKey, isIndoors)
    if type(isIndoors) == "boolean" then
        Soundscape.setIndoors(isIndoors)
    end
    local ok, message = Soundscape.setWeatherCondition(weatherKey or "none")
    printTestResult("Soundscape weather", ok, message)
    UpdateUIDisplays()
end

--- Quick test: set the looping rain layer directly.
-- @param rainKey string e.g. "rainLight", "rainHeavy", "none"
function DEBUG.soundscapeRain(rainKey)
    local ok, message = Soundscape.setRainLayer(rainKey or "none")
    printTestResult("Soundscape rain", ok, message)
    UpdateUIDisplays()
end

--- Quick test: set the looping wind layer directly.
-- @param windKey string e.g. "windLow", "windMed", "windMax", "none"
function DEBUG.soundscapeWind(windKey)
    local ok, message = Soundscape.setWindLayer(windKey or "none")
    printTestResult("Soundscape wind", ok, message)
    UpdateUIDisplays()
end

--- Quick test: enable/disable scheduled thunder, or trigger one hit immediately.
-- @param mode string|boolean|nil Use true/"on" to schedule, false/"off" to cancel, "hit" to play once.
-- @param hitKey string|nil Optional thunder hit key when mode is "hit".
function DEBUG.soundscapeThunder(mode, hitKey)
    local ok, message
    if mode == "hit" then
        ok, message = Soundscape.triggerThunder(hitKey)
    else
        local enabled = mode == true or mode == "true" or mode == "on" or mode == "enable"
        ok, message = Soundscape.setThunderEnabled(enabled)
    end
    printTestResult("Soundscape thunder", ok, message)
    UpdateUIDisplays()
end

--- Quick test: play featured music on the dedicated featured lane.
-- @param featureKey string e.g. "TR_Full", "TR_Intro", "STB_HouseOfTheRisingSun"
function DEBUG.soundscapeFeatured(featureKey)
    local ok, message = Soundscape.playFeaturedMusic(featureKey or "TR_Intro")
    printTestResult("Soundscape featured music", ok, message)
    UpdateUIDisplays()
end

--- Quick test: stop featured music without clearing background context.
function DEBUG.soundscapeStopFeatured()
    local ok, message = Soundscape.stopFeaturedMusic()
    printTestResult("Soundscape stop featured", ok, message)
    UpdateUIDisplays()
end

--- Quick test: resume the saved background music context.
function DEBUG.soundscapeResumeBackground()
    local ok, message = Soundscape.resumeBackgroundMusic()
    printTestResult("Soundscape resume background", ok, message)
    UpdateUIDisplays()
end

--- Quick test: set site/location ambience.
-- @param locationKey string e.g. "airport", "industry", "sewers", "warehouse", "none"
function DEBUG.soundscapeLocation(locationKey)
    local ok, message = Soundscape.setLocationAudio(locationKey or "none")
    printTestResult("Soundscape location", ok, message)
    UpdateUIDisplays()
end

--- Quick test: stop every soundscape layer.
function DEBUG.soundscapeStopAll()
    local ok, message = Soundscape.stopAll()
    printTestResult("Soundscape stop all", ok, message)
    UpdateUIDisplays()
end

--[[
    Quick Test Functions (Single Commands)
]]

--- Quick test: Set player hunger
-- @param color string Player color
-- @param value number Hunger value (0-5)
function DEBUG.setHunger(color, value)
    if not color or not value then
        print("Usage: setHunger(color, value)")
        print("Example: setHunger('Red', 3)")
        return
    end

    local playerID = S.getPlayerID(color)
    if not playerID then
        print("ERROR: Could not find player ID for color: " .. color)
        return
    end

    setHungerAndSync(color, value)
    print("Set " .. color .. " player (ID: " .. playerID .. ") hunger to " .. tostring(value))

    UpdateUIDisplays()
end

--- Debug helper: Move an object to a world-angle position around table center
-- Positions an object at a given distance and world angle around the table center.
-- ============================================================================
-- DICE SYSTEM DEBUG HELPERS
-- Solo/dev-mode shortcuts for testing roll flow without a second player.
-- Call from the TTS console with:  lua rollTest("Brown", 3)
-- Or via the "DICE DEBUG" buttons in the admin panel (Host only).
-- E2E harness (TOR-164): rollE2eSeatPrep, rollSetFaces, rollE2eApplyConditions.
-- ============================================================================

local DK = require("lib.dice_kinds")
local DBV = require("lib.dice_bag_visibility")

--- Pool kinds with per-seat bags (E2E staged-dice verification).
local E2E_POOL_STAGED_KINDS = { "normal", "hunger", "rouse", "oblivRouse" }

--- Pool kinds that increment via `GlobalOnBagDieSpawned` (+1 per die), not `setPoolKindCount` preset.
local E2E_ADJUST_ON_BAG_SPAWN_KINDS = {
    [DK.KIND_ROUSE] = true,
    [DK.KIND_OBLIV_ROUSE] = true,
    [DK.KIND_BLOOD_SURGE_ROUSE] = true,
}

--- Staged die count in one player bag (PRE_ROLL arc), or 0 when bag missing.
-- @param color string
-- @param dieTypeName string  "normal"|"hunger"|"rouse"|"oblivRouse"
-- @return number
local function e2eBagStagedCount(color, dieTypeName)
    local guid = nil
    if dieTypeName == "hunger" then
        guid = G.GetDiceHungerGUID(color)
    elseif dieTypeName == "normal" then
        guid = G.GetDiceNormalGUID(color)
    elseif dieTypeName == "rouse" then
        guid = G.GetDiceRouseGUID(color)
    elseif dieTypeName == "oblivRouse" then
        guid = G.GetDiceOblivRouseGUID(color)
    end
    if type(guid) ~= "string" then
        return 0
    end
    local bag = G.getObject(guid)
    if bag == nil then
        return 0
    end
    local result = nil
    if dieTypeName == "rouse" then
        result = bag.call("getManualRouseSpawnedCount")
    end
    if type(result) ~= "number" then
        result = bag.call("getSpawnedCount")
    end
    return type(result) == "number" and result or 0
end

--- Compares expected pool counts to bag-staged dice (PRE_ROLL only).
-- @param color string
-- @param pool table  normalized or raw pool map
-- @return table  failure lines (empty when all kinds match)
local function e2ePoolStagedMismatchLines(color, pool)
    local lines = {}
    pool = DK.normalizePool(pool)
    for _, kindName in ipairs(E2E_POOL_STAGED_KINDS) do
        local want = pool[kindName] or 0
        if want > 0 then
            local got = e2eBagStagedCount(color, kindName)
            if got ~= want then
                lines[#lines + 1] = string.format("%s staged=%d want=%d", kindName, got, want)
            end
        end
    end
    return lines
end

--- Flags pool kinds whose bags must be at rest Y before staged spawn (hidden bags spawn underground).
-- @param color string
-- @param pool table
-- @return table  failure lines (empty when visible)
local function e2ePoolBagVisibilityLines(color, pool)
    -- Staged dice spawn at the Normal bag arc (per-kind rings); optional bags need not be raised.
    return {}
end

--- Total numeric face entries in a rollSetFaces / settlePresetCheck map.
-- @param facesByKind table
-- @return number
local function e2eCountExpectedFaces(facesByKind)
    local n = 0
    if type(facesByKind) ~= "table" then
        return 0
    end
    for _, values in pairs(facesByKind) do
        if type(values) == "table" then
            for _, faceVal in ipairs(values) do
                if type(faceVal) == "number" then
                    n = n + 1
                end
            end
        end
    end
    return n
end

--- Optional 5th argument to rollTest / rollTestNoDiff: hunger (number) or opts table.
-- @param extra number|table|nil
-- @return table opts { hunger?, skipOpen? }
local function parseRollTestExtras(extra)
    local opts = {}
    if type(extra) == "number" then
        opts.hunger = extra
    elseif type(extra) == "table" then
        if type(extra.hunger) == "number" then
            opts.hunger = extra.hunger
        end
        if extra.skipOpen == true then
            opts.skipOpen = true
        end
    end
    return opts
end

--- Pending async drawer release for rollE2eSettlePresetCheck (y > 2.5 gate).
-- @type table<string, { facesByKind: table, opts: table }>
local e2eSettlePresetPending = {}

--- @param color string
-- @return string|nil
local function rollE2eDrawerReleaseMinY()
    return 2.5
end

--- @param color string
-- @return boolean
local function rollE2eDrawerReadyForRelease(color)
    local DiceDrawer = require("lib.dice_drawer")
    local y = DiceDrawer.getPositionY(color)
    return type(y) == "number" and y > rollE2eDrawerReleaseMinY()
end

--- Host seat color after E2E roll tests (Storyteller).
local E2E_RETURN_SEAT = "Black"

--- Seated Host player (any seat). Do not use Player.Black when Host is on Brown, etc.
-- @return table|nil TTS Player
local function rollE2eGetHostPlayer()
    if Player == nil then
        return nil
    end
    local seated = Player.getPlayers()
    if type(seated) == "table" then
        for _, p in ipairs(seated) do
            if p ~= nil and p.host == true then
                return p
            end
        end
    end
    local black = Player[E2E_RETURN_SEAT]
    if black ~= nil and black.seated == true then
        return black
    end
    return nil
end

--- Move Host to seat, hide loading overlay, apply Debug Camera spoof for that seat.
-- @param targetColor string
-- @return boolean ok
local function rollE2eSeatPrepInternal(targetColor)
    if type(targetColor) ~= "string" or targetColor == "" then
        return false
    end

    local hostPlayer = rollE2eGetHostPlayer()
    if hostPlayer == nil then
        U.AlertGM("rollE2eSeatPrep: no seated Host found (join as Host first)")
        return false
    end

    local currentColor = hostPlayer.color
    if currentColor ~= targetColor and type(hostPlayer.changeColor) == "function" then
        local changed = hostPlayer.changeColor(targetColor)
        -- TTS returns false when already on targetColor; re-read seat after change.
        currentColor = hostPlayer.color
        if changed ~= true and currentColor ~= targetColor then
            U.AlertGM("rollE2eSeatPrep: changeColor failed for " .. targetColor
                .. " (Host on " .. tostring(currentColor) .. ")")
            return false
        end
    end

    UI.hide("overlay_loadingScreen_startup")

    local CameraDebugFocus = require("core.camera_debug_focus")
    local ok = CameraDebugFocus.onSpoofColor(hostPlayer, targetColor)
    if ok ~= true then
        U.AlertGM("rollE2eSeatPrep: camera spoof failed for " .. targetColor)
        return false
    end
    return true
end

--- Return Host to Black after testing another seat.
local function rollE2eReturnHostSeat()
    local hostPlayer = rollE2eGetHostPlayer()
    if hostPlayer == nil or type(hostPlayer.changeColor) ~= "function" then
        return
    end
    if hostPlayer.color == E2E_RETURN_SEAT then
        return
    end
    hostPlayer.changeColor(E2E_RETURN_SEAT)
end

--- Manual seat prep only; rollTest / rollStTest run prep automatically.
-- @param targetColor string
function DEBUG.rollE2eSeatPrep(targetColor)
    if type(targetColor) ~= "string" or targetColor == "" then
        print("Usage: rollE2eSeatPrep(targetColor)")
        return
    end
    rollE2eSeatPrepInternal(targetColor)
end

--- E2E spawn params: Black requires `fromRollClick` (production defers spawn until Roll).
-- @param color string
-- @return table
local function rollE2eSpawnActivePoolParams(color)
    local params = { color = color }
    if color == "Black" then
        params.fromRollClick = true
    end
    return params
end

--- Spawns staged dice for auto-populated roll types after rollTest / rollTestNoDiff openRoll.
-- @param color string
-- @param rollType string  C.RollType value
local function rollE2eSpawnDefaultPoolIfNeeded(color, rollType)
    local RO = require("lib.roll_options")
    if not RO.isRouseFamilyRoll(rollType)
        and rollType ~= C.RollType.SIMPLE_CHECK
        and rollType ~= C.RollType.REMORSE then
        return
    end
    if Global == nil or type(Global.call) ~= "function" then
        print("[rollE2eSpawnDefaultPoolIfNeeded] WARN: Global.call unavailable — pool die may be missing")
        return
    end
    Global.call("GlobalSpawnDefaultPoolDiceForActive", rollE2eSpawnActivePoolParams(color))
end

--- Initiates, opens, and arms a roll for a player in one shot.
-- Always runs E2E seat prep: Host changeColor → target seat, loading overlay hide, camera spoof.
--
-- @param color      string       Player seat color (e.g. "Brown")
-- @param difficulty number|nil   Required successes; default 2
-- @param rollType   string|nil   C.RollType value; default C.RollType.STANDARD
-- @param label      string|nil   Roll label shown in the panel; default "Debug Test"
-- @param hungerLevel number|nil  Sets player hunger 0–5 before arming (Suite G, etc.)
-- @usage lua rollTest("Brown", 3)
-- @usage lua rollTest("Brown", 3, C.RollType.STANDARD, "E2E G2", 2)
function DEBUG.rollTest(color, difficulty, rollType, label, hungerLevel)
    if type(color) ~= "string" then
        print("Usage: rollTest(color, difficulty?, rollType?, label?, hungerLevel?)")
        return
    end
    if RC == nil then
        print("[DEBUG.rollTest] RC (roll_controller) is not loaded yet.")
        return
    end

    local opts = parseRollTestExtras(hungerLevel)
    local diff  = (type(difficulty) == "number") and difficulty or 2
    local rType = (type(rollType) == "string" and rollType ~= "") and rollType or C.RollType.STANDARD
    local lbl   = (type(label) == "string" and label ~= "") and label or "Debug Test"

    if rollE2eSeatPrepInternal(color) ~= true then
        print("[DEBUG.rollTest] E2E seat prep failed — aborting.")
        return
    end

    if type(opts.hunger) == "number" then
        setHungerAndSync(color, opts.hunger)
    end

    RC.cancelRoll(color)

    local ok = RC.initiateRoll(color, { rollType = rType, label = lbl })
    if not ok then
        print(string.format("[DEBUG.rollTest] initiateRoll failed for %s (already active?)", color))
        rollE2eReturnHostSeat()
        return
    end
    if opts.skipOpen ~= true then
        RC.openRoll(color)
    end
    RC.setDifficulty(color, diff)
    rollE2eSpawnDefaultPoolIfNeeded(color, rType)

    local pid = S.getPlayerID(color)
    local phase = pid and S.getStateVal("playerData", pid, "hud", "rollData", "active", "phase")
    print(string.format(
        "[DEBUG.rollTest] %s %s — diff=%d type=%s label='%s'",
        color, tostring(phase or "unknown"), diff, rType, lbl))
end

--- Arms PRE_ROLL without setting ST difficulty (optional-difficulty E2E).
-- @param color string
-- @param rollType string|nil
-- @param label string|nil
-- @param hungerLevel number|nil
function DEBUG.rollTestNoDiff(color, rollType, label, hungerLevel)
    if type(color) ~= "string" then
        print("Usage: rollTestNoDiff(color, rollType?, label?, hungerLevel?)")
        return
    end
    if RC == nil then
        print("[DEBUG.rollTestNoDiff] RC (roll_controller) is not loaded yet.")
        return
    end

    local opts = parseRollTestExtras(hungerLevel)
    local rType = (type(rollType) == "string" and rollType ~= "") and rollType or C.RollType.STANDARD
    local lbl   = (type(label) == "string" and label ~= "") and label or "Debug Test No Diff"

    if rollE2eSeatPrepInternal(color) ~= true then
        print("[DEBUG.rollTestNoDiff] E2E seat prep failed — aborting.")
        return
    end

    if type(opts.hunger) == "number" then
        setHungerAndSync(color, opts.hunger)
    end

    RC.cancelRoll(color)

    local ok = RC.initiateRoll(color, { rollType = rType, label = lbl })
    if not ok then
        print(string.format("[DEBUG.rollTestNoDiff] initiateRoll failed for %s (already active?)", color))
        rollE2eReturnHostSeat()
        return
    end
    if opts.skipOpen ~= true then
        RC.openRoll(color)
    end
    rollE2eSpawnDefaultPoolIfNeeded(color, rType)

    local pid = S.getPlayerID(color)
    local phase = pid and S.getStateVal("playerData", pid, "hud", "rollData", "active", "phase")
    print(string.format(
        "[DEBUG.rollTestNoDiff] %s %s — no diff type=%s label='%s'",
        color, tostring(phase or "unknown"), rType, lbl))
end

--- E2E: ST dashboard **Open** (SETUP → PRE_ROLL). Use after SETUP pool build before Roll.
-- @param color string
-- @return boolean
function DEBUG.rollE2eOpenRoll(color)
    if RC == nil then
        print("[rollE2eOpenRoll] FAIL: RC not loaded")
        return false
    end
    if type(color) ~= "string" then
        print("[rollE2eOpenRoll] FAIL: color required")
        return false
    end
    local ok = RC.openRoll(color)
    if ok ~= true then
        print("[rollE2eOpenRoll] FAIL: RC.openRoll failed for " .. color)
        return false
    end
    print("[rollE2eOpenRoll] PASS — " .. color .. " opened to PRE_ROLL")
    return true
end

--- Normalizes E2E phase strings to C.RollPhase values.
-- @param phase string|nil
-- @return string|nil
local function normalizeRollPhaseAlias(phase)
    if type(phase) ~= "string" then
        return nil
    end
    local key = string.gsub(phase, "[%s_]", "")
    key = string.lower(key)
    local map = {
        setup = C.RollPhase.SETUP,
        preroll = C.RollPhase.PRE_ROLL,
        rolling = C.RollPhase.ROLLING,
        postroll = C.RollPhase.POST_ROLL,
        resolved = C.RollPhase.RESOLVED,
    }
    return map[key] or phase
end

--- True when `expected` is a numeric/presence matcher table (not a data subtree).
-- @param expected table
-- @return boolean
local function isRollExpectMatcher(expected)
    if type(expected) ~= "table" then
        return false
    end
    local hasMatcher = expected.present == true
        or expected.present == false
        or type(expected.min) == "number"
        or type(expected.max) == "number"
        or type(expected.gte) == "number"
        or type(expected.lte) == "number"
        or type(expected.oneOf) == "table"
    if not hasMatcher then
        return false
    end
    for k, _ in pairs(expected) do
        if k ~= "present" and k ~= "min" and k ~= "max" and k ~= "gte" and k ~= "lte" and k ~= "oneOf" then
            return false
        end
    end
    return true
end

--- Compares actual vs expected for rollConfirm; appends failure lines to `failures`.
-- @param actual any
-- @param expected any
-- @param path string
-- @param failures table
local function rollExpectCompare(actual, expected, path, failures)
    if expected == nil then
        return
    end

    if type(expected) == "table" and isRollExpectMatcher(expected) then
        if expected.present == false then
            if actual ~= nil then
                failures[#failures + 1] = path .. ": expected nil/absent, got " .. tostring(actual)
            end
            return
        end
        if expected.present == true then
            if actual == nil then
                failures[#failures + 1] = path .. ": expected value to be present, got nil"
            end
            return
        end
        if type(actual) ~= "number" then
            failures[#failures + 1] = path .. ": expected number, got " .. type(actual)
            return
        end
        if type(expected.min) == "number" and actual < expected.min then
            failures[#failures + 1] = path .. ": " .. tostring(actual) .. " < min " .. tostring(expected.min)
        end
        if type(expected.max) == "number" and actual > expected.max then
            failures[#failures + 1] = path .. ": " .. tostring(actual) .. " > max " .. tostring(expected.max)
        end
        if type(expected.gte) == "number" and actual < expected.gte then
            failures[#failures + 1] = path .. ": " .. tostring(actual) .. " < gte " .. tostring(expected.gte)
        end
        if type(expected.lte) == "number" and actual > expected.lte then
            failures[#failures + 1] = path .. ": " .. tostring(actual) .. " > lte " .. tostring(expected.lte)
        end
        if type(expected.oneOf) == "table" then
            local found = false
            for _, allowed in ipairs(expected.oneOf) do
                if actual == allowed then
                    found = true
                    break
                end
            end
            if not found then
                failures[#failures + 1] = path .. ": " .. tostring(actual) .. " not in oneOf list"
            end
        end
        return
    end

    if type(expected) ~= "table" then
        if actual ~= expected then
            failures[#failures + 1] = path
                .. ": got " .. tostring(actual) .. ", want " .. tostring(expected)
        end
        return
    end

    if type(actual) ~= "table" then
        failures[#failures + 1] = path .. ": expected table, got " .. type(actual)
        return
    end

    -- Mixed matcher + fields, e.g. { present = true, resultClass = { present = true } }:
    -- `present` means the actual table exists; other keys compare into actual (not actual.present).
    if expected.present == true and actual == nil then
        failures[#failures + 1] = path .. ": expected value to be present, got nil"
        return
    end

    for key, subExpected in pairs(expected) do
        if key == "marginAbsent" then
            if subExpected == true then
                if type(actual) ~= "table" or actual.margin ~= nil then
                    failures[#failures + 1] = path
                        .. ": expected margin absent (nil), got "
                        .. tostring(type(actual) == "table" and actual.margin or actual)
                end
            end
        elseif key == "present" then
            if subExpected == true then
                if actual == nil then
                    failures[#failures + 1] = path .. ": expected value to be present, got nil"
                end
            else
                rollExpectCompare(actual, subExpected, path, failures)
            end
        else
            rollExpectCompare(actual[key], subExpected, path .. "." .. tostring(key), failures)
        end
    end
end

--- Resolves seat color + expectation table from rollConfirm call shapes.
-- @param colorOrSpec string|table
-- @param maybeExpected table|nil
-- @return string|nil color, table|nil expected, string|nil err
local function parseRollConfirmArgs(colorOrSpec, maybeExpected)
    if type(colorOrSpec) == "table" and maybeExpected == nil then
        local spec = colorOrSpec
        return spec.color, spec, nil
    end
    if type(colorOrSpec) ~= "string" then
        return nil, nil, "first argument must be seat color or { color = ... }"
    end
    if string.lower(colorOrSpec) == "global" then
        if type(maybeExpected) ~= "table" then
            return nil, nil, "\"Global\" is deprecated — use rollConfirm(\"Brown\", { phase = \"preRoll\", ... })"
        end
        local seat = maybeExpected.color
        if type(seat) ~= "string" then
            return nil, nil, "with legacy \"Global\", pass expected.color = seat (e.g. \"Brown\")"
        end
        return seat, maybeExpected, nil
    end
    if type(maybeExpected) ~= "table" then
        return nil, nil, "second argument must be expectation table"
    end
    return colorOrSpec, maybeExpected, nil
end

--- Asserts roll state against expected values; prints PASS/FAIL with details.
-- @param colorOrSpec string|table  Seat color, or { color, phase?, active?, pool?, ... }
-- @param expected table|nil  e.g. { phase = "preRoll", active = { difficulty = 3, rollType = ... } }
-- @return boolean pass
-- @usage lua rollConfirm("Brown", { phase = "preRoll", active = { difficulty = 3, rollType = C.RollType.STANDARD } })
-- @usage lua rollConfirm({ color = "Brown", phase = "postRoll", active = { result = { present = true } } })
function DEBUG.rollConfirm(colorOrSpec, maybeExpected)
    -- Legacy one-arg form (pre–TOR-141 E2E harness): force-confirm POST_ROLL.
    if maybeExpected == nil and type(colorOrSpec) == "string" then
        print("[rollConfirm] DEPRECATED: one-arg rollConfirm(color) — use rollForceConfirm(color)")
        return DEBUG.rollForceConfirm(colorOrSpec)
    end

    local color, expected, err = parseRollConfirmArgs(colorOrSpec, maybeExpected)
    if err ~= nil then
        print("[rollConfirm] FAIL: " .. err)
        return false
    end

    local failures = {}
    local label = color

    if expected.noActive == true then
        label = color .. " noActive"
        local pid = S.getPlayerID(color)
        local active = pid and S.getStateVal("playerData", pid, "hud", "rollData", "active")
        if active ~= nil then
            failures[#failures + 1] = "expected no active roll, but active roll exists"
        end
    else
        local pid = S.getPlayerID(color)
        if pid == nil then
            failures[#failures + 1] = "no playerID for seat " .. tostring(color)
        else
            local active = S.getStateVal("playerData", pid, "hud", "rollData", "active")
            if active == nil then
                failures[#failures + 1] = "expected active roll, got nil"
            else
                local phaseExpected = expected.phase or expected.preRoll or expected.postRoll
                    or expected.rolling or expected.setup or expected.resolved
                if type(phaseExpected) == "string" then
                    local wantPhase = normalizeRollPhaseAlias(phaseExpected)
                    local actualPhase = active.phase
                    if Global ~= nil and type(Global.call) == "function" then
                        local gPhase = Global.call("GlobalGetRollPhase", { color = color })
                        if type(gPhase) == "string" then
                            actualPhase = gPhase
                        end
                    end
                    if actualPhase ~= wantPhase then
                        failures[#failures + 1] = "phase: got "
                            .. tostring(actualPhase) .. ", want " .. tostring(wantPhase)
                    end
                end

                if type(expected.pool) == "table" then
                    expected.active = expected.active or {}
                    expected.active.pool = expected.active.pool or {}
                    for k, v in pairs(expected.pool) do
                        expected.active.pool[k] = v
                    end
                    if active.phase == C.RollPhase.PRE_ROLL or active.phase == C.RollPhase.SETUP then
                        local stagedMismatches = e2ePoolStagedMismatchLines(color, expected.pool)
                        for _, line in ipairs(stagedMismatches) do
                            failures[#failures + 1] = "pool staged: " .. line
                        end
                        local visibilityMismatches = e2ePoolBagVisibilityLines(color, expected.pool)
                        for _, line in ipairs(visibilityMismatches) do
                            failures[#failures + 1] = "pool bag visibility: " .. line
                        end
                    end
                end
                if type(expected.meta) == "table" then
                    expected.active = expected.active or {}
                    expected.active.meta = expected.active.meta or {}
                    for k, v in pairs(expected.meta) do
                        expected.active.meta[k] = v
                    end
                end
                if type(expected.result) == "table" then
                    expected.active = expected.active or {}
                    expected.active.result = expected.active.result or {}
                    for k, v in pairs(expected.result) do
                        expected.active.result[k] = v
                    end
                end

                if type(expected.active) == "table" then
                    rollExpectCompare(active, expected.active, "active", failures)
                end

                if type(expected.rouseStripLabel) == "string" then
                    local strips = active.rouseOutcomeStrips
                    local found = false
                    if type(strips) == "table" then
                        for _, strip in ipairs(strips) do
                            if type(strip) == "table" and strip.label == expected.rouseStripLabel then
                                found = true
                                break
                            end
                        end
                    end
                    if not found then
                        failures[#failures + 1] = "rouseOutcomeStrips: no strip labeled "
                            .. expected.rouseStripLabel
                    end
                end

                if type(expected.rouseOutcomeStripsMin) == "number" then
                    local n = (type(active.rouseOutcomeStrips) == "table") and #active.rouseOutcomeStrips or 0
                    if n < expected.rouseOutcomeStripsMin then
                        failures[#failures + 1] = "rouseOutcomeStrips: count "
                            .. tostring(n) .. " < min " .. tostring(expected.rouseOutcomeStripsMin)
                    end
                end

                if type(expected.rouseStrip) == "table" then
                    local wantLabel = expected.rouseStrip.label
                    local wantNarrative = expected.rouseStrip.narrative
                    local strips = active.rouseOutcomeStrips
                    local found = false
                    if type(strips) == "table" then
                        for _, strip in ipairs(strips) do
                            if type(strip) == "table" then
                                local labelOk = type(wantLabel) ~= "string" or strip.label == wantLabel
                                local narrOk = type(wantNarrative) ~= "string"
                                    or strip.narrative == wantNarrative
                                if labelOk and narrOk then
                                    found = true
                                    break
                                end
                            end
                        end
                    end
                    if not found then
                        failures[#failures + 1] = "rouseOutcomeStrips: no strip matching "
                            .. JSON.encode(expected.rouseStrip)
                    end
                end

                if type(expected.wpRerollChosenCount) == "number" then
                    local guids = (type(active.willpower) == "table")
                        and active.willpower.rerollRandomizedGuids or nil
                    local nChosen = 0
                    if type(guids) == "table" then
                        for _ in pairs(guids) do
                            nChosen = nChosen + 1
                        end
                    end
                    if nChosen ~= expected.wpRerollChosenCount then
                        failures[#failures + 1] = "willpower.rerollRandomizedGuids: count "
                            .. tostring(nChosen) .. ", want "
                            .. tostring(expected.wpRerollChosenCount)
                    end
                end

                if type(expected.contributingIncludes) == "table" then
                    local contrib = (type(active.rollPolicy) == "table")
                        and active.rollPolicy.contributingConditions or {}
                    for _, wantId in ipairs(expected.contributingIncludes) do
                        local foundId = false
                        if type(contrib) == "table" then
                            for _, cid in ipairs(contrib) do
                                if cid == wantId then
                                    foundId = true
                                    break
                                end
                            end
                        end
                        if not foundId then
                            failures[#failures + 1] = "rollPolicy.contributingConditions: missing "
                                .. tostring(wantId)
                        end
                    end
                end
            end
        end
    end

    if #failures == 0 then
        print("[rollConfirm] PASS — " .. label)
        return true
    end
    print("[rollConfirm] FAIL — " .. label)
    for i, line in ipairs(failures) do
        print(string.format("  %d) %s", i, line))
    end
    return false
end

--- Reads humanity stain count for a seat (player PCs only).
-- @param color string
-- @return number
local function readHumanityStains(color)
    local pid = S.getPlayerID(color)
    if pid == nil then
        return 0
    end
    local stats = S.getStateVal("playerData", pid, "stats")
    if type(stats) ~= "table" then
        return 0
    end
    if type(stats.humanity) == "table" then
        return math.max(0, math.floor(tonumber(stats.humanity.stains) or 0))
    end
    return math.max(0, math.floor(tonumber(stats.stains) or 0))
end

--- Asserts tracker fields (hunger, stains, superficial willpower) for E2E.
-- @param color string
-- @param expected table  { hunger?, stains?, willpowerSuperficial? }
-- @return boolean pass
-- @usage lua rollConfirmTracker("Brown", { hunger = 3 })
function DEBUG.rollConfirmTracker(color, expected)
    if type(color) ~= "string" or type(expected) ~= "table" then
        print("[rollConfirmTracker] FAIL: usage rollConfirmTracker(color, { hunger = N, ... })")
        return false
    end
    local failures = {}
    if type(expected.hunger) == "number" then
        local actual = math.floor(tonumber(S.getPlayerVal(color, "hunger")) or 0)
        if actual ~= expected.hunger then
            failures[#failures + 1] = "hunger: got " .. tostring(actual)
                .. ", want " .. tostring(expected.hunger)
        end
    end
    if type(expected.stains) == "number" then
        local actual = readHumanityStains(color)
        if actual ~= expected.stains then
            failures[#failures + 1] = "stains: got " .. tostring(actual)
                .. ", want " .. tostring(expected.stains)
        end
    end
    if type(expected.willpowerSuperficial) == "number" then
        local pid = S.getPlayerID(color)
        local actual = 0
        if pid ~= nil then
            local stats = S.getStateVal("playerData", pid, "stats")
            if type(stats) == "table" and type(stats.willpower) == "table" then
                actual = math.floor(tonumber(stats.willpower.superficial) or 0)
            end
        end
        if actual ~= expected.willpowerSuperficial then
            failures[#failures + 1] = "willpower.superficial: got " .. tostring(actual)
                .. ", want " .. tostring(expected.willpowerSuperficial)
        end
    end
    if #failures == 0 then
        print("[rollConfirmTracker] PASS — " .. color)
        return true
    end
    print("[rollConfirmTracker] FAIL — " .. color)
    for i, line in ipairs(failures) do
        print(string.format("  %d) %s", i, line))
    end
    return false
end

--- Sets humanity stain count for a seat and syncs presentation.
-- @param color string
-- @param value number
-- @usage lua setHumanityStains("Purple", 2)
function DEBUG.setHumanityStains(color, value)
    if type(color) ~= "string" or type(value) ~= "number" then
        print("Usage: setHumanityStains(color, value)")
        return
    end
    local pid = S.getPlayerID(color)
    if pid == nil then
        print("[setHumanityStains] no playerID for " .. color)
        return
    end
    local n = math.max(0, math.floor(value))
    S.setStateVal(n, "playerData", pid, "stats", "humanity", "stains")
    local Conditions = require("core.conditions")
    Conditions.reconcileDerivedForPlayer(pid)
    Conditions.afterChange(pid)
    Sync.player(color)
    print(string.format("[setHumanityStains] %s stains=%d", color, n))
end

--- Sets superficial willpower boxes for a seat and syncs presentation.
-- @param color string
-- @param value number
-- @usage lua setWillpowerSuperficial("Brown", 3)
function DEBUG.setWillpowerSuperficial(color, value)
    if type(color) ~= "string" or type(value) ~= "number" then
        print("Usage: setWillpowerSuperficial(color, value)")
        return
    end
    local pid = S.getPlayerID(color)
    if pid == nil then
        print("[setWillpowerSuperficial] no playerID for " .. color)
        return
    end
    local n = math.max(0, math.floor(value))
    S.setStateVal(n, "playerData", pid, "stats", "willpower", "superficial")
    local Conditions = require("core.conditions")
    Conditions.reconcileDerivedForPlayer(pid)
    Conditions.afterChange(pid)
    Sync.player(color)
    print(string.format("[setWillpowerSuperficial] %s superficial=%d", color, n))
end

--- Asserts a player dice bag is enabled/disabled (visible at rest Y).
-- @param color string
-- @param dieKind string  DK.KIND_* value
-- @param wantEnabled boolean
-- @return boolean
-- @usage lua rollE2eConfirmBagEnabled("Brown", "hunger", false)
function DEBUG.rollE2eConfirmBagEnabled(color, dieKind, wantEnabled)
    if type(color) ~= "string" or type(dieKind) ~= "string" or type(wantEnabled) ~= "boolean" then
        print("[rollE2eConfirmBagEnabled] FAIL: usage rollE2eConfirmBagEnabled(color, dieKind, wantEnabled)")
        return false
    end
    if Global == nil or type(Global.call) ~= "function" then
        print("[rollE2eConfirmBagEnabled] FAIL: Global.call unavailable")
        return false
    end
    local got = Global.call("GlobalIsPlayerDiceBagEnabled", { color = color, dieKind = dieKind }) == true
    if got == wantEnabled then
        print(string.format(
            "[rollE2eConfirmBagEnabled] PASS — %s %s enabled=%s",
            color, dieKind, tostring(got)))
        return true
    end
    print(string.format(
        "[rollE2eConfirmBagEnabled] FAIL — %s %s: got enabled=%s, want %s",
        color, dieKind, tostring(got), tostring(wantEnabled)))
    return false
end

--- Asserts gameState.storytellerRolls fields for E2E.
-- @param expected table  { liveSlotIndex?, slotNotCleared?, initiateBlocked?, initiateLabel?, initiateRollType? }
-- @return boolean pass
-- @usage lua rollStConfirm({ liveSlotIndex = 1 })
function DEBUG.rollStConfirm(expected)
    if type(expected) ~= "table" then
        print("[rollStConfirm] FAIL: expected table required")
        return false
    end
    local failures = {}
    local root = S.getStateVal("storytellerRolls") or {}
    if expected.liveSlotIndexAbsent == true then
        if root.liveSlotIndex ~= nil then
            failures[#failures + 1] = "liveSlotIndex: expected nil, got "
                .. tostring(root.liveSlotIndex)
        end
    elseif type(expected.liveSlotIndex) == "number" then
        local actual = root.liveSlotIndex
        if actual ~= expected.liveSlotIndex then
            failures[#failures + 1] = "liveSlotIndex: got " .. tostring(actual)
                .. ", want " .. tostring(expected.liveSlotIndex)
        end
    end
    if type(expected.slotNotCleared) == "number" then
        local idx = expected.slotNotCleared
        local slot = type(root.slots) == "table" and root.slots[idx] or nil
        if type(slot) ~= "table" or slot.cleared == true then
            failures[#failures + 1] = string.format(
                "slotNotCleared: slot %d should remain occupied (cleared=false), got %s",
                idx, type(slot) == "table" and tostring(slot.cleared) or "no slot")
        end
    end
    if expected.initiateBlocked == true then
        local STR = require("core.storyteller_rolls")
        local lbl = (type(expected.initiateLabel) == "string") and expected.initiateLabel or "E2E blocked"
        local rt = expected.initiateRollType or C.RollType.STANDARD
        local ok = STR.initiateNpcRoll(lbl, rt)
        if ok then
            failures[#failures + 1] = "initiateNpcRoll: expected blocked, got ok"
        end
    end
    if #failures == 0 then
        print("[rollStConfirm] PASS")
        return true
    end
    print("[rollStConfirm] FAIL")
    for i, line in ipairs(failures) do
        print(string.format("  %d) %s", i, line))
    end
    return false
end

--- Seconds to wait after staged dice spawn (or human bag clicks) before releasing to the tray.
-- Covers drawer smooth transition to y > 2.5 (`GlobalWaitDrawerThenReleaseBagDice` gate).
-- @return number
function DEBUG.rollE2eDiceTrayWaitSeconds()
    local OP = require("lib.object_positions")
    return OP.DEFAULT_TRANSITION_DURATION + 0.5
end

--- `U.RunSequence` step: pause after spawn so the dice tray can finish opening before release.
-- @return number seconds for RunSequence wait
function DEBUG.rollE2eWaitForDiceTray()
    return DEBUG.rollE2eDiceTrayWaitSeconds()
end

--- E2E: dice-tray camera + open drawer (mirrors panel Roll prep, without releasing dice).
-- @param color string
-- @return boolean
function DEBUG.rollE2ePrepareRollRelease(color)
    if type(color) ~= "string" then
        print("[rollE2ePrepareRollRelease] FAIL: color required")
        return false
    end
    if Global == nil or type(Global.call) ~= "function" then
        print("[rollE2ePrepareRollRelease] FAIL: Global.call unavailable")
        return false
    end
    Global.call("GlobalRollSeatCamera", { color = color, mode = "diceTray" })
    local DiceDrawer = require("lib.dice_drawer")
    DiceDrawer.openForRoll(color)
    return true
end

--- E2E: spawn any missing active PRE_ROLL pool dice (same as panel Roll auto-spawn).
-- @param color string
-- @return boolean
function DEBUG.rollE2eSpawnActivePool(color)
    if type(color) ~= "string" then
        print("[rollE2eSpawnActivePool] FAIL: color required")
        return false
    end
    if Global == nil or type(Global.call) ~= "function" then
        print("[rollE2eSpawnActivePool] FAIL: Global.call unavailable")
        return false
    end
    Global.call("GlobalSpawnActivePoolDiceForActive", rollE2eSpawnActivePoolParams(color))
    return true
end

--- Internal: release (if needed), startRolling, preset faces, settle.
-- @param color string
-- @param facesByKind table
-- @param opts table
-- @return boolean pass
local function rollE2eFinishSettlePresetCheck(color, facesByKind, opts)
    local pid = S.getPlayerID(color)
    if pid == nil then
        print("[rollE2eSettlePresetCheck] FAIL: no playerID for " .. color)
        return false
    end
    local active = S.getStateVal("playerData", pid, "hud", "rollData", "active")
    if active == nil then
        print("[rollE2eSettlePresetCheck] FAIL: no active roll for " .. color
            .. " (run rollTest first; do not click panel Roll before this helper)")
        return false
    end

    if active.phase == C.RollPhase.PRE_ROLL then
        if opts.skipSpawn ~= true then
            DEBUG.rollE2eSpawnActivePool(color)
        end
        if opts.skipRelease ~= true then
            DEBUG.rollE2ePrepareRollRelease(color)
            if Global ~= nil and type(Global.call) == "function" then
                Global.call("GlobalReleaseBagDice", { color = color })
            end
        end
        active = S.getStateVal("playerData", pid, "hud", "rollData", "active")
        if active == nil or active.phase ~= C.RollPhase.PRE_ROLL then
            print("[rollE2eSettlePresetCheck] FAIL: roll left PRE_ROLL before startRolling for " .. color)
            return false
        end
        if RC.startRolling(color) ~= true then
            print("[rollE2eSettlePresetCheck] FAIL: RC.startRolling failed (phase="
                .. tostring(active.phase) .. ")")
            return false
        end
    elseif active.phase ~= C.RollPhase.ROLLING and active.phase ~= C.RollPhase.POST_ROLL then
        print("[rollE2eSettlePresetCheck] FAIL: phase " .. tostring(active.phase)
            .. " (expected preRoll, rolling, or postRoll)")
        return false
    end

    local expectedFaces = e2eCountExpectedFaces(facesByKind)
    local facesSet = DEBUG.rollSetFaces(color, facesByKind)
    if type(facesSet) ~= "number" or facesSet < expectedFaces then
        print(string.format(
            "[rollE2eSettlePresetCheck] FAIL: preset %d/%d face(s) for %s",
            type(facesSet) == "number" and facesSet or 0, expectedFaces, color))
        return false
    end

    RC.onDiceSettled(color)
    print("[rollE2eSettlePresetCheck] PASS — preset faces applied and settle triggered for " .. color)
    return true
end

--- E2E: spawn pool dice, startRolling, preset faces, settle (no randomize).
-- Replaces panel **Roll** + manual face tweak. Playbook flow: spawn (or human bags) →
-- `rollE2eWaitForDiceTray` → this helper (returns wait seconds when drawer release is async)
-- → `rollE2eSettlePresetCheckResume` → assertions.
-- Do not click panel Roll before this helper.
-- @param color string
-- @param facesByKind table
-- @param opts table|nil  `{ skipSpawn = true, skipRelease = true }`
-- @return boolean|number  true/false when done; number = seconds to wait before Resume
-- @usage lua rollE2eSettlePresetCheck("Brown", { rouse = { 4 } }, { skipSpawn = true })
function DEBUG.rollE2eSettlePresetCheck(color, facesByKind, opts)
    if RC == nil then
        print("[rollE2eSettlePresetCheck] FAIL: RC not loaded")
        return false
    end
    opts = type(opts) == "table" and opts or {}
    if type(color) ~= "string" or type(facesByKind) ~= "table" then
        print("[rollE2eSettlePresetCheck] FAIL: usage rollE2eSettlePresetCheck(color, { rouse = { 4 } }, { skipSpawn = true })")
        return false
    end

    e2eSettlePresetPending[color] = nil

    local pid = S.getPlayerID(color)
    if pid == nil then
        print("[rollE2eSettlePresetCheck] FAIL: no playerID for " .. color)
        return false
    end
    local active = S.getStateVal("playerData", pid, "hud", "rollData", "active")
    if active == nil then
        print("[rollE2eSettlePresetCheck] FAIL: no active roll for " .. color
            .. " (run rollTest first; do not click panel Roll before this helper)")
        return false
    end

    if active.phase == C.RollPhase.PRE_ROLL and opts.skipRelease ~= true then
        if opts.skipSpawn ~= true then
            DEBUG.rollE2eSpawnActivePool(color)
        end
        -- Check drawer height before opening; openForRoll animates ~DEFAULT_TRANSITION_DURATION.
        if rollE2eDrawerReadyForRelease(color) then
            return rollE2eFinishSettlePresetCheck(color, facesByKind, opts)
        end
        DEBUG.rollE2ePrepareRollRelease(color)
        if Global ~= nil and type(Global.call) == "function" then
            Global.call("GlobalReleaseBagDice", { color = color })
        end
        e2eSettlePresetPending[color] = { facesByKind = facesByKind, opts = opts }
        return DEBUG.rollE2eDiceTrayWaitSeconds()
    end

    return rollE2eFinishSettlePresetCheck(color, facesByKind, opts)
end

--- E2E: complete `rollE2eSettlePresetCheck` after async drawer release wait.
-- No-op when the prior settle step finished synchronously.
-- @param color string
-- @return boolean|nil
function DEBUG.rollE2eSettlePresetCheckResume(color)
    if type(color) ~= "string" then
        print("[rollE2eSettlePresetCheckResume] FAIL: color required")
        return false
    end
    local pending = e2eSettlePresetPending[color]
    if pending == nil then
        return true
    end
    e2eSettlePresetPending[color] = nil
    local opts = type(pending.opts) == "table" and pending.opts or {}
    opts.skipRelease = true
    return rollE2eFinishSettlePresetCheck(color, pending.facesByKind, opts)
end

--- Prints the full active roll record and queue for a player.
-- @param color string  Player seat color
-- @usage lua rollState("Brown")
function DEBUG.rollState(color)
    if type(color) ~= "string" then
        print("Usage: rollState(color)")
        return
    end
    local pid    = S.getPlayerID(color)
    local active = pid and S.getStateVal("playerData", pid, "hud", "rollData", "active")
    local queue  = (pid and S.getStateVal("playerData", pid, "hud", "rollData", "queue")) or {}
    print(string.format("[DEBUG.rollState] %s — active roll:", color))
    if active ~= nil then
        print(JSON.encode_pretty(active))
    else
        print("  (no active roll)")
    end
    if #queue > 0 then
        print(string.format("  queue (%d): %s", #queue, JSON.encode(queue)))
    end
end

--- Normalizes a custom-die rotation label (TTS may use string or number).
-- @param v string|number|nil
-- @return number|nil
local function e2eFaceValueNumber(v)
    if type(v) == "number" then
        return math.floor(v)
    end
    if type(v) == "string" then
        return tonumber(v)
    end
    return nil
end

--- True when a rotation-value entry matches the target face (1–10).
-- @param entryValue string|number
-- @param target number
-- @return boolean
local function e2eRotationEntryMatches(entryValue, target)
    local n = e2eFaceValueNumber(entryValue)
    return n ~= nil and n == target
end

--- Reads a custom die face as integer 1–10 (E2E preset verify only).
-- @param obj Object
-- @return number|nil
local function e2eReadDieFaceValue(obj)
    if obj == nil then
        return nil
    end
    local raw = nil
    if obj.getRotationValue ~= nil then
        raw = obj.getRotationValue()
    end
    if raw == nil and obj.getValue ~= nil then
        raw = obj.getValue()
    end
    return e2eFaceValueNumber(raw)
end

--- Applies one custom-die face via instant setRotation (not setRotationValue).
-- setValue/setRotationValue animate (lift/spin/drop); immediate getValue still reads the old face.
-- Bag-spawned dice are locked until panel Roll — unlock briefly for the snap.
-- @param obj Object
-- @param faceVal number 1–10
-- @return boolean true when read-back matches target
local function e2eSetDieFaceValue(obj, faceVal)
    if obj == nil or type(faceVal) ~= "number" then
        return false
    end
    local target = math.floor(faceVal)
    if target < 1 or target > 10 then
        return false
    end

    local snapRot = nil
    if obj.getRotationValues ~= nil then
        local rotValues = obj.getRotationValues()
        if type(rotValues) == "table" then
            for _, entry in ipairs(rotValues) do
                if type(entry) == "table" and e2eRotationEntryMatches(entry.value, target) then
                    snapRot = entry.rotation
                    break
                end
            end
        end
    end
    if snapRot == nil or type(snapRot.x) ~= "number" then
        print(string.format(
            "[DEBUG.rollSetFaces] WARN: no RotationValues entry for face %d (guid=%s)",
            target, tostring(obj.getGUID())))
        return false
    end

    local wasLocked = obj.getLock()
    if wasLocked then
        obj.setLock(false)
    end
    if obj.setRotation ~= nil then
        obj.setRotation({ x = snapRot.x, y = snapRot.y, z = snapRot.z })
    elseif obj.setRotationSmooth ~= nil then
        obj.setRotationSmooth({ x = snapRot.x, y = snapRot.y, z = snapRot.z }, false, true)
    else
        if wasLocked then
            obj.setLock(true)
        end
        return false
    end

    local actual = e2eReadDieFaceValue(obj)
    if wasLocked then
        obj.setLock(true)
    end
    if actual ~= target then
        print(string.format(
            "[DEBUG.rollSetFaces] WARN: snap face %d read back as %s (guid=%s)",
            target, tostring(actual), tostring(obj.getGUID())))
        return false
    end
    return true
end

--- Locks rouse-family dice so Take Half + rouse `recalculate` gate passes after E2E face preset.
-- @param color string
-- @param rollId string
local function e2eLockRouseDiceForTakeHalf(color, rollId)
    if RC == nil or type(rollId) ~= "string" then
        return
    end
    local _, objectsByKind = RC._getDicePoolsForRoll(color, rollId)
    for _, kind in ipairs({ DK.KIND_ROUSE, DK.KIND_BLOOD_SURGE_ROUSE, DK.KIND_OBLIV_ROUSE }) do
        local objs = objectsByKind[kind]
        if type(objs) == "table" then
            for _, obj in ipairs(objs) do
                if obj ~= nil then
                    obj.setLock(true)
                end
            end
        end
    end
end

--- Sets physical die faces for the active roll (ROLLING or POST_ROLL), then recalculates.
-- @param color string
-- @param facesByKind table e.g. { normal = {7, 7}, hunger = {10, 1}, rouse = {4} }
-- @return number facesSet
-- @usage lua rollSetFaces("Brown", { normal = {4, 4}, hunger = {10, 1} })
function DEBUG.rollSetFaces(color, facesByKind)
    if RC == nil then
        print("[DEBUG.rollSetFaces] RC not loaded")
        return 0
    end
    if type(color) ~= "string" or type(facesByKind) ~= "table" then
        print("Usage: rollSetFaces(color, { normal={7,7}, hunger={10,1}, ... })")
        return 0
    end
    local pid = S.getPlayerID(color)
    if pid == nil then
        print("[DEBUG.rollSetFaces] no playerID for " .. color)
        return 0
    end
    local active = S.getStateVal("playerData", pid, "hud", "rollData", "active")
    if active == nil or type(active.id) ~= "string" then
        print("[DEBUG.rollSetFaces] no active roll for " .. color)
        return 0
    end
    if active.phase ~= C.RollPhase.ROLLING and active.phase ~= C.RollPhase.POST_ROLL then
        print(string.format(
            "[DEBUG.rollSetFaces] %s: roll phase is %s (need rolling or postRoll)",
            color, tostring(active.phase)))
        return 0
    end
    local expectedCount = e2eCountExpectedFaces(facesByKind)
    local _, objectsByKind = RC._getDicePoolsForRoll(color, active.id)
    local setCount = 0
    for kind, values in pairs(facesByKind) do
        if type(values) == "table" then
            local objs = type(objectsByKind[kind]) == "table" and objectsByKind[kind] or {}
            for i, faceVal in ipairs(values) do
                if type(faceVal) == "number" then
                    local obj = objs[i]
                    if obj == nil then
                        print(string.format(
                            "[DEBUG.rollSetFaces] WARN: no %s die #%d on table for face %d",
                            tostring(kind), i, faceVal))
                    elseif e2eSetDieFaceValue(obj, faceVal) then
                        setCount = setCount + 1
                    end
                end
            end
        end
    end
    if expectedCount > 0 and setCount < expectedCount then
        print(string.format(
            "[DEBUG.rollSetFaces] FAIL — %s: set %d/%d face(s) (missing dice or face snap failed)",
            color, setCount, expectedCount))
        return 0
    end
    if active.takeHalfAwaitingRouse == true then
        e2eLockRouseDiceForTakeHalf(color, active.id)
    end
    local recalcOk = RC.recalculate(color)
    if recalcOk == true then
        print(string.format("[DEBUG.rollSetFaces] %s: set %d face(s); recalculated.", color, setCount))
    else
        local after = S.getStateVal("playerData", pid, "hud", "rollData", "active")
        print(string.format(
            "[DEBUG.rollSetFaces] %s: set %d face(s); recalculate returned false (phase=%s, takeHalfAwaitingRouse=%s)",
            color,
            setCount,
            tostring(type(after) == "table" and after.phase or nil),
            tostring(type(after) == "table" and after.takeHalfAwaitingRouse or nil)
        ))
    end
    return setCount
end

--- Apply temporary manual conditions for E2E (e.g. Suite F). Cleared with rollE2eClearConditions.
-- @param color string
-- @param conditionIds table Array of condition id strings from condition_defs
-- @usage lua rollE2eApplyConditions("Brown", { "bestialNull" })
function DEBUG.rollE2eApplyConditions(color, conditionIds)
    if type(color) ~= "string" or type(conditionIds) ~= "table" then
        print("Usage: rollE2eApplyConditions(color, { 'conditionId', ... })")
        return
    end
    local playerID = S.getPlayerID(color)
    if playerID == nil then
        print("[DEBUG.rollE2eApplyConditions] no playerID for " .. color)
        return
    end
    local Conditions = require("core.conditions")
    for _, cid in ipairs(conditionIds) do
        if type(cid) == "string" and cid ~= "" then
            Conditions.setManual(playerID, cid, true)
            print("[DEBUG.rollE2eApplyConditions] set " .. cid .. " on " .. color)
        end
    end
end

--- Clear manual conditions applied for E2E on one seat.
-- @param color string
-- @param conditionIds table|nil  If nil, clears bestialNull
function DEBUG.rollE2eClearConditions(color, conditionIds)
    if type(color) ~= "string" then
        print("Usage: rollE2eClearConditions(color, conditionIds?)")
        return
    end
    local playerID = S.getPlayerID(color)
    if playerID == nil then
        return
    end
    local ids = conditionIds
    if type(ids) ~= "table" then
        ids = { "bestialNull" }
    end
    local Conditions = require("core.conditions")
    for _, cid in ipairs(ids) do
        if type(cid) == "string" and cid ~= "" then
            Conditions.clear(playerID, cid)
        end
    end
    print("[DEBUG.rollE2eClearConditions] cleared on " .. color)
end

--- Sets active.pool and spawns staged dice (PRE_ROLL). Use before rollE2eSettlePresetCheck.
-- @param color string
-- @param normal number
-- @param hunger number
-- @return boolean
-- @usage lua rollE2eSetPoolAndSpawn("Brown", 2, 0)
function DEBUG.rollE2eSetPoolAndSpawn(color, normal, hunger)
    if RC == nil then
        print("[rollE2eSetPoolAndSpawn] FAIL: RC not loaded")
        return false
    end
    if type(color) ~= "string" or type(normal) ~= "number" or type(hunger) ~= "number" then
        print("[rollE2eSetPoolAndSpawn] FAIL: usage rollE2eSetPoolAndSpawn(color, normal, hunger)")
        return false
    end
    if RC.setPool(color, normal, hunger) ~= true then
        print("[rollE2eSetPoolAndSpawn] FAIL: RC.setPool failed for " .. color)
        return false
    end
    if Global == nil or type(Global.call) ~= "function" then
        print("[rollE2eSetPoolAndSpawn] FAIL: Global.call unavailable")
        return false
    end
    Global.call("GlobalSpawnActivePoolDiceForActive", rollE2eSpawnActivePoolParams(color))

    local pid = S.getPlayerID(color)
    local active = pid and S.getStateVal("playerData", pid, "hud", "rollData", "active")
    local pool = type(active) == "table" and DK.normalizePool(active.pool) or {}
    local stagedMismatches = e2ePoolStagedMismatchLines(color, pool)
    local visibilityMismatches = e2ePoolBagVisibilityLines(color, pool)
    if #stagedMismatches > 0 or #visibilityMismatches > 0 then
        print("[rollE2eSetPoolAndSpawn] FAIL — pool spawn check for " .. color)
        for i, line in ipairs(stagedMismatches) do
            print(string.format("  %d) %s", i, line))
        end
        for i, line in ipairs(visibilityMismatches) do
            print(string.format("  %d) %s", i, line))
        end
        return false
    end

    print(string.format(
        "[rollE2eSetPoolAndSpawn] %s: pool normal=%d hunger=%d spawned",
        color, normal, hunger))
    return true
end

--- Builds pool from Auto-Hunger bag-click rules, then spawns (Suite G classify tests).
-- Each virtual Normal-bag click adds Hunger while pool.hunger < seat hunger, else Normal.
-- @param color string
-- @param normalBagClicks number  Total left-clicks on Normal bag (not Hunger bag)
-- @return boolean
-- @usage lua rollE2eSetPoolAutoHunger("Brown", 4)
function DEBUG.rollE2eSetPoolAutoHunger(color, normalBagClicks)
    if type(color) ~= "string" or type(normalBagClicks) ~= "number" then
        print("[rollE2eSetPoolAutoHunger] FAIL: usage rollE2eSetPoolAutoHunger(color, normalBagClicks)")
        return false
    end
    local clicks = math.max(0, math.floor(normalBagClicks))
    local pid = S.getPlayerID(color)
    local hungerLevel = 0
    if pid ~= nil then
        local stats = S.getStateVal("playerData", pid, "stats")
        if type(stats) == "table" then
            hungerLevel = tonumber(stats.hunger) or 0
        end
    end
    local hunger = 0
    local normal = 0
    for _ = 1, clicks do
        if hunger < hungerLevel then
            hunger = hunger + 1
        else
            normal = normal + 1
        end
    end
    return DEBUG.rollE2eSetPoolAndSpawn(color, normal, hunger)
end

--- E2E: add a pool kind count and spawn staged dice (PRE_ROLL only).
-- Use after `rollE2eSetPoolAndSpawn` when a test needs rouse (or other kinds) in the pool.
-- Rouse-family kinds match bag-click semantics: each spawn increments pool (+1); do not preset count.
-- @param color string
-- @param kind string  e.g. `DK.KIND_ROUSE` (`"rouse"`)
-- @param count number
-- @return boolean
-- @usage lua rollE2eAddPoolKindSpawn("Brown", "rouse", 1)
function DEBUG.rollE2eAddPoolKindSpawn(color, kind, count)
    if RC == nil then
        print("[rollE2eAddPoolKindSpawn] FAIL: RC not loaded")
        return false
    end
    if type(color) ~= "string" or type(kind) ~= "string" or type(count) ~= "number" then
        print("[rollE2eAddPoolKindSpawn] FAIL: usage rollE2eAddPoolKindSpawn(color, kind, count)")
        return false
    end
    if Global == nil or type(Global.call) ~= "function" then
        print("[rollE2eAddPoolKindSpawn] FAIL: Global.call unavailable")
        return false
    end

    count = math.max(0, math.floor(count))

    if E2E_ADJUST_ON_BAG_SPAWN_KINDS[kind] == true then
        local pid = S.getPlayerID(color)
        if pid == nil then
            print("[rollE2eAddPoolKindSpawn] FAIL: no playerID for " .. color)
            return false
        end
        local active = S.getStateVal("playerData", pid, "hud", "rollData", "active")
        if active == nil or active.phase ~= C.RollPhase.PRE_ROLL then
            print("[rollE2eAddPoolKindSpawn] FAIL: no PRE_ROLL active roll for " .. color)
            return false
        end
        local pool = DK.normalizePool(active.pool)
        local current = pool[kind] or 0
        local needed = count - current
        if needed < 0 then
            -- Rouse-family pool counts decrement via bag remove hook only (mirror right-click).
            for _ = 1, -needed do
                Global.call("GlobalRemoveDieFromBag", { color = color, dieKind = kind })
            end
        elseif needed > 0 then
            for _ = 1, needed do
                Global.call("GlobalSpawnDieFromBag", { color = color, dieKind = kind })
            end
        end
    else
        if RC.setPoolKindCount(color, kind, count) ~= true then
            print("[rollE2eAddPoolKindSpawn] FAIL: RC.setPoolKindCount failed for " .. color)
            return false
        end
        Global.call("GlobalSpawnActivePoolDiceForActive", rollE2eSpawnActivePoolParams(color))
    end

    local pid = S.getPlayerID(color)
    local active = pid and S.getStateVal("playerData", pid, "hud", "rollData", "active")
    local pool = type(active) == "table" and DK.normalizePool(active.pool) or {}
    local got = pool[kind] or 0
    if got ~= count then
        print(string.format(
            "[rollE2eAddPoolKindSpawn] FAIL — %s: pool %s=%d want=%d (bag spawn hook mismatch?)",
            color, kind, got, count))
        return false
    end
    local stagedMismatches = e2ePoolStagedMismatchLines(color, pool)
    if #stagedMismatches > 0 then
        print("[rollE2eAddPoolKindSpawn] FAIL — staged dice check for " .. color)
        for i, line in ipairs(stagedMismatches) do
            print(string.format("  %d) %s", i, line))
        end
        return false
    end

    print(string.format("[rollE2eAddPoolKindSpawn] %s: %s=%d spawned", color, kind, count))
    return true
end

--- Pinned broadcast context from the most recent successful `rollForceConfirm` on one seat.
-- Used to assert against the confirmed roll without cross-seat history scans.
local e2eLastBroadcastContext = nil

--- Last history entry for one seat (post-confirm broadcast source of truth).
-- @param color string
-- @return table|nil entry
local function rollE2eHistoryTailForColor(color)
    if type(color) ~= "string" or color == "" then
        return nil
    end
    local pid = S.getPlayerID(color)
    if pid == nil then
        return nil
    end
    local history = S.getStateVal("playerData", pid, "hud", "rollData", "history") or {}
    return history[#history]
end

--- Resolve seat color for a broadcast assertion (never scans other seats).
-- @param expected table rollE2eExpectBroadcast args
-- @return string|nil color
-- @return string|nil errorMessage
local function rollE2eResolveBroadcastColor(expected)
    local color = expected.color
    if type(color) ~= "string" or color == "" then
        if e2eLastBroadcastContext ~= nil and type(e2eLastBroadcastContext.color) == "string" then
            color = e2eLastBroadcastContext.color
        end
    end
    if type(color) ~= "string" or color == "" then
        return nil, "no seat color (pass expected.color or call rollForceConfirm first)"
    end
    return color, nil
end

--- Resolve which history entry backs a broadcast assertion for one seat only.
-- @param expected table|nil rollE2eExpectBroadcast args
-- @return table|nil historyEntry
-- @return string|nil color
-- @return string|nil errorMessage
local function rollE2eResolveBroadcastHistory(expected)
    expected = type(expected) == "table" and expected or {}
    local color, colorErr = rollE2eResolveBroadcastColor(expected)
    if color == nil then
        return nil, nil, colorErr
    end

    local tail = rollE2eHistoryTailForColor(color)
    if e2eLastBroadcastContext ~= nil
        and e2eLastBroadcastContext.color == color
        and type(e2eLastBroadcastContext.entry) == "table" then
        local pinned = e2eLastBroadcastContext.entry
        if type(tail) == "table"
            and type(pinned.timestamp) == "number"
            and tail.timestamp == pinned.timestamp then
            return pinned, color, nil
        end
    end
    return tail, color, nil
end

--- Parse signed margin from rollRes_resultDisplay text (headline + optional margin).
-- @param resultText string
-- @return number|nil
local function rollE2eParseMarginFromResultDisplay(resultText)
    if type(resultText) ~= "string" or resultText == "" then
        return nil
    end
    local signed = resultText:match(" ([+%−][%d]+)$")
    if signed == nil then
        signed = resultText:match(" ([+%-][%d]+)$")
    end
    if signed == nil then
        return nil
    end
    local normalized = signed:gsub("−", "-")
    return tonumber(normalized)
end

--- True when rollRes_resultDisplay has no trailing signed margin.
-- @param resultText string
-- @return boolean
local function rollE2eResultDisplayMarginAbsent(resultText)
    return rollE2eParseMarginFromResultDisplay(resultText) == nil
end

--- Parse integer success count from rollRes_successes panel text.
-- @param succText string
-- @return number|nil
local function rollE2eParseSuccessesFromUiText(succText)
    if type(succText) ~= "string" then
        return nil
    end
    local digits = succText:match("^(%d+)")
    if digits == nil then
        return nil
    end
    return tonumber(digits)
end

--- True when UI successes text matches expected count (exact or parsed).
-- @param succText string
-- @param expectedSuccesses number
-- @return boolean
local function rollE2eUiSuccessesMatch(succText, expectedSuccesses)
    if tonumber(succText) == expectedSuccesses then
        return true
    end
    local wantSucc = string.format("%d success", expectedSuccesses)
    local wantSucces = string.format("%d successes", expectedSuccesses)
    if succText == wantSucc or succText == wantSucces then
        return true
    end
    return rollE2eParseSuccessesFromUiText(succText) == expectedSuccesses
end

--- True when broadcast panel content was populated (active attribute may lag in same frame).
-- @return boolean
local function rollE2eBroadcastPanelHasContent()
    local resultText = tostring(UI.getValue("rollRes_resultDisplay") or "")
    local introText = tostring(UI.getValue("rollRes_rollIntro") or "")
    return resultText ~= "" or introText ~= ""
end

--- True when the shared roll result broadcast panel is visible.
-- @return boolean
local function rollE2eBroadcastPanelVisible()
    local activeAttr = UI.getAttribute("rollRes_panel", "active")
    if string.lower(tostring(activeAttr or "")) == "true" then
        return true
    end
    return rollE2eBroadcastPanelHasContent()
end

--- Maps E2E `resultClass` shorthands (e.g. "Win") to panel labels (`C.ResultClassLabel`).
-- @param expected string
-- @return boolean matches classText
local function rollE2eBroadcastClassMatches(classText, expected)
    if type(expected) ~= "string" or expected == "" then
        return false
    end
    local hay = string.lower(classText)
    local needles = { string.lower(expected) }
    local norm = string.lower(expected):gsub("%s+", "")
    local shorthandToKey = {
        win = C.ResultClass.WIN,
        success = C.ResultClass.WIN,
        failure = C.ResultClass.FAILURE,
        fail = C.ResultClass.FAILURE,
        criticalwin = C.ResultClass.CRITICAL_WIN,
        messycritical = C.ResultClass.MESSY_CRITICAL,
        bestialfailure = C.ResultClass.BESTIAL_FAILURE,
        totalfailure = C.ResultClass.TOTAL_FAILURE,
        totalbestialfailure = C.ResultClass.TOTAL_BESTIAL_FAILURE,
    }
    local rcKey = shorthandToKey[norm]
    if rcKey ~= nil and type(C.ResultClassLabel[rcKey]) == "string" then
        needles[#needles + 1] = string.lower(C.ResultClassLabel[rcKey])
    end
    for _, needle in ipairs(needles) do
        if needle ~= "" and string.find(hay, needle, 1, true) ~= nil then
            return true
        end
    end
    return false
end

--- True when resolved history result class matches E2E shorthand (Win, Failure, …).
-- @param result table|nil
-- @param expectedClass string
-- @return boolean
local function rollE2eHistoryResultClassMatches(result, expectedClass)
    if type(result) ~= "table" or type(result.resultClass) ~= "string" then
        return false
    end
    local label = C.ResultClassLabel[result.resultClass] or result.resultClass
    return rollE2eBroadcastClassMatches(label, expectedClass)
end

--- True when roll history tail matches all field constraints in `expected`.
-- @param expected table rollE2eExpectBroadcast args
-- @param histResult table|nil
-- @return boolean
local function rollE2eHistorySatisfiesBroadcastFields(expected, histResult)
    if type(histResult) ~= "table" then
        return false
    end
    if type(expected.resultClass) == "string" then
        if not rollE2eHistoryResultClassMatches(histResult, expected.resultClass) then
            return false
        end
    end
    if type(expected.successes) == "number" and histResult.successes ~= expected.successes then
        return false
    end
    if type(expected.margin) == "number" and histResult.margin ~= expected.margin then
        return false
    end
    if expected.marginAbsent == true and histResult.margin ~= nil then
        return false
    end
    return true
end

--- History tail proves broadcast occurred (visibility + per-field UI lag fallback).
-- @param expected table rollE2eExpectBroadcast args
-- @param histResult table|nil
-- @return boolean
local function rollE2eHistorySatisfiesBroadcastExpected(expected, histResult)
    if type(expected.resultClass) == "string"
        or type(expected.successes) == "number"
        or type(expected.margin) == "number"
        or expected.marginAbsent == true then
        return rollE2eHistorySatisfiesBroadcastFields(expected, histResult)
    end
    if expected.visible == true then
        return type(histResult) == "table"
    end
    return false
end

--- E2E: assert `rollRes_*` broadcast UI after `rollForceConfirm` / panel Confirm.
-- @param expected table|nil  `{ color = "Brown", visible = true, resultClass = "Failure", successes = 2, margin = -1 }`
--   `color` — seat color; required unless the prior step called `rollForceConfirm` on that seat
--   `visible` — when true, or when any field below is set, panel must be active or history must match
--   `resultClass` — substring match (case-insensitive) on `rollRes_resultDisplay`; E2E shorthands
--     ("Win", "Failure", …) also match `C.ResultClassLabel` panel text (e.g. Win → WIN)
--   `successes` — integer N matches `rollRes_successes` (number only) or history
--   `margin` — integer matches trailing signed margin on `rollRes_resultDisplay`
--   `marginAbsent` — when true, `rollRes_resultDisplay` must not end with a signed margin
-- UI `getValue` can lag `setValue` by a frame; call from the RunSequence step *after*
-- `rollForceConfirm` (default inter-step wait). Field checks run even when the panel is not
-- visible yet; matching resolved history tail for the same seat satisfies visibility and each field.
-- Pinned context from `rollForceConfirm` is consumed only on PASS so a failed step can retry
-- without passing `color` again.
-- @return boolean pass
-- @usage lua rollE2eExpectBroadcast({ color = "Brown", visible = true, resultClass = "Failure", successes = 2 })
function DEBUG.rollE2eExpectBroadcast(expected)
    if type(expected) ~= "table" then
        print("[rollE2eExpectBroadcast] FAIL: usage rollE2eExpectBroadcast({ color = \"Brown\", visible = true, ... })")
        return false
    end

    local failures = {}
    local histEntry, histColor, histErr = rollE2eResolveBroadcastHistory(expected)
    if histErr ~= nil then
        failures[#failures + 1] = histErr
    end
    local histResult = type(histEntry) == "table" and histEntry.result or nil
    local panelVisible = rollE2eBroadcastPanelVisible()
    local historySatisfies = rollE2eHistorySatisfiesBroadcastExpected(expected, histResult)
    local broadcastEvident = panelVisible or historySatisfies
    local wantsContent = type(expected.resultClass) == "string"
        or type(expected.successes) == "number"
        or type(expected.margin) == "number"
        or expected.marginAbsent == true
    local mustBeVisible = expected.visible == true
        or (expected.visible == nil and wantsContent)

    if expected.visible == false then
        if panelVisible then
            failures[#failures + 1] = "rollRes_panel: visible but expected hidden"
        end
        if wantsContent then
            failures[#failures + 1] = "rollRes_panel: field assertions require visible panel"
        end
    elseif mustBeVisible and not broadcastEvident then
        failures[#failures + 1] = "rollRes_panel: not visible (no matching history tail)"
    end

    if mustBeVisible then
        local resultText = panelVisible and tostring(UI.getValue("rollRes_resultDisplay") or "") or ""
        if type(expected.resultClass) == "string" then
            if not rollE2eBroadcastClassMatches(resultText, expected.resultClass)
                and not rollE2eHistoryResultClassMatches(histResult, expected.resultClass) then
                failures[#failures + 1] = "rollRes_resultDisplay: got \""
                    .. resultText .. "\", want contains \"" .. expected.resultClass .. "\""
            end
        end

        if type(expected.successes) == "number" then
            local succText = tostring(UI.getValue("rollRes_successes") or "")
            local uiOk = rollE2eUiSuccessesMatch(succText, expected.successes)
            local histOk = type(histResult) == "table" and histResult.successes == expected.successes
            if not uiOk and not histOk then
                local histNote = ""
                if type(histResult) == "table" and type(histResult.successes) == "number" then
                    histNote = string.format("; history(%s)=%d", tostring(histColor), histResult.successes)
                end
                failures[#failures + 1] = "rollRes_successes: got \""
                    .. succText .. "\", want number \"" .. tostring(expected.successes) .. "\"" .. histNote
            end
        end

        if type(expected.margin) == "number" then
            local margFromDisplay = rollE2eParseMarginFromResultDisplay(resultText)
            local histOk = type(histResult) == "table" and histResult.margin == expected.margin
            if margFromDisplay ~= expected.margin and not histOk then
                failures[#failures + 1] = "rollRes_resultDisplay margin: got "
                    .. tostring(margFromDisplay) .. ", want " .. tostring(expected.margin)
            end
        end

        if expected.marginAbsent == true then
            local marginPresent = not rollE2eResultDisplayMarginAbsent(resultText)
            local histOk = type(histResult) == "table" and histResult.margin == nil
            if marginPresent and not histOk then
                failures[#failures + 1] = "rollRes_resultDisplay: expected no margin suffix, got \""
                    .. resultText .. "\""
            end
        end
    end

    if #failures == 0 then
        -- Consume pinned context only after a successful assertion so a failed
        -- step can retry without passing color again (same RunSequence block).
        if e2eLastBroadcastContext ~= nil
            and type(histColor) == "string"
            and e2eLastBroadcastContext.color == histColor then
            e2eLastBroadcastContext = nil
        end
        print("[rollE2eExpectBroadcast] PASS")
        return true
    end
    print("[rollE2eExpectBroadcast] FAIL")
    for i, line in ipairs(failures) do
        print(string.format("  %d) %s", i, line))
    end
    return false
end

--- Clears all Storyteller roll drawer slots (live + resolved rows).
local function clearAllStorytellerRollSlots()
    local STR = require("core.storyteller_rolls")
    for slotIndex = 1, 3 do
        STR.cancelSlot(slotIndex)
    end
end

--- Cancels the active roll for a single player. Useful for mid-test cleanup.
-- Destroys staged/released bag dice and table dice for the roll (via RC.cancelRoll).
-- On Black, also clears all ST dashboard slots and drawer dice.
-- @param color string  Player seat color
-- @usage lua rollCancel("Brown")
function DEBUG.rollCancel(color)
    if RC == nil then print("[DEBUG.rollCancel] RC not loaded"); return end
    if type(color) ~= "string" then print("Usage: rollCancel(color)"); return end
    local cancelled = RC.cancelRoll(color)
    if color == "Black" then
        clearAllStorytellerRollSlots()
    end
    if color ~= E2E_RETURN_SEAT then
        rollE2eReturnHostSeat()
    end
    print(string.format("[DEBUG.rollCancel] %s: %s",
        color, cancelled and "roll cancelled" or "no active roll to cancel"))
end

--- E2E console banner width (fixed line length for visual scan in TTS log).
local E2E_PRINT_HEADER_WIDTH = 100
local E2E_PRINT_HEADER_LEAD = 10

--- Padding character for `printHeader` by level: 1 = *, 2 = =, 3 = -.
-- @param level number 1..3
-- @return string|nil
local function e2ePrintHeaderPadChar(level)
    if level == 1 then
        return "*"
    end
    if level == 2 then
        return "="
    end
    if level == 3 then
        return "-"
    end
    return nil
end

--- Active campaign for DEBUG.RunTest / RunTest (console step runner).
local e2eRunTestState = {
    campaign = nil,
    playbook = nil,
    index = 0,
    sequenceCancel = nil,
}

local e2eEndRunTestPrintWatch

--- While a RunTest step runs, intercept print() and abort on case-sensitive "FAIL".
-- failAbortArmed stays false until a level-1 printHeader (10× *) opens a suite so
-- mid-playbook RunTest("Dice", N) steps are not aborted by prerequisite FAIL lines.
local e2eRunTestPrintWatch = {
    active = false,
    failAbortArmed = false,
    originalPrint = nil,
    cancelRegistry = nil,
}

--- Arms RunTest FAIL-abort after a level-1 suite banner (10 leading pad chars).
-- @param line string Printed banner line
local function e2eRunTestMaybeArmFailAbortOnSuiteHeader(line)
    if not e2eRunTestPrintWatch.active or e2eRunTestPrintWatch.failAbortArmed then
        return
    end
    if type(line) ~= "string" then
        return
    end
    local lead = string.rep("*", E2E_PRINT_HEADER_LEAD)
    if string.sub(line, 1, E2E_PRINT_HEADER_LEAD) == lead then
        e2eRunTestPrintWatch.failAbortArmed = true
    end
end

--- Build an E2E section banner and print it to the console.
-- Layout when text fits in 100 chars: 10 pad chars + " " + text + " " + pad fill to 100.
-- When text is too long: 10 pad chars + " " + full text (no trailing pad).
-- @param text string Banner label (use "" for pad-only separator line)
-- @param level number Header level 1..3 (*, =, -)
-- @return string|nil Printed line, or nil on validation error
-- @usage lua printHeader("Suite 0: Cleanup", 1)
-- @usage lua printHeader("", 1)
function DEBUG.printHeader(text, level)
    if type(text) ~= "string" then
        print("[DEBUG.printHeader] FAIL: text must be a string")
        return nil
    end
    if type(level) ~= "number" or level < 1 or level > 3 then
        print("[DEBUG.printHeader] FAIL: level must be 1, 2, or 3")
        return nil
    end

    local padChar = e2ePrintHeaderPadChar(level)
    local lead = string.rep(padChar, E2E_PRINT_HEADER_LEAD)
    local line
    if text == "" then
        line = lead .. string.rep(padChar, E2E_PRINT_HEADER_WIDTH - E2E_PRINT_HEADER_LEAD)
    else
        local mid = " " .. text .. " "
        local used = E2E_PRINT_HEADER_LEAD + #mid
        if used > E2E_PRINT_HEADER_WIDTH then
            line = lead .. " " .. text
        else
            local trailLen = E2E_PRINT_HEADER_WIDTH - used
            line = lead .. mid .. string.rep(padChar, trailLen)
        end
    end
    print(line)
    if level == 1 then
        e2eRunTestMaybeArmFailAbortOnSuiteHeader(line)
    end
    return line
end

--- @return string
local function e2eFormatPrintArgs(...)
    local n = select("#", ...)
    if n == 0 then
        return ""
    end
    local parts = {}
    for i = 1, n do
        parts[i] = tostring(select(i, ...))
    end
    return table.concat(parts, "\t")
end

local function e2eRunTestPrintWatchHandler(...)
    local original = e2eRunTestPrintWatch.originalPrint
    local msg = e2eFormatPrintArgs(...)
    if original ~= nil then
        original(...)
    end
    if e2eRunTestPrintWatch.active
        and e2eRunTestPrintWatch.failAbortArmed
        and msg:find("FAIL", 1, true) ~= nil
    then
        local cancel = e2eRunTestPrintWatch.cancelRegistry
        if type(cancel) == "table" then
            cancel.cancelled = true
            cancel.reason = "fail_detected"
        end
    end
end

e2eEndRunTestPrintWatch = function()
    if e2eRunTestPrintWatch.active and e2eRunTestPrintWatch.originalPrint ~= nil then
        print = e2eRunTestPrintWatch.originalPrint
    end
    e2eRunTestPrintWatch.active = false
    e2eRunTestPrintWatch.failAbortArmed = false
    e2eRunTestPrintWatch.originalPrint = nil
    e2eRunTestPrintWatch.cancelRegistry = nil
end

--- @param cancelRegistry table|nil
local function e2eBeginRunTestPrintWatch(cancelRegistry)
    e2eEndRunTestPrintWatch()
    e2eRunTestPrintWatch.originalPrint = print
    e2eRunTestPrintWatch.cancelRegistry = cancelRegistry
    e2eRunTestPrintWatch.failAbortArmed = false
    e2eRunTestPrintWatch.active = true
    print = e2eRunTestPrintWatchHandler
end

--- Cancel any in-flight RunSequence from a prior RunTest() (re-init or next step).
-- Allocates a fresh cancelRegistry so stale cancelled flags do not abort the new step.
local function e2eCancelActiveRunTestSequence()
    e2eEndRunTestPrintWatch()
    local cancel = e2eRunTestState.sequenceCancel
    if type(cancel) == "table" then
        cancel.cancelled = true
        -- Preserve fail_detected for a prior step's onComplete closure.
        if cancel.reason ~= "fail_detected" then
            cancel.reason = "reinitialized"
        end
    end
    e2eRunTestState.sequenceCancel = { cancelled = false, reason = nil }
end

--- Shallow copy so U.RunSequence (U.shift) does not drain the embedded playbook step table.
-- @param sequence table
-- @return table
local function e2eCopyRunSequenceStep(sequence)
    local copy = {}
    for i, fn in ipairs(sequence) do
        copy[i] = fn
    end
    return copy
end

--- Normalize top-level suite id (0, A–P, E2). Rejects substeps (H1, K2a, …).
-- @param suiteId string|nil
-- @return string|nil
local function normalizeE2eSuiteId(suiteId)
    if type(suiteId) ~= "string" then
        return nil
    end
    local trimmed = suiteId:match("^%s*(.-)%s*$")
    if trimmed == "" then
        return nil
    end
    local upper = string.upper(trimmed)
    if upper == "0" then
        return "0"
    end
    if upper == "E2" then
        return "E2"
    end
    if #upper == 1 and upper:match("[A-P]") then
        return upper
    end
    return nil
end

--- Normalize playbook campaign name (Dice, Scenes, Gameboard).
-- @param name string|nil
-- @return string|nil
local function normalizeE2eCampaignName(name)
    if type(name) ~= "string" then
        return nil
    end
    local trimmed = name:match("^%s*(.-)%s*$")
    if trimmed == "" then
        return nil
    end
    local lower = string.lower(trimmed)
    if lower == "dice" then
        return "Dice"
    end
    if lower == "scenes" then
        return "Scenes"
    end
    if lower == "gameboard" then
        return "Gameboard"
    end
    return nil
end

--- Load generated playbook module for a prepared campaign.
-- @param campaign string Dice | Scenes | Gameboard
-- @return table|nil playbook
-- @return string|nil err
local function e2eLoadPlaybookModule(campaign)
    if campaign == "Dice" then
        -- Generated by npm run e2e-playbook:generate (committed lib/e2e_playbook_dice.ttslua).
        -- Direct require: missing module or parse error must fail loud, not be masked.
        local mod = require("lib.e2e_playbook_dice")
        if type(mod) ~= "table" or type(mod.steps) ~= "table" or type(mod.stepCount) ~= "number" then
            return nil, "lib.e2e_playbook_dice is missing steps/stepCount — regenerate playbook Lua"
        end
        return mod, nil
    end
    return nil, "not yet prepared"
end

--- Resolve 1-based step index or top-level suite id for optional RunTest jump.
-- @param playbook table
-- @param startRaw number|string|nil  e.g. 8, "8", "H", "E2"
-- @return number|nil index offset (0 = start at step 1); nil on error (message printed)
local function e2eResolveStartIndex(playbook, startRaw)
    if startRaw == nil then
        return 0
    end

    local stepNum = tonumber(startRaw)
    if stepNum ~= nil and stepNum == math.floor(stepNum) and stepNum >= 1 then
        if stepNum > playbook.stepCount then
            print(string.format(
                "[RunTest] Start step %d exceeds stepCount %d",
                stepNum,
                playbook.stepCount
            ))
            return nil
        end
        return stepNum - 1
    end

    if type(startRaw) == "string" then
        local sid = normalizeE2eSuiteId(startRaw)
        if sid ~= nil then
            if type(playbook.suiteSteps) ~= "table" then
                print("[RunTest] Playbook missing suiteSteps — run npm run e2e-playbook:generate && Save & Play")
                return nil
            end
            local suiteStep = playbook.suiteSteps[sid]
            if type(suiteStep) == "number" then
                return suiteStep - 1
            end
            local known = ""
            if type(playbook.suiteIds) == "table" and #playbook.suiteIds > 0 then
                known = table.concat(playbook.suiteIds, ", ")
            end
            print(string.format("[RunTest] Suite '%s' not found. Top-level suites: %s", sid, known))
            return nil
        end
    end

    print(string.format(
        "[RunTest] Invalid start '%s' (use step 1..%d or suite 0, A–P, E2)",
        tostring(startRaw),
        playbook.stepCount
    ))
    return nil
end

--- Run the next E2E playbook step, or initialize a campaign.
-- With a campaign name: cancel any in-flight step, reset index, arm the playbook (does not run a step).
-- Optional second arg: 1-based step index (8) or top-level suite id ("H", "E2") before the first RunTest().
-- With no args: advance index by one and execute the next U.RunSequence from the armed campaign.
-- @param campaignName string|nil "Dice" | "Scenes" | "Gameboard"
-- @param startAt number|string|nil Step index or suite id when initializing
-- @usage lua RunTest("Dice")
-- @usage lua RunTest("Dice", 8)
-- @usage lua RunTest("Dice", "H")
-- @usage lua RunTest()
function DEBUG.RunTest(campaignName, startAt)
    if campaignName ~= nil then
        local key = normalizeE2eCampaignName(campaignName)
        if key == nil then
            print(string.format(
                "[RunTest] Unknown campaign '%s'. Known: Dice, Scenes, Gameboard",
                tostring(campaignName)
            ))
            return false
        end

        if key == "Scenes" or key == "Gameboard" then
            print(string.format("[RunTest] Campaign '%s' is not yet prepared.", key))
            return false
        end

        local playbook, err = e2eLoadPlaybookModule(key)
        if playbook == nil then
            print("[RunTest] " .. tostring(err))
            return false
        end

        local startIndex = e2eResolveStartIndex(playbook, startAt)
        if startIndex == nil then
            return false
        end

        e2eCancelActiveRunTestSequence()

        e2eRunTestState.campaign = key
        e2eRunTestState.playbook = playbook
        e2eRunTestState.index = startIndex
        if startAt ~= nil then
            print(string.format(
                "[RunTest] Initialized '%s' at step %d/%d (next RunTest runs this step)",
                key,
                startIndex + 1,
                playbook.stepCount
            ))
        else
            print(string.format(
                "[RunTest] Initialized '%s' (next RunTest runs step 1/%d)",
                key,
                playbook.stepCount
            ))
        end
        return true
    end

    if e2eRunTestState.playbook == nil then
        return false
    end

    local nextIndex = e2eRunTestState.index + 1
    local playbook = e2eRunTestState.playbook
    if nextIndex > playbook.stepCount then
        print(string.format("[RunTest] %s finished (%d steps)", e2eRunTestState.campaign, playbook.stepCount))
        return false
    end

    local steps = playbook.steps
    local sequence = steps[nextIndex]
    if type(sequence) ~= "table" or #sequence == 0 then
        print(string.format(
            "[RunTest] FAIL: step %d has no runnable functions — re-arm with RunTest(\"%s\")",
            nextIndex,
            e2eRunTestState.campaign
        ))
        return false
    end

    e2eRunTestState.index = nextIndex
    print(string.format(
        "[RunTest] %s step %d/%d",
        e2eRunTestState.campaign,
        nextIndex,
        playbook.stepCount
    ))
    -- Fresh cancelRegistry per step: prior FAIL sets cancelled=true on the shared table.
    e2eCancelActiveRunTestSequence()
    local sequenceCopy = e2eCopyRunSequenceStep(sequence)
    local cancelRegistry = e2eRunTestState.sequenceCancel
    e2eBeginRunTestPrintWatch(cancelRegistry)
    U.RunSequenceWithOptions(sequenceCopy, {
        cancelRegistry = cancelRegistry,
        onComplete = function(ok, detail)
            local failDetected = type(cancelRegistry) == "table"
                and cancelRegistry.reason == "fail_detected"
            e2eEndRunTestPrintWatch()
            if failDetected then
                local stepCount = playbook.stepCount
                print(string.format(
                    "[RunTest] Stopped at step %d/%d: FAIL detected in output",
                    nextIndex,
                    stepCount
                ))
            end
        end,
    })
    return true
end

--- Cancels all active rolls across every player color. Quick full reset between tests.
-- @usage lua rollCancelAll()
function DEBUG.rollCancelAll()
    if RC == nil then print("[DEBUG.rollCancelAll] RC not loaded"); return end
    local count = 0
    for _, color in ipairs(C.PlayerColors) do
        if RC.cancelRoll(color) then count = count + 1 end
    end
    if RC.cancelRoll("Black") then count = count + 1 end
    clearAllStorytellerRollSlots()
    rollE2eReturnHostSeat()
    print(string.format("[DEBUG.rollCancelAll] Cancelled %d active roll(s).", count))
end

--- Initiates a Storyteller NPC roll on Black (uses drawer slot 1..3).
-- @param label string|nil
-- @param rollType string|nil
-- @usage lua rollStTest("Evangeline Dupont")
-- @usage lua rollStTest("Garou", C.RollType.WEREWOLF)
function DEBUG.rollStTest(label, rollType)
    if rollE2eSeatPrepInternal("Black") ~= true then
        print("[DEBUG.rollStTest] E2E seat prep failed — aborting.")
        return
    end
    local STR = require("core.storyteller_rolls")
    local lbl = (type(label) == "string" and label ~= "") and label or "Debug NPC"
    local ok = STR.initiateNpcRoll(lbl, rollType)
    print(string.format("[DEBUG.rollStTest] initiate: %s", ok and "ok" or "blocked"))
end

--- Prints gameState.storytellerRolls slots.
-- @usage lua rollStSlots()
function DEBUG.rollStSlots()
    local root = S.getStateVal("storytellerRolls") or {}
    print("[DEBUG.rollStSlots] " .. JSON.encode_pretty(root))
end

--- Force-confirms the active roll (clicks CONFIRM). Use after POST_ROLL settle/recalc.
-- @param color string  Player seat color
-- @usage lua rollForceConfirm("Brown")
function DEBUG.rollForceConfirm(color)
    if RC == nil then print("[DEBUG.rollForceConfirm] RC not loaded"); return end
    if type(color) ~= "string" then print("Usage: rollForceConfirm(color)"); return end
    local ok = RC.confirmRoll(color)
    if ok == true then
        e2eLastBroadcastContext = {
            color = color,
            entry = rollE2eHistoryTailForColor(color),
        }
    end
    print(string.format("[DEBUG.rollForceConfirm] %s: %s",
        color, ok and "roll confirmed and resolved" or "confirm failed — is the roll in POST_ROLL?"))
end

-- Uses U.rotateAroundPoint to keep angle conventions consistent.
-- @param obj Object|table|userdata TTS object to move (GameObject)
-- @param distance number Distance from center in world units
-- @param angle number World angle in degrees (0 = +Z, 90 = +X, 180 = -Z, 270 = -X)
-- @param center Vector|table Optional center position (default: table center at object's current Y)
-- @usage lua DEBUG.tableRotateTo(obj, 30, 180)
function DEBUG.tableRotateTo(obj, distance, angle, center)
    local resolvedObj = U.getObject(obj, true) or obj
    U.Val("DEBUG.tableRotateTo", resolvedObj, U.isGameObject(resolvedObj), "obj must be a valid GameObject or GUID")
    U.Val("DEBUG.tableRotateTo", distance, U.Type(distance) == "number", "distance must be a number")
    U.Val("DEBUG.tableRotateTo", angle, U.Type(angle) == "number", "angle must be a number")

    local objPos = resolvedObj.getPosition()
    local centerPos
    if center ~= nil then
        centerPos = Vector(center)
    else
        centerPos = Vector(0, objPos.y, 0)
    end

    local targetPos = U.rotateAroundPoint(centerPos, distance, angle, centerPos.y)
    resolvedObj.setPosition(targetPos)
    print("DEBUG.tableRotateTo: Moved " .. resolvedObj.getName() .. " to " .. tostring(targetPos))
end

-- ============================================================================
-- Look-at calibration + 3D positioning helpers (console/admin use)
-- ============================================================================

-- Vars stored on each object to correct its default forward direction
-- when rotation is not adjusted by a look-at function.
local LOOKAT_VAR_PITCH = "lookAtOffsetPitchDeg"
local LOOKAT_VAR_YAW = "lookAtOffsetYawDeg"
local LOOKAT_VAR_ROLL = "lookAtOffsetRollDeg"

--- Resolves a TTS GameObject from either a GUID string or an object reference.
-- @param objOrGuid Object|string TTS object or GUID
-- @param source string Debug call site label for errors
-- @return table|userdata GameObject
local function resolveDebugObject(objOrGuid, source)
    local resolved = U.getObject(objOrGuid, true)
    if resolved ~= nil then
        return resolved
    end
    if U.isGameObject(objOrGuid) then
        return objOrGuid
    end
    U.error(source, "objOrGuid must be a valid GameObject or GUID string", objOrGuid)
    return nil -- U.error throws; this return is for completeness
end

--- Safely reads a numeric `obj.getVar(varName)` value.
-- Returns nil if var missing or not numeric.
-- @param obj userdata GameObject
-- @param varName string Variable name
-- @return number|nil
local function safeGetVarNumber(obj, varName)
    if obj == nil or obj.getVar == nil then return nil end
    local val = obj.getVar(varName)
    if val == nil then return nil end
    if type(val) == "number" then return val end
    if type(val) == "string" then
        return tonumber(val)
    end
    return nil
end

--- Safely writes a numeric `obj.setVar(varName, number)` value.
-- @param obj userdata GameObject
-- @param varName string Variable name
-- @param value number
local function safeSetVarNumber(obj, varName, value)
    if obj == nil or obj.setVar == nil then
        U.error("DEBUG.safeSetVarNumber", "Object does not support setVar()", obj)
        return
    end
    obj.setVar(varName, value)
end

--- Resolves a target position from either:
-- 1) an Object (uses getPosition())
-- 2) a table/Vector {x,y,z}
-- @param targetOrPos Object|table|userdata
-- @param source string Debug call site label
-- @return Vector
local function resolveTargetPosition(targetOrPos, source)
    if targetOrPos == nil then
        U.error(source, "lookAtTargetOrPos cannot be nil")
        return Vector(0, 0, 0)
    end

    -- Accept GUID strings for targets as well.
    -- This keeps setRotationLookAt() consistent with other DEBUG helpers that accept GUIDs.
    if U.Type(targetOrPos) == "string" then
        local targetObj = U.getObject(targetOrPos, true)
        if targetObj ~= nil then
            return targetObj.getPosition()
        end
    end

    if U.isGameObject(targetOrPos) then
        local pos = targetOrPos.getPosition()
        U.Val(source, pos, pos ~= nil, "Target object's getPosition() failed")
        return pos
    end

    -- Accept Vector userdata or a table-like {x,y,z} or {[1],[2],[3]}
    local vec = Vector(targetOrPos)
    if vec == nil then
        U.error(source, "lookAtTargetOrPos must be a GameObject or a {x,y,z} table/Vector", targetOrPos)
    end
    return vec
end

--- Internal helper: Resolves the configured center object (or fallback to table center).
-- If the GUID isn't set or can't be resolved, returns Vector(0,0,0).
-- @return table|userdata Vector|GameObject (GameObject preserves local frame conversions)
local function resolveCenterArg()
    local centerGuid = S.getStateVal("debug", "centerObjGuid")
    if type(centerGuid) == "string" and centerGuid ~= "" then
        local centerObj = U.getObject(centerGuid, true)
        if centerObj ~= nil then
            return centerObj
        end
    end
    return Vector(0, 0, 0)
end

--- Gets the per-object look-at correction offset.
-- If the calibration vars are missing on the object, returns Vector(0,0,0).
-- @param objOrGuid Object|string GameObject or GUID
-- @return Vector
function DEBUG.getLookAtOffset(objOrGuid)
    local obj = resolveDebugObject(objOrGuid, "DEBUG.getLookAtOffset")

    local pitch = safeGetVarNumber(obj, LOOKAT_VAR_PITCH)
    local yaw = safeGetVarNumber(obj, LOOKAT_VAR_YAW)
    local roll = safeGetVarNumber(obj, LOOKAT_VAR_ROLL)

    -- Default when calibration vars are absent.
    if pitch == nil and yaw == nil and roll == nil then
        return Vector(0, 0, 0)
    end

    return Vector(pitch or 0, yaw or 0, roll or 0)
end

--- Applies a look-at rotation, adjusted by the per-object calibration offset (if present).
-- @param objOrGuid Object|string GameObject or GUID
-- @param lookAtTargetOrPos Object|table|Vector target object or position
-- @param useCalibration boolean Optional; default is true
function DEBUG.setRotationLookAt(objOrGuid, lookAtTargetOrPos, useCalibration)
    local obj = resolveDebugObject(objOrGuid, "DEBUG.setRotationLookAt")
    local targetPos = resolveTargetPosition(lookAtTargetOrPos, "DEBUG.setRotationLookAt")

    if obj == nil then
      U.error("DEBUG.setRotationLookAt", "Object is nil", objOrGuid)
      return
    end

    local baseRot = U.lookAtRotation(obj.getPosition(), targetPos)
    if baseRot == nil or baseRot.x == nil or baseRot.y == nil or baseRot.z == nil then
        U.error("DEBUG.setRotationLookAt", "U.lookAtRotation failed", baseRot)
        return
    end

    local offset = Vector(0, 0, 0)
    if useCalibration == nil or useCalibration ~= false then
        offset = DEBUG.getLookAtOffset(obj)
    end
    if offset == nil or offset.x == nil or offset.y == nil or offset.z == nil then
        offset = Vector(0, 0, 0)
    end

    local finalRot = Vector(
        baseRot.x + offset.x,
        baseRot.y + offset.y,
        baseRot.z + offset.z
    )

    if obj.setRotation ~= nil then
        obj.setRotation(finalRot)
    elseif obj.setRotationSmooth ~= nil then
        obj.setRotationSmooth(finalRot, false, false)
    else
        U.error("DEBUG.setRotationLookAt", "Object has neither setRotation nor setRotationSmooth()", obj)
        return
    end

    local objName = (obj.getName and obj.getName()) or "Object"
    print("DEBUG.setRotationLookAt: Set rotation for " .. tostring(objName) .. " to " .. tostring(finalRot) .. " (base=" .. tostring(baseRot) .. ", offset=" .. tostring(offset) .. ")")
end

--- Calibrates per-object look-at offset vars so the object correctly aims at the target.
-- This is meant for admin/setup time.
-- @param objOrGuid Object|string GameObject or GUID
-- @param lookAtTargetOrPos Object|table|Vector target object or position
-- @param roundIntervalDeg number Optional rounding interval in degrees (default: 90)
function DEBUG.calibrateLookAtOffset(objOrGuid, lookAtTargetOrPos, roundIntervalDeg)
    local obj = resolveDebugObject(objOrGuid, "DEBUG.calibrateLookAtOffset")
    local targetPos = resolveTargetPosition(lookAtTargetOrPos, "DEBUG.calibrateLookAtOffset")

    local interval = roundIntervalDeg
    if interval == nil then interval = 90 end
    U.Val("DEBUG.calibrateLookAtOffset", interval, U.Type(interval) == "number" and interval > 0, "roundIntervalDeg must be a positive number")

    if obj == nil then
      U.error("DEBUG.calibrateLookAtOffset", "Object is nil", objOrGuid)
      return
    end

    local baseRot = U.lookAtRotation(obj.getPosition(), targetPos)
    local currentRot = obj.getRotation()

    if baseRot == nil or baseRot.x == nil or baseRot.y == nil or baseRot.z == nil then
        U.error("DEBUG.calibrateLookAtOffset", "U.lookAtRotation returned nil/invalid Vector", baseRot)
        return
    end
    if currentRot == nil or currentRot.x == nil or currentRot.y == nil or currentRot.z == nil then
        U.error("DEBUG.calibrateLookAtOffset", "obj.getRotation() returned nil/invalid Vector", currentRot)
        return
    end

    local baseRounded = Vector(
        U.pAngle(baseRot.x, interval),
        U.pAngle(baseRot.y, interval),
        U.pAngle(baseRot.z, interval)
    )
    local currentRounded = Vector(
        U.pAngle(currentRot.x, interval),
        U.pAngle(currentRot.y, interval),
        U.pAngle(currentRot.z, interval)
    )

    local offset = Vector(
        currentRounded.x - baseRounded.x,
        currentRounded.y - baseRounded.y,
        currentRounded.z - baseRounded.z
    )

    safeSetVarNumber(obj, LOOKAT_VAR_PITCH, offset.x)
    safeSetVarNumber(obj, LOOKAT_VAR_YAW, offset.y)
    safeSetVarNumber(obj, LOOKAT_VAR_ROLL, offset.z)

    print(
        "DEBUG.calibrateLookAtOffset: Stored offset " ..
        tostring(offset) ..
        " (baseRounded=" .. tostring(baseRounded) .. ", currentRounded=" .. tostring(currentRounded) .. ")"
    )
end

--- Sets the center object used for coordinate conversions in the positioning helpers.
-- Stores the object GUID in game state (session-oriented; save whitelist may exclude it).
-- @param objOrGuid Object|string GameObject or GUID string
function DEBUG.setCenterObject(objOrGuid)
    local obj = resolveDebugObject(objOrGuid, "DEBUG.setCenterObject")

    if obj == nil then
      U.error("DEBUG.setCenterObject", "Object is nil", objOrGuid)
      return
    end

    local guid = nil
    if obj.getGUID ~= nil then
        guid = obj.getGUID()
    end
    if guid == nil then
        guid = obj.guid
    end
    if guid == nil or U.Type(guid) ~= "string" then
        U.error("DEBUG.setCenterObject", "Object GUID could not be resolved", guid)
        return
    end

    S.setStateVal(guid, "debug", "centerObjGuid")
    print("DEBUG.setCenterObject: Stored center GUID = " .. tostring(guid))
end

--- Clears the stored center object GUID used for coordinate conversions.
function DEBUG.clearCenterObject()
    S.setStateVal(nil, "debug", "centerObjGuid")
    print("DEBUG.clearCenterObject: Cleared stored center GUID")
end

--- Positions an object in cylindrical coordinates relative to the stored center.
-- If `lookAtTargetOrPos` is provided, also applies a calibration-aware look-at rotation.
function DEBUG.setPositionCylindrical(objOrGuid, radius, angleDeg, height, lookAtTargetOrPos)
    local obj = resolveDebugObject(objOrGuid, "DEBUG.setPositionCylindrical")
    U.Val("DEBUG.setPositionCylindrical", radius, U.Type(radius) == "number", "radius must be a number")
    U.Val("DEBUG.setPositionCylindrical", angleDeg, U.Type(angleDeg) == "number", "angleDeg must be a number")
    U.Val("DEBUG.setPositionCylindrical", height, U.Type(height) == "number", "height must be a number")

    local centerArg = resolveCenterArg()
    local xyz = U.resolvePositionData(
        { position = { radius = radius, angle = angleDeg, height = height } },
        "cylindrical",
        centerArg
    )
    if xyz == nil then
        U.error("DEBUG.setPositionCylindrical", "Failed to resolve cylindrical coordinates to XYZ")
        return
    end

    if obj == nil then
        U.error("DEBUG.setPositionCylindrical", "Object is nil", objOrGuid)
        return
    end

    if obj.setPosition == nil then
        U.error("DEBUG.setPositionCylindrical", "Object does not support setPosition()", obj)
        return
    end

    obj.setPosition(xyz)

    if lookAtTargetOrPos ~= nil then
        DEBUG.setRotationLookAt(obj, lookAtTargetOrPos, true)
    end

    local objName = (obj.getName and obj.getName()) or "Object"
    print("DEBUG.setPositionCylindrical: Moved " .. tostring(objName) .. " to " .. tostring(xyz))
end

--- Positions an object in spherical coordinates relative to the stored center.
-- If `lookAtTargetOrPos` is provided, also applies a calibration-aware look-at rotation.
function DEBUG.setPositionSpherical(objOrGuid, radius, angleDeg, angle2Deg, lookAtTargetOrPos)
    local obj = resolveDebugObject(objOrGuid, "DEBUG.setPositionSpherical")
    U.Val("DEBUG.setPositionSpherical", radius, U.Type(radius) == "number", "radius must be a number")
    U.Val("DEBUG.setPositionSpherical", angleDeg, U.Type(angleDeg) == "number", "angleDeg must be a number")
    U.Val("DEBUG.setPositionSpherical", angle2Deg, U.Type(angle2Deg) == "number", "angle2Deg must be a number")

    local centerArg = resolveCenterArg()
    local xyz = U.resolvePositionData(
        { position = { radius = radius, angle = angleDeg, angle2 = angle2Deg } },
        "spherical",
        centerArg
    )
    if xyz == nil then
        U.error("DEBUG.setPositionSpherical", "Failed to resolve spherical coordinates to XYZ")
        return
    end

    if obj == nil then
        U.error("DEBUG.setPositionSpherical", "Object is nil", objOrGuid)
        return
    end

    if obj.setPosition == nil then
        U.error("DEBUG.setPositionSpherical", "Object does not support setPosition()", obj)
        return
    end

    obj.setPosition(xyz)

    if lookAtTargetOrPos ~= nil then
        DEBUG.setRotationLookAt(obj, lookAtTargetOrPos, true)
    end

    local objName = (obj.getName and obj.getName()) or "Object"
    print("DEBUG.setPositionSpherical: Moved " .. tostring(objName) .. " to " .. tostring(xyz))
end

--[[
    File Logging Functions
    These functions write debug information to files in the workspace using
    the TTS Tools extension's sendExternalMessage API.
]]

--- Logs a message to a file with timestamp
-- @param level string Log level: "INFO", "WARN", "ERROR", "DEBUG"
-- @param message string Message to log
-- @param filename string Optional filename (default: "debug_log")
function DEBUG.logToFile(level, message, filename)
    filename = filename or "debug_log"
    local timestamp = getTimestamp()
    local logEntry = string.format("[%s] [%s] %s\n", timestamp, level, message)

    -- Append to existing cached content
    writeToFile(filename, logEntry, "none", true)
end

--- Logs current game state to a file as JSON
-- @param filename string Optional filename (default: "game_state")
function DEBUG.logStateToFile(filename)
    filename = filename or "game_state"
    local state = S.getGameState()
    local stateJson = JSON.encode_pretty(state)
    local timestamp = getTimestamp()
    local content = string.format("Game State Dump - %s\n%s\n", timestamp, stateJson)

    -- State dumps are overwritten (not appended) - each dump is a complete snapshot
    writeToFile(filename, content, "auto", false)
    print("DEBUG: Game state logged to " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. LOG_DIR .. "/" .. filename .. "." .. LOG_EXTENSION)
end

--- @param lightRef string
--- @param seatColor string
--- @return boolean
local function debugLightRefMatchesSeatColor(lightRef, seatColor)
    if type(lightRef) ~= "string" or type(seatColor) ~= "string" then
        return false
    end
    return string.sub(lightRef, -#seatColor) == seatColor
end

--- @param seatKey string
--- @return boolean
local function debugIsSeatPresentInActiveLayout(seatKey)
    if type(seatKey) ~= "string" then
        return false
    end
    local currentTableKey = S.getStateVal("seatLayout", "currentTableKey")
    if type(currentTableKey) ~= "string" or currentTableKey == "" then
        return true
    end
    local tableCfg = C.Tables[currentTableKey]
    if type(tableCfg) ~= "table" then
        return true
    end
    ---@diagnostic disable-next-line: undefined-field
    local map = tableCfg.seatToPositionMap
    if type(map) ~= "table" then
        return true
    end
    return map[seatKey] ~= nil
end

--- @param seatKey string
--- @return boolean
local function debugIsPlayerSeatKey(seatKey)
    return type(seatKey) == "string" and U.isIn(seatKey, C.PlayerColors or {})
end

--- @param seatKey string
--- @param lData table
--- @return string|nil
local function debugComputeDesiredSeatLightMode(seatKey, lData)
    if type(seatKey) ~= "string" or type(lData) ~= "table" then
        return nil
    end

    local isPlayerSeat = debugIsPlayerSeatKey(seatKey)
    if isPlayerSeat then
        local playerID = S.getPlayerID(seatKey)
        local isRolling = playerID ~= nil and S.getStateVal("playerData", playerID, "lighting", "isRolling") == true
        if isRolling and lData.ROLLING ~= nil then
            return "ROLLING"
        end
    end

    local currentScene = Scenes.getCurrentScene and Scenes.getCurrentScene() or S.getStateVal("currentScene")
    local isDark = type(currentScene) == "string" and string.lower(currentScene) == "dark"
    local seatPresent = debugIsSeatPresentInActiveLayout(seatKey)
    if (isDark or not seatPresent) and lData.OFF ~= nil then
        return "OFF"
    end

    if isPlayerSeat then
        local hunger = tonumber(S.getPlayerVal(seatKey, "hunger")) or 0
        if hunger >= 4 and lData.HUNGRY ~= nil then
            return "HUNGRY"
        end
    end

    if lData.STANDARD ~= nil then
        return "STANDARD"
    end
    if type(lData.default) == "string" and lData[lData.default] ~= nil then
        return lData.default
    end
    if lData.OFF ~= nil then
        return "OFF"
    end
    return nil
end

--- @return table<string, boolean>
local function debugGetOccupiedNPCSlotSet()
    local occupied = S.getStateVal("seatLayout", "occupiedNPCSlots")
    local out = {}
    if type(occupied) ~= "table" then
        return out
    end
    for _, seatKey in ipairs(C.NPCSeats or {}) do
        local val = occupied[seatKey]
        if val ~= nil and val ~= false then
            out[seatKey] = true
        end
    end
    return out
end

--- Logs a per-seat light diagnostic snapshot to a workspace file.
-- Output includes: color | resolvedPlayerID | hunger | desired vs persisted `lights` per ref | smokeWanted.
-- (`current` column is `S.getStateVal("lights", ...)`, not live Light component intensity.)
-- @param filename string Optional filename (default: "seat_lights")
function DEBUG.logSeatLightsToFile(filename)
    filename = filename or "seat_lights"
    local timestamp = getTimestamp()
    local rows = {
        "Seat Light Diagnostics - " .. tostring(timestamp),
        "seat | seatType | occupant | playerID | hunger | rolling | scene | seatPresent | desired | current | smokeWanted",
    }

    local occupiedNpcSlots = debugGetOccupiedNPCSlotSet()
    local orderedSeats = {}
    for _, seatColor in ipairs(C.PlayerColors or {}) do
        orderedSeats[#orderedSeats + 1] = seatColor
    end
    for _, seatKey in ipairs(C.NPCSeats or {}) do
        if occupiedNpcSlots[seatKey] == true then
            orderedSeats[#orderedSeats + 1] = seatKey
        end
    end

    for _, seatKey in ipairs(orderedSeats) do
        local isPlayerSeat = debugIsPlayerSeatKey(seatKey)
        local seatType = isPlayerSeat and "PLAYER" or "NPC"
        local occupiedRaw = S.getStateVal("seatLayout", "occupiedNPCSlots", seatKey)
        local occupant = isPlayerSeat and seatKey or tostring(occupiedRaw)
        local playerID = isPlayerSeat and S.getPlayerID(seatKey) or nil
        local hunger = isPlayerSeat and (tonumber(S.getPlayerVal(seatKey, "hunger")) or 0) or 0
        local rolling = playerID ~= nil and S.getStateVal("playerData", playerID, "lighting", "isRolling") == true
        local currentScene = Scenes.getCurrentScene and Scenes.getCurrentScene() or S.getStateVal("currentScene")
        local seatPresent = debugIsSeatPresentInActiveLayout(seatKey)
        local smokeWanted = isPlayerSeat and hunger >= 4

        local refs = {}
        for lightRef, lData in pairs(L.LIGHTMODES or {}) do
            if type(lightRef) == "string"
                and type(lData) == "table"
                and debugLightRefMatchesSeatColor(lightRef, seatKey)
            then
                refs[#refs + 1] = lightRef
            end
        end
        table.sort(refs)

        local desiredParts = {}
        local currentParts = {}
        for _, lightRef in ipairs(refs) do
            local lData = L.LIGHTMODES[lightRef]
            local desiredMode = debugComputeDesiredSeatLightMode(seatKey, lData) or "nil"
            local currentMode = tostring(S.getStateVal("lights", lightRef) or "nil")
            desiredParts[#desiredParts + 1] = lightRef .. "=" .. desiredMode
            currentParts[#currentParts + 1] = lightRef .. "=" .. currentMode
        end

        rows[#rows + 1] = string.format(
            "%s | %s | %s | %s | %d | %s | %s | %s | %s | %s | %s",
            tostring(seatKey),
            tostring(seatType),
            tostring(occupant),
            tostring(playerID),
            hunger,
            tostring(rolling),
            tostring(currentScene),
            tostring(seatPresent),
            table.concat(desiredParts, ", "),
            table.concat(currentParts, ", "),
            tostring(smokeWanted)
        )
    end

    local content = table.concat(rows, "\n") .. "\n"
    writeToFile(filename, content, "none", false)
    print("DEBUG: Seat light diagnostics logged to " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. LOG_DIR .. "/" .. filename .. "." .. LOG_EXTENSION)
end

--- Logs Step Zero placement intent plus occupied slots for NPC reconcile verification.
--- After scene edits, run `Sync.full({ force = true })`, then compare world to intent:
--- 1) byArea + seatSlots same char → target area; 2) seat only → target seat + physicalSeatAllowed;
--- 3) seat on table without slot → target preload, assignment retained; 4) assigned + present false → seated layout, hidden props;
--- 5) seat-only scene change → no full pool stash; 6) save load bootstrap → one successful reconcile.
--- @param filename string Optional filename (default: "npc_placement_intent")
function DEBUG.logNpcPlacementIntentToFile(filename)
    filename = filename or "npc_placement_intent"
    local NPCS = require("core.npcs")
    local intent = NPCS.resolveNpcPlacementIntent()
    local rows = {
        "character | target | areaKey | slot | seatKey | pendingSeat",
        "seat | assigned | character | tableHasSlot | present | physicalSeatAllowed | occupiedNPCSlots",
    }
    local byCharacter = intent.byCharacter or {}
    local charKeys = {}
    for name, _ in pairs(byCharacter) do
        charKeys[#charKeys + 1] = name
    end
    table.sort(charKeys)
    for i = 1, #charKeys do
        local name = charKeys[i]
        local ci = byCharacter[name]
        rows[#rows + 1] = table.concat({
            tostring(name),
            tostring(ci and ci.target or ""),
            tostring(ci and ci.areaKey or ""),
            tostring(ci and ci.slotIndex or ""),
            tostring(ci and ci.seatKey or ""),
            tostring(ci and ci.pendingSeatKey or ""),
        }, " | ")
    end
    rows[#rows + 1] = ""
    local occupied = S.getStateVal("seatLayout", "occupiedNPCSlots") or {}
    for _, seatKey in ipairs(C.NPCSeats or {}) do
        local info = (intent.bySeat or {})[seatKey] or {}
        rows[#rows + 1] = table.concat({
            tostring(seatKey),
            tostring(info.assigned),
            tostring(info.assignedCharacter or ""),
            tostring(info.tableHasSlot),
            tostring(info.present),
            tostring(info.physicalSeatAllowed),
            tostring(occupied[seatKey] or ""),
        }, " | ")
    end
    rows[#rows + 1] = "tableKey=" .. tostring(intent.tableKey or "")
    writeToFile(filename, table.concat(rows, "\n") .. "\n", "none", false)
    print(
        "DEBUG: NPC placement intent logged to "
            .. WORKSPACE_DEBUG_WRITE_ROOT
            .. "/"
            .. LOG_DIR
            .. "/"
            .. filename
            .. "."
            .. LOG_EXTENSION
    )
end

--- Display name for export rows (Name only; Nickname is not a TTS Object API method).
--- @param obj Object|nil
--- @return string
local function exportObjectDisplayName(obj)
    if not U.isGameObject(obj) then
        return ""
    end
    if obj.getName ~= nil then
        return tostring(obj.getName() or "")
    end
    return ""
end

--- True when an `NPC#Object`-tagged object is a seat figurine (pooled NPC cutout with `SEAT_FIGURE_*` or `npcInstance:` identity).
--- @param obj Object|nil
--- @param npcsModule table core.npcs
--- @return boolean
local function isNpcSeatFigurineObject(obj, npcsModule)
    if not U.isGameObject(obj) then
        return false
    end
    if type(npcsModule.isPooledFigurineObject) == "function" and npcsModule.isPooledFigurineObject(obj) then
        return true
    end
    local identity = SRI.readRoleColorIdentity(obj)
    if identity ~= nil then
        local role, _ = SRI.parseRoleAndSuffix(identity)
        if role == "SEAT_FIGURE" then
            return true
        end
    end
    if exportObjectDisplayName(obj) == "Figurine_Custom" then
        return true
    end
    return false
end

--- Collect `NPC1Object`..`NPC4Object` figurines and write a short Lua table `{ name, guid }` to workspace debug logs.
--- Run from the External Editor / TTS console: `lua DEBUG.exportNpcSeatFigurinesToFile()`.
--- Requires TTS Tools bridge on port 39998 for file write (`.dev/.debug/debug_logs/...`).
--- @param relativePath string|nil default `debug_logs/npc_seat_figurines.lua`
--- @return table rows `{ tag, name, guid }[]`
--- @return boolean wrote true when sendExternalMessage write succeeded
function DEBUG.exportNpcSeatFigurinesToFile(relativePath)
    relativePath = relativePath or "debug_logs/npc_seat_figurines.lua"
    local NPCS = require("core.npcs")
    local rows = {}
    local seenGuids = {}

    for _, seatKey in ipairs(C.NPCSeats or {}) do
        local tag = tostring(seatKey) .. "Object"
        local objs = getObjectsWithTag(tag)
        if type(objs) ~= "table" then
            objs = {}
        end
        for _, obj in ipairs(objs) do
            if isNpcSeatFigurineObject(obj, NPCS) then
                local guid = obj.getGUID and obj.getGUID() or nil
                if U.Type(guid) == "string" and guid ~= "" and seenGuids[guid] ~= true then
                    seenGuids[guid] = true
                    rows[#rows + 1] = {
                        tag = tag,
                        name = exportObjectDisplayName(obj),
                        guid = guid,
                    }
                end
            end
        end
    end

    table.sort(rows, function(a, b)
        local ta = tostring(a.tag)
        local tb = tostring(b.tag)
        if ta ~= tb then
            return ta < tb
        end
        return tostring(a.name) < tostring(b.name)
    end)

    local ts = getTimestamp()
    local lines = {
        "-- Generated by DEBUG.exportNpcSeatFigurinesToFile at " .. tostring(ts),
        "-- Tags: NPC1Object .. NPC4Object; figurines only (SEAT_FIGURE_* / pooled npc figurine / Figurine_Custom).",
        "",
        "return {",
    }
    for i = 1, #rows do
        local row = rows[i]
        local nameEsc = string.gsub(tostring(row.name), "\"", "\\\"")
        lines[#lines + 1] = string.format(
            "  { tag = \"%s\", name = \"%s\", guid = \"%s\" },",
            tostring(row.tag),
            nameEsc,
            tostring(row.guid)
        )
    end
    lines[#lines + 1] = "}"
    lines[#lines + 1] = ""

    local content = table.concat(lines, "\n")
    local wrote = DEBUG.writeWorkspaceFile(relativePath, content, "none", false)
    print("DEBUG.exportNpcSeatFigurinesToFile: " .. tostring(#rows) .. " figurine(s) → " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. relativePath)
    if JSON ~= nil and JSON.encode ~= nil then
        print(JSON.encode({ count = #rows, wrote = wrote, path = relativePath, rows = rows }))
    end
    return rows, wrote
end

--- Logs current scene information to a file
-- @param filename string Optional filename (default: "scene_info")
function DEBUG.logSceneToFile(filename)
    filename = filename or "scene_info"
    local currentScene = Scenes.getCurrentScene()
    local scene = Scenes.getScene(currentScene)
    local timestamp = getTimestamp()

    local content = string.format("Scene Information - %s\n", timestamp)
    content = content .. string.format("Current Scene: %s\n", currentScene or "nil")
    if scene then
        content = content .. string.format("Scene Data:\n")
        content = content .. JSON.encode_pretty(scene)
    else
        content = content .. "Scene data not found\n"
    end
    content = content .. "\n"

    -- Scene dumps are overwritten (not appended) - each dump is a complete snapshot
    writeToFile(filename, content, "auto", false)
    print("DEBUG: Scene info logged to " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. LOG_DIR .. "/" .. filename .. "." .. LOG_EXTENSION)
end

--- Logs a test result to a file
-- @param testName string Name of the test
-- @param passed boolean Whether the test passed
-- @param details string Optional details
-- @param filename string Optional filename (default: "test_results")
function DEBUG.logTestToFile(testName, passed, details, filename)
    filename = filename or "test_results"
    local timestamp = getTimestamp()
    local status = passed and "PASS" or "FAIL"

    local content = string.format("[%s] %s: %s\n", timestamp, status, testName)
    if details then
        content = content .. string.format("  Details: %s\n", details)
    end
    content = content .. "\n"

    -- Append to existing cached content
    writeToFile(filename, content, "none", true)
end

--- Logs all debug information to separate files
-- Creates: game_state, scene_info, and a general debug log
function DEBUG.logAllToFiles()
    print("DEBUG: Logging all information to files...")
    DEBUG.logStateToFile("game_state")
    DEBUG.logSceneToFile("scene_info")
    DEBUG.logToFile("INFO", "Full debug dump completed", "debug_log")
    print("DEBUG: All logs written under " .. WORKSPACE_DEBUG_WRITE_ROOT .. "/" .. LOG_DIR .. "/")
end

--- Clears all log files (writes empty/header content to reset them)
-- Called automatically on game load to start fresh logs
-- Also clears the in-memory cache
function DEBUG.clearAllLogs()
    local timestamp = getTimestamp()
    local sessionHeader = string.format("=== DEBUG LOG SESSION STARTED - %s ===\n\n", timestamp)

    -- Clear the cache
    logCache = {}

    -- Do not call writeToFile() for workspace logs on load: TTS Tools opens each written file
    -- in the editor, which is disruptive on every Save & Play (see prior test_results note).
    -- Seed append-style logs in memory only; first log line triggers the first disk write.
    logCache["debug_log"] = sessionHeader
    logCache["test_results"] = sessionHeader
end

--[[
    Custom UI Asset Upload Helpers
    These functions spawn temporary Custom_Token objects from generated manifest data.
    Use these tokens with Cloud Manager "Upload All Loaded Files", then save and run
    the Node merge script to update top-level CustomUIAssets.
]]

local DEBUG_UPLOAD_GMNOTES_PREFIX = "debug-custom-ui-upload:"
local DEBUG_UPLOAD_TAG = "debug-custom-ui-upload"
local DEBUG_UPLOAD_MANIFEST_MODULE = "lib.custom_ui_upload_manifest"

-- Bundler tracing + init load for committed manifest stubs (TTS_BUNDLING_SETUP.md § Issue 2a).
-- No init console noise — upload guidance prints only when DEBUG.loadCustomUiUploadManifest is invoked.
local CUSTOM_UI_UPLOAD_MANIFEST = require("lib.custom_ui_upload_manifest")
local NPC_TOKEN_UPLOAD_MANIFEST = require("lib.npc_token_upload_manifest")
local NPC_GROUP_UPLOAD_MANIFEST = require("lib.npc_group_upload_manifest")
local NPC_TOKEN_HOSTED_URLS = require("lib.npc_token_hosted_urls")
DEBUG.customUiUploadManifest = CUSTOM_UI_UPLOAD_MANIFEST

--- Parse an optional options table for upload spawn settings.
-- @param options table|nil
-- @param assetCount number|nil
-- @return table Parsed options with defaults
local function parseUploadOptions(options, assetCount)
    local resolved = options or {}
    local spawnX = resolved.startX
    local spawnY = resolved.startY
    local spawnZ = resolved.startZ
    local gap = resolved.gap
    local columns = resolved.columns
    local offset = resolved.offset
    local limit = resolved.limit

    local count = assetCount
    if U.Type(count) ~= "number" or count < 1 then
        count = 1
    end

    -- Auto-size columns when not provided:
    -- keep rows compact while avoiding very wide spawn grids.
    local autoColumns = math.ceil(math.sqrt(count))
    if autoColumns < 4 then autoColumns = 4 end
    if autoColumns > 12 then autoColumns = 12 end

    if U.Type(spawnX) ~= "number" then spawnX = 0 end
    if U.Type(spawnY) ~= "number" then spawnY = 3 end
    if U.Type(spawnZ) ~= "number" then spawnZ = 0 end
    if U.Type(gap) ~= "number" or gap <= 0 then gap = 2 end
    if U.Type(columns) ~= "number" or columns < 1 then columns = autoColumns end
    if U.Type(offset) ~= "number" or offset < 0 then offset = 0 end
    if U.Type(limit) ~= "number" or limit < 1 then limit = 999999 end

    return {
        startX = spawnX,
        startY = spawnY,
        startZ = spawnZ,
        gap = gap,
        columns = math.floor(columns),
        offset = math.floor(offset),
        limit = math.floor(limit)
    }
end

--- Normalize a manifest-like table into an asset list.
-- Accepted shapes:
--   { assets = { { name = "...", fileUrl = "file:///..." }, ... } }
--   { { name = "...", fileUrl = "file:///..." }, ... }
-- `name` may be supplied as `image` (Custom UI asset name) instead of `name`.
-- @param manifest table Manifest object or direct asset array
-- @return table|nil Array of normalized assets, or nil on validation failure
local function normalizeUploadManifest(manifest)
    if U.Type(manifest) ~= "table" then
        print("DEBUG.spawnCustomUiUploadBatchFromManifest: manifest must be a table")
        return nil
    end

    local assets = manifest.assets
    if U.Type(assets) ~= "table" then
        assets = manifest
    end
    if U.Type(assets) ~= "table" then
        print("DEBUG.spawnCustomUiUploadBatchFromManifest: manifest.assets must be a table")
        return nil
    end

    local normalized = {}
    local seenNames = {}

    for i, asset in ipairs(assets) do
        if U.Type(asset) ~= "table" then
            print("DEBUG: Skipping non-table asset at index " .. tostring(i))
        else
            local name = asset.name or asset.Name or asset.assetName or asset.image
            local fileUrl = asset.fileUrl or asset.url or asset.URL
            if U.Type(name) ~= "string" or name == "" then
                print("DEBUG: Skipping asset with invalid name at index " .. tostring(i))
            elseif U.Type(fileUrl) ~= "string" or fileUrl == "" then
                print("DEBUG: Skipping asset '" .. name .. "' with invalid fileUrl/url")
            elseif not string.find(fileUrl, "^file:///") then
                print("DEBUG: Skipping asset '" .. name .. "' because URL is not file:/// (" .. tostring(fileUrl) .. ")")
            elseif seenNames[name] then
                print("DEBUG: Skipping duplicate asset name '" .. name .. "'")
            else
                seenNames[name] = true
                table.insert(normalized, {
                    name = name,
                    fileUrl = fileUrl,
                    kind = asset.kind,
                    characterKey = asset.characterKey,
                })
            end
        end
    end

    if #normalized == 0 then
        print("DEBUG.spawnCustomUiUploadBatchFromManifest: no valid assets found")
        return nil
    end

    return normalized
end

--- @param asset table
--- @param spawnPos table
--- @return boolean
local function spawnSingleUploadToken(asset, spawnPos)
    ---@diagnostic disable-next-line: undefined-global
    local obj = spawnObject({
        type = "Custom_Token",
        position = spawnPos,
        sound = false
    })

    if obj == nil then
        print("DEBUG: Failed to spawn token for " .. tostring(asset.name))
        return false
    end

    obj.setName(asset.name)
    obj.setGMNotes(DEBUG_UPLOAD_GMNOTES_PREFIX .. asset.name)
    if obj.setTags ~= nil then
        obj.setTags({ DEBUG_UPLOAD_TAG, asset.name })
    end
    obj.setCustomObject({
        image = asset.fileUrl,
        thickness = 0.2,
        merge_distance = 15,
        stackable = false
    })
    if obj.setLock ~= nil then
        obj.setLock(true)
    end
    return true
end

--- Figurine_Custom with front + back file:/// URLs (Back WEBPs are often near-transparent on flat tokens).
--- Object Name is the front asset name; merge maps ImageSecondaryURL to `<name>Back`.
--- @param frontAsset table
--- @param backAsset table
--- @param spawnPos table
--- @return boolean
local function spawnNpcGroupUploadFigurinePair(frontAsset, backAsset, spawnPos)
    ---@diagnostic disable-next-line: undefined-global
    local obj = spawnObject({
        type = "Figurine_Custom",
        position = spawnPos,
        sound = false
    })

    if obj == nil then
        print("DEBUG: Failed to spawn figurine for " .. tostring(frontAsset.name))
        return false
    end

    obj.setName(frontAsset.name)
    obj.setGMNotes(DEBUG_UPLOAD_GMNOTES_PREFIX .. "figurine:" .. frontAsset.name)
    if obj.setTags ~= nil then
        obj.setTags({ DEBUG_UPLOAD_TAG, frontAsset.name })
    end
    obj.setCustomObject({
        image = frontAsset.fileUrl,
        image_secondary = backAsset.fileUrl,
    })
    if obj.setLock ~= nil then
        obj.setLock(true)
    end
    return true
end

--- @param assets table[]
--- @return table<string, table<string, table>>
local function groupNpcGroupManifestAssets(assets)
    local byKey = {}

    for _, asset in ipairs(assets) do
        local characterKey = asset.characterKey
        if U.Type(characterKey) ~= "string" or characterKey == "" then
            local kind = asset.kind or ""
            if kind == "figurineFront" then
                characterKey = asset.name
            elseif kind == "figurineBack" and U.Type(asset.name) == "string" and string.sub(asset.name, -4) == "Back" then
                characterKey = string.sub(asset.name, 1, -5)
            elseif kind == "tokenFront" and U.Type(asset.name) == "string" then
                local prefix = "tokenFront_"
                if string.sub(asset.name, 1, #prefix) == prefix then
                    characterKey = string.sub(asset.name, #prefix + 1)
                end
            elseif kind == "tokenBack" and U.Type(asset.name) == "string" then
                local prefix = "tokenBack_"
                if string.sub(asset.name, 1, #prefix) == prefix then
                    characterKey = string.sub(asset.name, #prefix + 1)
                end
            end
        end

        if U.Type(characterKey) == "string" and characterKey ~= "" then
            if byKey[characterKey] == nil then
                byKey[characterKey] = {}
            end
            local kind = asset.kind
            if U.Type(kind) == "string" and kind ~= "" then
                byKey[characterKey][kind] = asset
            end
        end
    end

    return byKey
end

--- NPC unified groups: one Figurine_Custom per character (front+back) plus token upload temps.
--- @param manifest table Manifest object (with .assets) or direct assets array
--- @param options table|nil Optional controls: startX/startY/startZ/gap/columns/offset/limit
--- @return number Count of spawned upload objects
local function spawnNpcGroupUploadBatchFromManifest(manifest, options)
    local assets = normalizeUploadManifest(manifest)
    if assets == nil then
        return 0
    end

    local grouped = groupNpcGroupManifestAssets(assets)
    local characterKeys = {}
    for key in pairs(grouped) do
        characterKeys[#characterKeys + 1] = key
    end
    table.sort(characterKeys)

    local opts = parseUploadOptions(options, #characterKeys * 3)
    local spawned = 0
    local processed = 0
    local charIndex = 0

    for _, characterKey in ipairs(characterKeys) do
        charIndex = charIndex + 1
        if charIndex > opts.offset and spawned < opts.limit then
            local row = grouped[characterKey]
            if U.Type(row) == "table" then
                local function nextSpawnPos()
                    local gridRow = math.floor(processed / opts.columns)
                    local col = processed % opts.columns
                    processed = processed + 1
                    return {
                        x = opts.startX + (col * opts.gap),
                        y = opts.startY,
                        z = opts.startZ + (gridRow * opts.gap)
                    }
                end

                local figurineFront = row.figurineFront
                local figurineBack = row.figurineBack
                if figurineFront ~= nil and figurineBack ~= nil then
                    if spawnNpcGroupUploadFigurinePair(figurineFront, figurineBack, nextSpawnPos()) then
                        spawned = spawned + 1
                    end
                elseif figurineFront ~= nil or figurineBack ~= nil then
                    print("DEBUG: Skipping incomplete figurine pair for " .. tostring(characterKey))
                end

                if spawned < opts.limit then
                    local tokenFront = row.tokenFront
                    if tokenFront ~= nil and spawnSingleUploadToken(tokenFront, nextSpawnPos()) then
                        spawned = spawned + 1
                    end
                end

                if spawned < opts.limit then
                    local tokenBack = row.tokenBack
                    if tokenBack ~= nil and spawnSingleUploadToken(tokenBack, nextSpawnPos()) then
                        spawned = spawned + 1
                    end
                end
            end
        end
    end

    print(
        "DEBUG.spawnNpcGroupUploadBatchFromManifest: Spawned "
            .. tostring(spawned)
            .. " upload objects (figurine pairs + token temps)"
    )
    return spawned
end

--- Spawn a batch of temporary Custom_Token objects from a manifest table.
-- Each token is named with `asset.name`, configured with the local `fileUrl`, then locked to avoid physics scatter.
-- @param manifest table Manifest object (with .assets) or direct assets array
-- @param options table|nil Optional controls: startX/startY/startZ/gap/columns/offset/limit
-- @return number Count of spawned tokens
function DEBUG.spawnCustomUiUploadBatchFromManifest(manifest, options)
    local assets = normalizeUploadManifest(manifest)
    if assets == nil then
        return 0
    end

    local opts = parseUploadOptions(options, #assets)
    local spawned = 0
    local processed = 0

    for i, asset in ipairs(assets) do
        if i > opts.offset and spawned < opts.limit then
            local row = math.floor(processed / opts.columns)
            local col = processed % opts.columns

            local spawnPos = {
                x = opts.startX + (col * opts.gap),
                y = opts.startY,
                z = opts.startZ + (row * opts.gap)
            }

            if spawnSingleUploadToken(asset, spawnPos) then
                spawned = spawned + 1
                processed = processed + 1
            end
        end
    end

    print("DEBUG.spawnCustomUiUploadBatchFromManifest: Spawned " .. tostring(spawned) .. " uploader tokens")
    return spawned
end

--- Spawn a batch from a JSON string generated by build-upload-manifest.js.
-- @param manifestJson string JSON string containing { assets: [ {name,fileUrl}, ... ] }
-- @param options table|nil Optional controls: startX/startY/startZ/gap/columns/offset/limit
-- @return number Count of spawned tokens
function DEBUG.spawnCustomUiUploadBatchFromJson(manifestJson, options)
    if U.Type(manifestJson) ~= "string" or manifestJson == "" then
        print("DEBUG.spawnCustomUiUploadBatchFromJson: manifestJson must be a non-empty string")
        return 0
    end

    local parsed = JSON.decode(manifestJson)
    if U.Type(parsed) ~= "table" then
        print("DEBUG.spawnCustomUiUploadBatchFromJson: failed to parse JSON manifest")
        return 0
    end

    return DEBUG.spawnCustomUiUploadBatchFromManifest(parsed, options)
end

--- Load a generated custom UI upload manifest Lua module via require().
-- This requires Save & Play after regenerating the module file.
-- @param moduleName string|nil Optional module name (default: lib.custom_ui_upload_manifest)
-- @param forceReload boolean|nil If true, clears require cache before loading (default: true)
-- @return table|nil Loaded manifest table or nil on failure
function DEBUG.loadCustomUiUploadManifest(moduleName, forceReload)
    local targetModule = moduleName
    if U.Type(targetModule) ~= "string" or targetModule == "" then
        targetModule = DEBUG_UPLOAD_MANIFEST_MODULE
    end
    local shouldForceReload = forceReload
    if shouldForceReload == nil then
        shouldForceReload = true
    end

    if shouldForceReload then
        if U.Type(package) == "table" and U.Type(package.loaded) == "table" then
            package.loaded[targetModule] = nil
        end
    end

    -- pcall: runtime reload after npm regen must not throw during explicit DEBUG invocations.
    local ok, manifest = pcall(require, targetModule)
    if not ok then
        print(
            "DEBUG.loadCustomUiUploadManifest: failed to require '"
                .. tostring(targetModule)
                .. "': "
                .. tostring(manifest)
        )
        return nil
    end

    if U.Type(manifest) ~= "table" then
        print("DEBUG.loadCustomUiUploadManifest: module '" .. tostring(targetModule) .. "' did not return a table")
        return nil
    end

    DEBUG.customUiUploadManifest = manifest
    local count = 0
    if U.Type(manifest.assets) == "table" then
        count = #manifest.assets
    end
    print("DEBUG.loadCustomUiUploadManifest: loaded " .. tostring(count) .. " assets from " .. tostring(targetModule))
    if count == 0 then
        print(
            "DEBUG.loadCustomUiUploadManifest: manifest is empty — run the matching npm custom-ui-assets:manifest-* script, then Save & Play"
        )
    end
    return manifest
end

--- Spawn upload tokens from lib.npc_token_upload_manifest (tokenFront_* / tokenBack_* WEBPs).
-- @param options table|nil Optional grid: columns, gap, startY, etc.
-- @return number Count of spawned tokens
function DEBUG.spawnNpcTokenUploadBatch(options)
    return DEBUG.spawnCustomUiUploadBatch(options, "lib.npc_token_upload_manifest")
end

--- Spawn upload tokens from lib.npc_group_upload_manifest (figurine + token 4-file sets).
-- @param options table|nil Optional grid: columns, gap, startY, etc.
-- @return number Count of spawned tokens
function DEBUG.spawnNpcGroupUploadBatch(options)
    local manifest = DEBUG.loadCustomUiUploadManifest("lib.npc_group_upload_manifest", true)
    if U.Type(manifest) ~= "table" then
        print("DEBUG.spawnNpcGroupUploadBatch: no manifest loaded")
        return 0
    end
    return spawnNpcGroupUploadBatchFromManifest(manifest, options)
end

--- Spawn a batch from the default generated manifest module.
-- @param options table|nil Optional controls: startX/startY/startZ/gap/columns/offset/limit
-- @param moduleName string|nil Optional module name override
-- @return number Count of spawned tokens
function DEBUG.spawnCustomUiUploadBatch(options, moduleName)
    -- Always force reload so this uses the latest generated manifest file.
    local manifest = DEBUG.loadCustomUiUploadManifest(moduleName, true)
    if U.Type(manifest) ~= "table" then
        print("DEBUG.spawnCustomUiUploadBatch: no manifest loaded")
        return 0
    end
    return DEBUG.spawnCustomUiUploadBatchFromManifest(manifest, options)
end

--- Delete spawned uploader tokens created by DEBUG.spawnCustomUiUploadBatchFromManifest().
-- @return number Count of deleted objects
function DEBUG.clearCustomUiUploadTokens()
    local allObjects = getAllObjects()
    local deleted = 0

    for _, obj in ipairs(allObjects) do
        if obj ~= nil and obj.getGMNotes ~= nil then
            local notes = obj.getGMNotes()
            local matchesNotes = U.Type(notes) == "string" and string.find(notes, "^" .. DEBUG_UPLOAD_GMNOTES_PREFIX) ~= nil
            local matchesTag = false
            if obj.getTags ~= nil then
                local tags = obj.getTags()
                if U.Type(tags) == "table" then
                    for _, tag in ipairs(tags) do
                        if tag == DEBUG_UPLOAD_TAG then
                            matchesTag = true
                            break
                        end
                    end
                end
            end

            if matchesNotes or matchesTag then
                obj.destruct()
                deleted = deleted + 1
            end
        end
    end

    print("DEBUG.clearCustomUiUploadTokens: Deleted " .. tostring(deleted) .. " upload temps (tokens or figurines)")
    return deleted
end

--[[
    NPC control-board tokens (paired front/back Custom_Tile on CONTROL_BOARD).
    Distinct from upload-batch Custom_Token spawns (Cloud Manager upload only).
]]

local NPC_GAMEBOARD_DATA = require("lib.npc_gameboard_data")

--- Lazy: avoid loading gameboard during `core.state` registration (see `lib.npc_placements_convert`).
--- @return table
local function npcGameboardModule()
    return require("core.npc_gameboard")
end
local NPC_CONTROL_TOKEN_TAG = NPC_GAMEBOARD_DATA.TAG_NPC_CONTROL_TOKEN
local NPC_TOKEN_GM_PREFIX = NPC_GAMEBOARD_DATA.TOKEN_GM_PREFIX
local NPC_TOKEN_MANIFEST_MODULE = "lib.npc_token_upload_manifest"
--- Custom_Tile.Type: 0 square, 1 hex, 2 circle (round), 3 rounded square (.dev/tts-api Custom Tile).
local NPC_CONTROL_TOKEN_TILE_TYPE = 2
local NPC_CONTROL_TOKEN_TILE_THICKNESS = 0.1

--- @param url string|nil
--- @return boolean
local function isFileUrl(url)
    return U.Type(url) == "string" and string.find(url, "^file:///") ~= nil
end

--- @param url string|nil
--- @return boolean
local function isHostedSteamUrl(url)
    return U.Type(url) == "string"
        and string.find(url, "^https?://") ~= nil
        and (
            string.find(url, "steamusercontent") ~= nil
            or string.find(url, "steamusercontent%-a%.akamaihd%.net") ~= nil
        )
end

--- @param manifest table
--- @return table|nil characterKey -> { front: string, back: string }
local function indexNpcTokenPairsFromManifest(manifest)
    if U.Type(manifest) ~= "table" then
        return nil
    end
    local assets = manifest.assets
    if U.Type(assets) ~= "table" then
        return nil
    end
    local pairsByKey = {}
    for _, asset in ipairs(assets) do
        if U.Type(asset) == "table" then
            local characterKey = asset.characterKey
            local side = asset.side
            local fileUrl = asset.fileUrl or asset.url or asset.URL
            if U.Type(characterKey) == "string" and characterKey ~= "" and U.Type(fileUrl) == "string" and fileUrl ~= "" then
                local row = pairsByKey[characterKey]
                if row == nil then
                    row = {}
                    pairsByKey[characterKey] = row
                end
                if side == "front" then
                    row.front = fileUrl
                elseif side == "back" then
                    row.back = fileUrl
                end
            end
        end
    end
    return pairsByKey
end

--- Prefer Steam URLs from lib.npc_token_hosted_urls; else file:/// rows from upload manifest.
--- @param options table|nil { preferHosted?: boolean }
--- @return table|nil pairsByKey, string sourceLabel
local function loadNpcControlTokenPairUrls(options)
    local opts = options or {}
    local preferHosted = opts.preferHosted
    if preferHosted == nil then
        preferHosted = true
    end

    if preferHosted and U.Type(NPC_TOKEN_HOSTED_URLS) == "table" then
        local hosted = NPC_TOKEN_HOSTED_URLS
        local pairsByKey = {}
        for characterKey, row in pairs(hosted) do
            if U.Type(characterKey) == "string" and U.Type(row) == "table" then
                local front = row.front or row.Front
                local back = row.back or row.Back
                if isHostedSteamUrl(front) and isHostedSteamUrl(back) then
                    pairsByKey[characterKey] = { front = front, back = back }
                end
            end
        end
        local count = 0
        for _ in pairs(pairsByKey) do
            count = count + 1
        end
        if count > 0 then
            return pairsByKey, "lib.npc_token_hosted_urls"
        end
    end

    local manifest = DEBUG.loadCustomUiUploadManifest(NPC_TOKEN_MANIFEST_MODULE, true)
    if manifest == nil then
        return nil, "none"
    end
    return indexNpcTokenPairsFromManifest(manifest), NPC_TOKEN_MANIFEST_MODULE
end

--- @param pairsByKey table
--- @return string[] sorted character keys with both sides
local function sortedCompleteNpcTokenKeys(pairsByKey)
    local npcChars = require("lib.npcs_data").getNpcCharacters()
    local keys = {}
    for characterKey, row in pairs(pairsByKey) do
        if U.Type(row) == "table" and npcChars[characterKey] ~= nil then
            local frontOk = isFileUrl(row.front) or isHostedSteamUrl(row.front)
            local backOk = isFileUrl(row.back) or isHostedSteamUrl(row.back)
            if frontOk and backOk then
                keys[#keys + 1] = characterKey
            end
        end
    end
    table.sort(keys)
    return keys
end

--- Save-format spawn data for a flip-capable NPC control token (matches dual-image Custom_Tile in save).
--- @param worldPos { x: number, y: number, z: number }
--- @param yawDeg number
--- @param characterKey string
--- @param frontUrl string
--- @param backUrl string
--- @param scale number|nil
--- @param faceUp boolean|nil
--- @return table
local function buildNpcControlTokenSpawnData(worldPos, yawDeg, characterKey, frontUrl, backUrl, scale, faceUp)
    local s = tonumber(scale)
    if s == nil or s ~= s or s <= 0 then
        s = 0.38
    end
    local showFront = faceUp ~= false
    return {
        GUID = "",
        Name = "Custom_Tile",
        Transform = {
            posX = worldPos.x,
            posY = worldPos.y,
            posZ = worldPos.z,
            rotX = 0,
            rotY = yawDeg,
            rotZ = showFront and 0 or 180,
            scaleX = s,
            scaleY = 1,
            scaleZ = s,
        },
        Nickname = characterKey,
        Description = "",
        GMNotes = NPC_TOKEN_GM_PREFIX .. characterKey,
        AltLookAngle = { x = 0, y = 0, z = 0 },
        ColorDiffuse = { r = 1, g = 1, b = 1 },
        LayoutGroupSortIndex = 0,
        Value = 0,
        Locked = false,
        Grid = false,
        Snap = false,
        IgnoreFoW = false,
        MeasureMovement = false,
        DragSelectable = true,
        Autoraise = true,
        Sticky = false,
        Tooltip = true,
        GridProjection = false,
        HideWhenFaceDown = false,
        Hands = false,
        CustomImage = {
            ImageURL = frontUrl,
            ImageSecondaryURL = backUrl,
            ImageScalar = 1,
            WidthScale = 0,
            CustomTile = {
                Type = NPC_CONTROL_TOKEN_TILE_TYPE,
                Thickness = NPC_CONTROL_TOKEN_TILE_THICKNESS,
                Stackable = false,
                Stretch = true,
            },
        },
        LuaScript = "",
        LuaScriptState = "",
        XmlUI = "",
        Tags = { NPC_CONTROL_TOKEN_TAG },
    }
end

--- Enforce round Custom_Tile (type 2) and thin thickness on an NPC control token.
--- spawnObjectData can omit CustomTile fields; setCustomObject + reload applies them in-game.
--- @param obj Object
--- @param frontUrl string
--- @param backUrl string
--- @return boolean
local function applyNpcControlTokenTileShape(obj, frontUrl, backUrl)
    if obj == nil or obj.setCustomObject == nil then
        return false
    end
    obj.setCustomObject({
        image = frontUrl,
        image_bottom = backUrl,
        type = NPC_CONTROL_TOKEN_TILE_TYPE,
        thickness = NPC_CONTROL_TOKEN_TILE_THICKNESS,
        stackable = false,
        stretch = true,
    })
    if obj.reload ~= nil then
        obj.reload()
    end
    return true
end

--- @return Object|nil
local function getControlBoardObject()
    local guid = G.GUIDS and G.GUIDS.CONTROL_BOARD
    if U.Type(guid) ~= "string" or guid == "" then
        return nil
    end
    local board = G.getObject(guid)
    if board == nil then
        return nil
    end
    return board
end

--- Delete every `npc_control_token` on or off the control board.
-- @return number
function DEBUG.clearNpcControlBoardTokens()
    local deleted = 0
    local tagObjs = getObjectsWithTag(NPC_CONTROL_TOKEN_TAG) or {}
    for _, obj in ipairs(tagObjs) do
        if obj ~= nil and obj.destruct ~= nil then
            obj.destruct()
            deleted = deleted + 1
        end
    end
    print("DEBUG.clearNpcControlBoardTokens: Deleted " .. tostring(deleted) .. " control-board token(s)")
    return deleted
end

--- Apply hosted front/back URLs to existing control-board tokens (after merge + extract).
-- @return number count updated
function DEBUG.applyNpcControlTokenHostedImages()
    local pairsByKey, source = loadNpcControlTokenPairUrls({ preferHosted = true })
    if pairsByKey == nil then
        print("DEBUG.applyNpcControlTokenHostedImages: no hosted URL pairs (" .. tostring(source) .. ")")
        return 0
    end
    local updated = 0
    local tagObjs = getObjectsWithTag(NPC_CONTROL_TOKEN_TAG) or {}
    for _, obj in ipairs(tagObjs) do
        if obj ~= nil and obj.getGMNotes ~= nil and obj.setCustomObject ~= nil then
            local notes = obj.getGMNotes()
            if U.Type(notes) == "string" and string.find(notes, "^" .. NPC_TOKEN_GM_PREFIX) ~= nil then
                local characterKey = string.sub(notes, #NPC_TOKEN_GM_PREFIX + 1)
                local row = pairsByKey[characterKey]
                if U.Type(row) == "table" and isHostedSteamUrl(row.front) and isHostedSteamUrl(row.back) then
                    if applyNpcControlTokenTileShape(obj, row.front, row.back) then
                        npcGameboardModule().setHiddenFromPlayerColors(obj)
                        updated = updated + 1
                    end
                end
            end
        end
    end
    print("DEBUG.applyNpcControlTokenHostedImages: Updated " .. tostring(updated) .. " token(s) from " .. tostring(source))
    return updated
end

--- Spawn paired front/back NPC control tokens on CONTROL_BOARD (palette UV grid on the minimap).
-- Uses `lib.npc_token_hosted_urls` when populated; otherwise `lib.npc_token_upload_manifest` file:/// URLs.
-- Tiles: Custom_Tile type 2 (circle), thickness 0.1 — enforced via spawn data + setCustomObject after spawn.
-- @param options table|nil { destroyExisting?, columns?, total?, uMin?, uMax?, vMin?, vMax?, scale?, preferHosted?, faceUp?, boardYaw? }
-- @return number spawned
function DEBUG.spawnNpcControlBoardTokens(options)
    local opts = options or {}
    if opts.destroyExisting ~= false then
        DEBUG.clearNpcControlBoardTokens()
    end

    local palette = G.getObject(G.GUIDS.CONTROL_BOARD_PALETTE)
    if palette == nil then
        print(
            "DEBUG.spawnNpcControlBoardTokens: CONTROL_BOARD_PALETTE missing (GUID "
                .. tostring(G.GUIDS.CONTROL_BOARD_PALETTE)
                .. ")"
        )
        return 0
    end
    npcGameboardModule().installPaletteSnaps(palette, { force = true })

    local pairsByKey, source = loadNpcControlTokenPairUrls(opts)
    if pairsByKey == nil then
        print("DEBUG.spawnNpcControlBoardTokens: no token URL pairs — run npm run custom-ui-assets:manifest-npc-tokens")
        return 0
    end

    local keys = sortedCompleteNpcTokenKeys(pairsByKey)
    if #keys == 0 then
        print("DEBUG.spawnNpcControlBoardTokens: manifest has no complete front+back pairs")
        return 0
    end

    local paletteRot = palette.getRotation()
    local boardYaw = (paletteRot and paletteRot.y) or 0
    if U.Type(opts.boardYaw) == "number" then
        boardYaw = opts.boardYaw
    end

    local faceUp = opts.faceUp
    if faceUp == nil then
        faceUp = true
    end

    local Palette = require("lib.npc_control_board_palette")
    local spawnPos = Palette.worldPositionForSnapIndex(palette, 0)
    if spawnPos == nil then
        print("DEBUG.spawnNpcControlBoardTokens: could not resolve palette snap world position")
        return 0
    end

    local spawned = 0
    local skipped = 0
    for i, characterKey in ipairs(keys) do
        local row = pairsByKey[characterKey]
        local worldPos = Vector(spawnPos.x, spawnPos.y, spawnPos.z)
        local data = buildNpcControlTokenSpawnData(
            { x = worldPos.x, y = worldPos.y, z = worldPos.z },
            boardYaw,
            characterKey,
            row.front,
            row.back,
            opts.scale,
            faceUp
        )
        ---@diagnostic disable-next-line: undefined-global
        local obj = spawnObjectData({ data = data })
        if obj == nil then
            print("DEBUG.spawnNpcControlBoardTokens: spawn failed for " .. characterKey)
            skipped = skipped + 1
        else
            applyNpcControlTokenTileShape(obj, row.front, row.back)
            if obj.setTags ~= nil then
                obj.setTags({ NPC_CONTROL_TOKEN_TAG })
            end
            if obj.setGMNotes ~= nil then
                obj.setGMNotes(NPC_TOKEN_GM_PREFIX .. characterKey)
            end
            if obj.setName ~= nil then
                obj.setName(characterKey)
            end
            npcGameboardModule().setHiddenFromPlayerColors(obj)
            spawned = spawned + 1
        end
    end

    local parked = npcGameboardModule().syncTokensToPalette()
    print(
        "DEBUG.spawnNpcControlBoardTokens: Spawned "
            .. tostring(spawned)
            .. "/"
            .. tostring(#keys)
            .. " → parked on CONTROL_BOARD_PALETTE ("
            .. tostring(source)
            .. "; tile type="
            .. tostring(NPC_CONTROL_TOKEN_TILE_TYPE)
            .. " circle, thickness="
            .. tostring(NPC_CONTROL_TOKEN_TILE_THICKNESS)
            .. "; layout sync moved "
            .. tostring(parked)
            .. ")"
    )
    if skipped > 0 then
        print("DEBUG.spawnNpcControlBoardTokens: Skipped " .. tostring(skipped))
    end
    print("  Drag tokens onto CONTROL_BOARD polar snaps, then Apply (or lua GlobalGameboardApply())")
    return spawned
end

--- One-time: copy seat-layout `ROLE_COLOR` from Name/Nickname into GM Notes (see `.dev/Rotational Coordinate Generator.md`).
--- @param options table|nil `{ dryRun?: boolean, clearLegacy?: boolean }`
--- @return table summary
function DEBUG.migrateSeatLayoutRolesToGmNotes(options)
    local R = require("lib.rotational-seat-layout")
    return R.migrateRoleIdentityToGmNotes(C.TableSourceObjects, options)
end

--- @param s string|nil
--- @return string
local function trimRoleQuery(s)
    if U.Type(s) ~= "string" then
        return ""
    end
    return string.match(s, "^%s*(.-)%s*$") or ""
end

--- True when `identity` matches `roleQuery` (exact ROLE_COLOR or role prefix / suffix).
--- @param identity string|nil
--- @param roleQuery string
--- @return boolean
local function roleIdentityMatchesQuery(identity, roleQuery)
    if type(identity) ~= "string" or identity == "" then
        return false
    end
    if type(roleQuery) ~= "string" or roleQuery == "" then
        return false
    end
    local iu = string.upper(trimRoleQuery(identity))
    local qu = string.upper(trimRoleQuery(roleQuery))
    if iu == qu then
        return true
    end
    local role, suffix = SRI.parseRoleAndSuffix(iu)
    if role == qu then
        return true
    end
    if suffix ~= nil and role .. "_" .. suffix == qu then
        return true
    end
    if #qu < #iu and string.sub(iu, 1, #qu) == qu and string.sub(iu, #qu + 1, #qu + 1) == "_" then
        return true
    end
    return false
end

--- Collect tag names on an object (empty table when none).
--- @param obj Object|nil
--- @return string[]
local function collectObjectTags(obj)
    local tags = {}
    if not U.isGameObject(obj) or obj.getTags == nil then
        return tags
    end
    local raw = obj.getTags()
    if type(raw) ~= "table" then
        return tags
    end
    for _, tag in ipairs(raw) do
        if type(tag) == "string" and tag ~= "" then
            tags[#tags + 1] = tag
        end
    end
    return tags
end

--- Scan all table objects whose GM Notes (or legacy Name/Nickname) match a role identity.
--- Use to debug duplicate SEAT_FIGURE errors: `lua DEBUG.findObjectsByRole("SEAT_FIGURE_NPC1")`
--- @param roleQuery string Full identity (`SEAT_FIGURE_NPC1`) or role prefix (`SEAT_FIGURE`, `CSHEET_PAGE_1`)
--- @param options table|nil `{ seatTag?: string, includeLegacy?: boolean, print?: boolean }`
--- @return table[] rows `{ guid, roleIdentity, gmNotes, legacyIdentity, name, nickname, tags, seatSuffix? }`
function DEBUG.findObjectsByRole(roleQuery, options)
    options = options or {}
    local seatTag = options.seatTag
    local includeLegacy = options.includeLegacy ~= false
    local doPrint = options.print ~= false
    local rows = {}

    for _, obj in ipairs(getObjects() or {}) do
        if U.isGameObject(obj) then
            local tags = collectObjectTags(obj)
            local seatTagOk = seatTag == nil
                or (obj.hasTag ~= nil and obj.hasTag(seatTag .. "Object") == true)
            if seatTagOk then
                local gmNotes = obj.getGMNotes ~= nil and tostring(obj.getGMNotes() or "") or ""
                local identity = SRI.readRoleColorIdentity(obj)
                local legacy = includeLegacy and SRI.readLegacyRoleColorIdentity(obj) or nil
                local matched = roleIdentityMatchesQuery(identity, roleQuery)
                    or (legacy ~= nil and roleIdentityMatchesQuery(legacy, roleQuery))
                if matched then
                    local _, suffix = SRI.parseRoleAndSuffix(identity or legacy or "")
                    local row = {
                        guid = obj.getGUID and obj.getGUID() or nil,
                        roleIdentity = identity or legacy,
                        gmNotes = gmNotes,
                        legacyIdentity = (legacy ~= nil and legacy ~= identity) and legacy or nil,
                        name = obj.getName ~= nil and tostring(obj.getName() or "") or "",
                        nickname = exportObjectDisplayName(obj),
                        tags = tags,
                        seatSuffix = suffix,
                    }
                    rows[#rows + 1] = row
                end
            end
        end
    end

    if doPrint then
        print(
            "DEBUG.findObjectsByRole('"
                .. tostring(roleQuery)
                .. "'"
                .. (seatTag and (", seatTag=" .. seatTag) or "")
                .. "): "
                .. tostring(#rows)
                .. " match(es)"
        )
        for i, row in ipairs(rows) do
            print(
                "  "
                    .. tostring(i)
                    .. ". guid="
                    .. tostring(row.guid)
                    .. " identity="
                    .. tostring(row.roleIdentity)
                    .. " nick="
                    .. tostring(row.nickname)
            )
            if row.legacyIdentity ~= nil then
                print("     legacyIdentity=" .. tostring(row.legacyIdentity))
            end
            if row.tags and #row.tags > 0 then
                print("     tags=" .. table.concat(row.tags, ", "))
            end
        end
    end

    return rows
end

--- Alias for `DEBUG.findObjectsByRole`.
DEBUG.scanObjectsByRole = DEBUG.findObjectsByRole

--- Return GUID strings only (console: `lua table.concat(DEBUG.guidsByRole("SEAT_FIGURE_NPC1"), ", ")`).
--- @param roleQuery string
--- @param options table|nil
--- @return string[]
function DEBUG.guidsByRole(roleQuery, options)
    options = options or {}
    options.print = false
    local rows = DEBUG.findObjectsByRole(roleQuery, options)
    local guids = {}
    for _, row in ipairs(rows) do
        if row.guid ~= nil then
            guids[#guids + 1] = row.guid
        end
    end
    return guids
end

--[[
    Help/Documentation
]]

--- Display help for all debug functions
function DEBUG.help()
    print("\n" .. string.rep("=", 70))
    print("TORONTO RISING — DEBUG CONSOLE COMMANDS")
    print(string.rep("=", 70))
    print("\nUse in TTS console: lua <command>()  |  Manual E2E: .dev/E2E Playbooks/")
    print("\nINSPECTION:")
    print("  showState()              - Full gameState JSON")
    print("  showScene()              - sessionScene + sceneLibrary keys")
    print("  inspectSoundscapeAudio() - Emitter GUID/tag/effect inventory")
    print("  dumpConditions(color)    - Active conditions + resolved effects")
    print("  dumpEffectiveStats(color) - Effective stat getters (tracker max, BP, …)")
    print("  dumpRollPolicy(color)    - Merged roll policy on active roll")
    print("\nFILE LOGGING:")
    print("  logToFile(level, msg, filename?) - Log a message to file")
    print("  logStateToFile(filename?)       - Log game state to file (JSON)")
    print("  logNpcPlacementIntentToFile(filename?) - Log Step Zero NPC placement intent (reconcile verify)")
    print("  exportNpcSeatFigurinesToFile(path?)     - Export NPC#Object figurines (name + GUID) to debug_logs")
    print("  exportControlBoardSnapGroupsToFile(filename?, config?) - CSV polar snap groups (logStateToFile path)")
    print("  migrateSeatLayoutRolesToGmNotes(opts?) - Copy ROLE_COLOR Name/Nick -> GM Notes (opts.dryRun)")
    print("  findObjectsByRole(query, opts?)        - Scan GM Notes role → GUID rows (opts.seatTag, opts.print)")
    print("  guidsByRole(query, opts?)              - Same scan; returns GUID string array only")
    print("  logSceneToFile(filename?)       - Log scene info to file")
    print("  logTestToFile(name, passed, details?, filename?) - Log test result")
    print("  logAllToFiles()                   - Log all debug info to files")
    print("  clearAllLogs()                    - Clear all log files (starts new session)")
    print("\nDICE DEBUG (solo — no second player):")
    print("  RunTest(\"Dice\")            - Arm Dice E2E playbook (npm run e2e-playbook:generate)")
    print("  RunTest(\"Dice\", 8)         - Arm at step 8; RunTest(\"Dice\", \"H\") at suite H")
    print("  RunTest()                  - Run next playbook step; stops step on console FAIL")
    print("  rollTest(color, diff?, type?, label?, hungerLevel?) - Arm PRE_ROLL (auto seat + camera prep)")
    print("  rollTestNoDiff(color, type?, label?, hungerLevel?) - Arm PRE_ROLL without ST difficulty")
    print("  rollE2eSeatPrep(color)     - Manual prep only; rollTest/rollStTest include this")
    print("  rollSetFaces(color, { normal={7,7}, hunger={10,1} }) - Set faces + recalculate (POST_ROLL)")
    print("  rollE2eApplyConditions(color, { 'bestialNull' }) / rollE2eClearConditions(color, ids?)")
    print("  rollE2eSetPoolAndSpawn / rollE2eSetPoolAutoHunger / rollE2eAddPoolKindSpawn / rollE2eExpectBroadcast")
    print("  rollE2eWaitForDiceTray / rollE2eSpawnActivePool / rollE2ePrepareRollRelease")
    print("  rollState(color)         - Print active roll record for a player")
    print("  rollConfirm(color, expected) - Assert roll state; prints PASS/FAIL (E2E)")
    print("  rollConfirmTracker(color, { hunger?, stains?, willpowerSuperficial? })")
    print("  rollStConfirm({ liveSlotIndex?, initiateBlocked? }) - ST slots E2E")
    print("  rollE2eSettlePresetCheck(color, faces, opts) - Release + startRolling + preset + settle; may return wait seconds")
    print("  rollE2eSettlePresetCheckResume(color) - Finish settle after async drawer release wait")
    print("  rollE2eOpenRoll(color) - ST Open (SETUP → PRE_ROLL) after SETUP bag build")
    print("  setHumanityStains(color, n) / setWillpowerSuperficial(color, n)")
    print("  rollForceConfirm(color)  - Force-confirm POST_ROLL (UI CONFIRM)")
    print("  rollCancel(color)        - Cancel roll; Black also clears ST slots/drawers")
    print("  rollCancelAll()          - Cancel all rolls + ST slots")
    print("  printHeader(text, level) - E2E banner (level 1..3: *, =, -; 100 chars or lead+text when long)")
    print("  rollStTest(label?, type?) - ST NPC roll on Black (e2e seat/camera prep)")
    print("  rollStSlots()            - Print gameState.storytellerRolls")
    print("\nGAMEBOARD E2E (TOR-141 — .dev/E2E Playbooks/Gameboard-E2E.md):")
    print("  gbE2ePrereqCheck()       - Boards, tokens, fixture NPCs, scene row")
    print("  gbE2eReset()             - Empty placements + fixture preload baseline")
    print("  gbE2eRunSmoke()          - Smoke macro; then human scene Apply + gbE2eContinue()")
    print("  gbE2eRunFull()           - Deep reconcile suites (+ optional reload gate)")
    print("  gbE2eRunDeferred()       - Expected FAIL until TOR-172/173/175/174 dedupe")
    print("  gbE2eContinue()          - Resume after HUMAN GATE (scene_apply / reload)")
    print("  gbE2eVerifyPcTokens()    - PC control tokens pinned + flip matches seat presence")
    print("  gbConfirm(label, expected) - Single assert (placements, intent, probes)")
    print("  dumpNpcPlacements()      - Print sessionScene.npcWorld.placements")
    print("  profileGameboardApply()  - Apply + hitch profile (phaseA_ms, catalog_builds) _(TOR-201)_")
    print("  profileGameboardClear()  - Clear + hitch profile")
    print("  profileGameboardTokenDrop() - Last token-drop profile (drop first)")
    print("  resetGameboardProfile()  - Clear profile snapshot")
    print("\nQUICK SETTERS (prefer Scenes/Sound panels for narrative apply):")
    print("  setHunger(color, value)  - Set player hunger (e.g., setHunger('Red', 3))")
    print("  soundscapeMusic(mood)    - Set music mood: main, intrigue, combat")
    print("  soundscapeWeather(key, isIndoors?) - Set weather preset: none, lightRain, heavyRain, wind, thunderstorm")
    print("  soundscapeRain(key)      - Set rain layer: none, lightRain, heavyRain")
    print("  soundscapeWind(key)      - Set wind layer: none, lowWindAmbient, whistlingWind1/2/3, windyLeaves1/2")
    print("  soundscapeThunder(mode, hit?) - Enable/disable scheduled thunder, or mode='hit' to trigger one")
    print("  soundscapeFeatured(key)  - Play featured music: TR_Full, TR_Intro, STB_HouseOfTheRisingSun")
    print("  soundscapeStopFeatured() - Stop featured lane only")
    print("  soundscapeResumeBackground() - Resume saved background music context")
    print("  soundscapeLocation(key)  - Set location ambience: airport, industrial, sewers, warehouse, none")
    print("  soundscapeStopAll()      - Stop all soundscape layers through the silent loop")
    print("  DEBUG.getLookAtOffset(obj)    - Read stored look-at calibration offset (Vector)")
    print("  DEBUG.calibrateLookAtOffset(obj, target, roundIntervalDeg?)")
    print("                          - Calibrate per-object look-at correction using current rotation")
    print("  DEBUG.setRotationLookAt(obj, target, useCalibration?)")
    print("                          - Apply look-at rotation (uses calibration by default)")
    print("  DEBUG.setCenterObject(centerObjOrGuid) - Store center object for coordinate conversions")
    print("  DEBUG.clearCenterObject()              - Clear stored center object")
    print("  DEBUG.setPositionCylindrical(obj, radius, angleDeg, height, target?)")
    print("                          - Position using cylindrical coords relative to stored center")
    print("  DEBUG.setPositionSpherical(obj, radius, angleDeg, angle2Deg, target?)")
    print("                          - Position using spherical coords relative to stored center")
    print("  DEBUG.spawnCustomUiUploadBatchFromManifest(manifest, options?)")
    print("                          - Spawn temporary upload tokens from manifest table")
    print("  DEBUG.spawnCustomUiUploadBatchFromJson(jsonString, options?)")
    print("                          - Spawn temporary upload tokens from JSON manifest string")
    print("  DEBUG.loadCustomUiUploadManifest(moduleName?)")
    print("                          - Require generated manifest module (default: lib.custom_ui_upload_manifest)")
    print("  DEBUG.spawnCustomUiUploadBatch(options?, moduleName?)")
    print("                          - Spawn from loaded/default manifest module")
    print("  DEBUG.spawnNpcTokenUploadBatch(options?)")
    print("                          - Spawn tokenFront_* / tokenBack_* from lib.npc_token_upload_manifest")
    print("  DEBUG.spawnNpcGroupUploadBatch(options?)")
    print("                          - Spawn Figurine_Custom pairs + token temps from lib.npc_group_upload_manifest")
    print("  DEBUG.spawnNpcControlBoardTokens(options?)")
    print("                          - Spawn 61 paired gameboard tokens on CONTROL_BOARD (palette grid)")
    print("  DEBUG.clearNpcControlBoardTokens()")
    print("                          - Delete npc_control_token objects")
    print("  DEBUG.applyNpcControlTokenHostedImages()")
    print("                          - After upload+extract, refresh token images from hosted URLs")
    print("  DEBUG.clearCustomUiUploadTokens()")
    print("                          - Delete temporary upload tokens created by debug uploader")
    print("\nLIGHT FEASIBILITY (workshop investigation):")
    print("  testSpawnLightType(), testCloneLight(guid), testInspectLight(guid)")
    print("  testSpawnLight(...), testCompareStructures(guid), findPotentialLights()")
    print("\nHELP:")
    print("  debugHelp()              - Show this help message")
    print("\n" .. string.rep("=", 70))
end

--[[
    Expose Functions Globally
    These functions can be called from the TTS console via: lua functionName()
    NOTE: In TTS Lua, assigning without 'local' makes variables global.
    No need for _G syntax - just assign directly.
]]

compareHandFrames = DEBUG.compareHandFrames
compareLayoutPaths = DEBUG.compareLayoutPaths
refreshSeatRigsFromReference = DEBUG.refreshSeatRigsFromReference
syncTableSimplified = DEBUG.syncTableSimplified

-- Expose inspection functions
showState = DEBUG.showState
showScene = DEBUG.showScene
inspectSoundscapeAudio = DEBUG.inspectSoundscapeAudio

-- Expose quick setters
setHunger = DEBUG.setHunger
setHumanityStains = DEBUG.setHumanityStains
setWillpowerSuperficial = DEBUG.setWillpowerSuperficial
soundscapeMusic = DEBUG.soundscapeMusic
soundscapeWeather = DEBUG.soundscapeWeather
soundscapeRain = DEBUG.soundscapeRain
soundscapeWind = DEBUG.soundscapeWind
soundscapeThunder = DEBUG.soundscapeThunder
soundscapeFeatured = DEBUG.soundscapeFeatured
soundscapeStopFeatured = DEBUG.soundscapeStopFeatured
soundscapeResumeBackground = DEBUG.soundscapeResumeBackground
soundscapeLocation = DEBUG.soundscapeLocation
soundscapeStopAll = DEBUG.soundscapeStopAll

-- Expose dice system debug helpers
rollTest      = DEBUG.rollTest
rollTestNoDiff = DEBUG.rollTestNoDiff
rollE2eSeatPrep = DEBUG.rollE2eSeatPrep
rollSetFaces  = DEBUG.rollSetFaces
rollE2eApplyConditions = DEBUG.rollE2eApplyConditions
rollE2eClearConditions = DEBUG.rollE2eClearConditions
rollState     = DEBUG.rollState
rollCancel    = DEBUG.rollCancel
rollCancelAll = DEBUG.rollCancelAll
RunTest       = DEBUG.RunTest
printHeader   = DEBUG.printHeader
rollConfirm       = DEBUG.rollConfirm
rollConfirmTracker = DEBUG.rollConfirmTracker
rollForceConfirm  = DEBUG.rollForceConfirm
rollStTest    = DEBUG.rollStTest
rollStConfirm = DEBUG.rollStConfirm
rollStSlots   = DEBUG.rollStSlots
rollE2eSetPoolAndSpawn    = DEBUG.rollE2eSetPoolAndSpawn
rollE2eSetPoolAutoHunger  = DEBUG.rollE2eSetPoolAutoHunger
rollE2eAddPoolKindSpawn   = DEBUG.rollE2eAddPoolKindSpawn
rollE2eSettlePresetCheck  = DEBUG.rollE2eSettlePresetCheck
rollE2eSettlePresetCheckResume = DEBUG.rollE2eSettlePresetCheckResume
rollE2eOpenRoll           = DEBUG.rollE2eOpenRoll
rollE2eWaitForDiceTray    = DEBUG.rollE2eWaitForDiceTray
rollE2eSpawnActivePool    = DEBUG.rollE2eSpawnActivePool
rollE2ePrepareRollRelease = DEBUG.rollE2ePrepareRollRelease
rollE2eConfirmBagEnabled   = DEBUG.rollE2eConfirmBagEnabled
rollE2eExpectBroadcast    = DEBUG.rollE2eExpectBroadcast
tableRotateTo = DEBUG.tableRotateTo
spawnCustomUiUploadBatchFromManifest = DEBUG.spawnCustomUiUploadBatchFromManifest
spawnCustomUiUploadBatchFromJson = DEBUG.spawnCustomUiUploadBatchFromJson
spawnCustomUiUploadBatch = DEBUG.spawnCustomUiUploadBatch
spawnNpcTokenUploadBatch = DEBUG.spawnNpcTokenUploadBatch
spawnNpcGroupUploadBatch = DEBUG.spawnNpcGroupUploadBatch
spawnNpcControlBoardTokens = DEBUG.spawnNpcControlBoardTokens
clearNpcControlBoardTokens = DEBUG.clearNpcControlBoardTokens
gbConfirm = DEBUG.gbConfirm
gbE2ePrereqCheck = DEBUG.gbE2ePrereqCheck
gbE2eReset = DEBUG.gbE2eReset
gbE2eRunSmoke = DEBUG.gbE2eRunSmoke
gbE2eRunFull = DEBUG.gbE2eRunFull
gbE2eRunDeferred = DEBUG.gbE2eRunDeferred
gbE2eContinue = DEBUG.gbE2eContinue
gbE2eVerifyPcTokens = DEBUG.gbE2eVerifyPcTokens
applyNpcControlTokenHostedImages = DEBUG.applyNpcControlTokenHostedImages
loadCustomUiUploadManifest = DEBUG.loadCustomUiUploadManifest
clearCustomUiUploadTokens = DEBUG.clearCustomUiUploadTokens

-- Expose file logging functions
logToFile = DEBUG.logToFile
logStateToFile = DEBUG.logStateToFile
logNpcPlacementIntentToFile = DEBUG.logNpcPlacementIntentToFile
logSceneToFile = DEBUG.logSceneToFile
logTestToFile = DEBUG.logTestToFile
logAllToFiles = DEBUG.logAllToFiles

-- Expose help
debugHelp = DEBUG.help

-- Expose light feasibility helpers
testSpawnLightType = DEBUG.testSpawnLightType
testCloneLight = DEBUG.testCloneLight
testInspectLight = DEBUG.testInspectLight
testSpawnLight = DEBUG.testSpawnLight
testCompareStructures = DEBUG.testCompareStructures
findPotentialLights = DEBUG.findPotentialLights

-- Auto-show help on load (optional - comment out if not desired)
-- DEBUG.help()

--- Print active condition ids and resolved effects for one seat (MCP / console diagnosis).
--- @param seatColor string e.g. "Brown"
function DEBUG.dumpConditions(seatColor)
  if type(seatColor) ~= "string" or seatColor == "" then
    error("DEBUG.dumpConditions: seatColor required")
  end
  local S = require("core.state")
  local Conditions = require("core.conditions")
  local playerID = S.getPlayerID(seatColor)
  if type(playerID) ~= "string" then
    print("[DEBUG.dumpConditions] no playerID for seat " .. seatColor)
    return
  end
  local conditions = S.getStateVal("playerData", playerID, "conditions") or {}
  local resolved = Conditions.resolveForPlayer(playerID)
  print("[DEBUG.dumpConditions] seat=" .. seatColor .. " playerID=" .. playerID)
  print("  activeIds: " .. JSON.encode(conditions))
  print("  resolved.statChanges: " .. JSON.encode(resolved.statChanges))
  print("  resolved.hudElementIds: " .. JSON.encode(resolved.hudElementIds))
  print("  resolved.lightingModes: " .. JSON.encode(resolved.lightingModes))
end

--- Print effective stat getters for one seat (MCP / console diagnosis).
--- @param seatColor string e.g. "Brown"
function DEBUG.dumpEffectiveStats(seatColor)
  if type(seatColor) ~= "string" or seatColor == "" then
    error("DEBUG.dumpEffectiveStats: seatColor required")
  end
  local S = require("core.state")
  local ES = require("lib.effective_stats")
  local playerID = S.getPlayerID(seatColor)
  if type(playerID) ~= "string" then
    print("[DEBUG.dumpEffectiveStats] no playerID for seat " .. seatColor)
    return
  end
  local ctx = ES.forPlayer(playerID)
  print("[DEBUG.dumpEffectiveStats] seat=" .. seatColor .. " playerID=" .. playerID)
  print("  trackerMax health=" .. tostring(ctx.trackerMax("health"))
    .. " willpower=" .. tostring(ctx.trackerMax("willpower"))
    .. " humanity=" .. tostring(ctx.trackerMax("humanity")))
  print("  bloodPotencyRating=" .. tostring(ctx.bloodPotencyRating())
    .. " bloodSurge=" .. tostring(ctx.bloodPotencyDerived().bloodSurge))
  print("  humanityHasImpairedSlot=" .. tostring(ctx.humanityHasImpairedSlot()))
  print("  statChanges: " .. JSON.encode(ctx.statChanges))
end

--- Print merged roll policy for the active roll on one seat (MCP / console diagnosis).
--- @param seatColor string e.g. "Brown"
function DEBUG.dumpRollPolicy(seatColor)
  if type(seatColor) ~= "string" or seatColor == "" then
    error("DEBUG.dumpRollPolicy: seatColor required")
  end
  local S = require("core.state")
  local Conditions = require("core.conditions")
  local playerID = S.getPlayerID(seatColor)
  if type(playerID) ~= "string" then
    print("[DEBUG.dumpRollPolicy] no playerID for seat " .. seatColor)
    return
  end
  local active = S.getStateVal("playerData", playerID, "hud", "rollData", "active")
  print("[DEBUG.dumpRollPolicy] seat=" .. seatColor .. " playerID=" .. playerID)
  if type(active) ~= "table" then
    print("  no active roll — resolved policy at initiate would be:")
    print("  " .. JSON.encode(Conditions.resolveRollPolicy(playerID)))
    return
  end
  if type(active.rollPolicy) == "table" then
    print("  active.rollPolicy: " .. JSON.encode(active.rollPolicy))
  else
    print("  active.rollPolicy: (missing — re-initiate roll after deploy)")
    print("  current merge: " .. JSON.encode(Conditions.resolveRollPolicy(playerID)))
  end
end

return DEBUG
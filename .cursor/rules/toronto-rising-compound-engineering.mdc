---
description: Toronto Rising TTS — proactively use Compound Engineering workflows (sessions, plan, review, ship) when they help; do not wait for slash-command syntax.
alwaysApply: true
---

# Toronto Rising — Compound Engineering (proactive use)

The **Compound Engineering** Cursor plugin adds skills and slash commands for planning, debugging, session continuity, code review, git/PR flows, and related workflows. The user should **not** need to memorize `/ce:…` names for agents to do the right thing.

## Default behavior for agents

- **Infer the workflow from the task**, then use the matching Compound Engineering **skill** (read and follow it) or equivalent capability. Natural-language requests count: e.g. “check past sessions,” “structured review before PR,” “plan the implementation,” “commit and open a PR” map to the same outcomes as `/ce:sessions`, `/ce:review`, `/ce:plan`, `/git-commit-push-pr`, etc.
- **Do not wait** for the user to type slash commands unless they prefer that. Prefer acting over asking “should I run `/ce:…`?”

## When to reach for it (this repo)

This project has **long-running** Tabletop Simulator systems (dice, roll pipeline, rotational layout, lighting, workshop objects). Use Compound Engineering **proactively** in these situations:

| Situation | Use |
|-----------|-----|
| Resuming work, debugging something touched before, or avoiding contradicting earlier design | **Session history** (`ce-sessions` skill): search prior agent sessions for this workspace before large edits. |
| Multi-step or cross-module change (several files, unclear sequencing) | **Planning** (`ce-plan` / `ce:work` skills): short plan or structured execution, then implement. |
| Feature or fix is “done” and non-trivial; before merge/PR | **Structured review** (`ce:review` skill): tiered review appropriate to risk/size. |
| Shipping: commit message + PR body should match repo standards | **Git / PR** skills (`git-commit`, `git-commit-push-pr`, `ce-pr-description`): align with `.cursorrules` (Linear refs, clear narrative). |
| After solving a non-obvious bug or pattern worth remembering | **Compound learnings** (`ce:compound` / `cecompound-refresh` when applicable): capture in `docs/solutions/` if that tree exists and the repo uses it. |

## Proportionality

- **Do not** invoke every subagent or skill on every tiny edit.
- **Do** use session lookup and planning/review when uncertainty, scope, or blast radius are high.

## Slash commands (optional for the user)

Slash commands are a **convenience** for humans (e.g. `/ce:sessions`, `/ce:plan`, `/ce:review`, `/git-commit`). Agents should meet the same intent via **skills and tools** without requiring the user to remember syntax.
